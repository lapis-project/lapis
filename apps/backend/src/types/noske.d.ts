/**
 * This file was auto-generated by openapi-typescript.
 * Do not make direct changes to the file.
 */

export interface paths {
	"/search/corp_info": {
		parameters: {
			query?: never;
			header?: never;
			path?: never;
			cookie?: never;
		};
		/** Statistics and information about the whole corpus. */
		get: {
			parameters: {
				query: {
					/**
					 * @description Corpus name. To query your own corpus (e.g. username john, corpus mycorpus), `use` value `user/john/mycorpus`.
					 * @example preloaded/bnc2_tt21
					 */
					corpname: components["parameters"]["001_corpname"];
					/** @description The name of the `subcorpus`. The `default` value `empty string` refers to the entire corpus. An example for `preloaded/bnc2_tt21` can be `Written Academic` or `1960-1974`. */
					usesubcorp?: components["parameters"]["002_usesubcorp"];
					/** @description A parameter to obtain the list of subcorpora and their sizes (e.g. number of tokens, words). */
					subcorpora?: components["parameters"]["006_subcorpora"];
					/** @description A list of grammatical relations from the correspoding `sketch grammar`. */
					gramrels?: components["parameters"]["003_gramrels"];
					/** @description Results of the last corpcheck (if available in the compilation log). */
					corpcheck?: components["parameters"]["004_corpcheck"];
					/** @description The content of the registry file (registry_dump and registry_text). */
					registry?: components["parameters"]["005_registry"];
					/** @description The lexicon sizes of structure attributes. */
					struct_attr_stats?: components["parameters"]["007_struct_attr_stats"];
					/** @description The `format` of the output. `Empty value` is interpreted as `JSON`. Not every endpoint supports all formats. */
					format?: components["parameters"]["008_format"];
				};
				header?: never;
				path?: never;
				cookie?: never;
			};
			requestBody?: never;
			responses: {
				/** @description `OK` */
				200: {
					headers: Record<string, unknown>;
					content: {
						"application/json": components["schemas"]["01_corp_info"];
					};
				};
			};
		};
		put?: never;
		post?: never;
		delete?: never;
		options?: never;
		head?: never;
		patch?: never;
		trace?: never;
	};
	"/search/wordlist": {
		parameters: {
			query?: never;
			header?: never;
			path?: never;
			cookie?: never;
		};
		/**
		 * A list of word frequencies from the specified corpus.
		 * @description This method can be used for generating frequency lists of all tokens, lemmas, word forms etc. or for retrieving frequencies of concrete items. Regex can be used for detailed criteria.
		 */
		get: {
			parameters: {
				query: {
					/**
					 * @description Corpus name. To query your own corpus (e.g. username john, corpus mycorpus), `use` value `user/john/mycorpus`.
					 * @example preloaded/bnc2_tt21
					 */
					corpname: components["parameters"]["001_corpname"];
					/**
					 * @description Sets the corpus attribute you want to work with. Some corpora may have specific additional attributes.
					 *
					 *      Basic examples:
					 *      - word
					 *      - lc
					 *      - lemma
					 *      - lemma_lc
					 *      - lempos
					 *      - lempos_lc
					 *      - tag
					 *      - pos
					 *
					 *      For list of available word list attributes see **\/ca/api/corpora/{corpusId}**
					 */
					wlattr: components["parameters"]["010_wlattr"];
					/** @description The name of the `subcorpus`. The `default` value `empty string` refers to the entire corpus. An example for `preloaded/bnc2_tt21` can be `Written Academic` or `1960-1974`. */
					usesubcorp?: components["parameters"]["002_usesubcorp"];
					/** @description The type of frequency. The values stand for: `frq` -> absolute or raw frequency, `docf` -> document frequency, `arf` -> average reduced frequency, `aldf` -> average logarithmic distance frequency. */
					wlnums?: components["parameters"]["011_wlnums"];
					/** @description Sets the maximum frequency limit in the wordlist. */
					wlmaxfreq?: components["parameters"]["072_wlmaxfreq"];
					/** @description Sets the minimum frequency limit. Items with a lower frequency will not be included. */
					wlminfreq?: components["parameters"]["012_wlminfreq"];
					/**
					 * @description Sets a regex to filter the results. Relevant only in a simple wordlist.
					 * @example .*
					 */
					wlpat?: components["parameters"]["014_wlpat"];
					/** @description Sets the sorting of the results. The values stand for: `frq` -> absolute or raw frequency, `docf` -> document frequency, `arf` -> average reduced frequency, `aldf` -> average logarithmic distance frequency. */
					wlsort?: components["parameters"]["015_wlsort"];
					/** @description A deny list (formerly known as blacklist) is a list of items that should be excluded from the result. The values should be be separated by a newline symbol (without commas between values). In the URL, the newline symbol is `%0A`. */
					wlblacklist?: components["parameters"]["019_wlblacklist"];
					/** @description Includes, or excludes, nonwords in the in the result. Nonwords are tokens which do not start with letter of the alphabet (e.g. numbers, punctuation). The regex to match the nonwords is `[^[:alpha:]].*`. Certain specialized corpora may use their own specific definition of nonwords. */
					include_nonwords?: components["parameters"]["073_include_nonwords"];
					/** @description Includes the relative frequency of each item in the result. */
					relfreq?: components["parameters"]["091_relfreq"];
					/** @description Calculate the document frequency for each item in the result. Must be used with `addfreqs` set to `docf`. */
					reldocf?: components["parameters"]["092_reldocf"];
					/** @description Defines the allow list (formerly known as whitelist), the list of words which should be included in the list. See also `wlblacklist`. */
					wlfile?: components["parameters"]["018_wlfile"];
					/** @description Sets the case sensitivity of the corpus, i.e. the data are extracted from a lowercased version of the corpus. It is used for case insensitive analysis. Parameter "1" means case sensitivity, "0" means case insensitivity. */
					wlicase?: components["parameters"]["071_wlicase"];
					/**
					 * @description Sets the number of items to be returned in the API response. It is not limited for user corpora, in preloaded corpora can be some limitation. This parameter is often used with wlpage to help with pagination in frontend development.
					 * @example 100
					 */
					wlmaxitems?: components["parameters"]["013_wlmaxitems"];
					/** @description To select page of the response. The number of items on the page is specified by parameter wlmaxitems. */
					wlpage?: components["parameters"]["093_wlpage"];
					/** @description The `format` of the output. `Empty value` is interpreted as `JSON`. Not every endpoint supports all formats. */
					format?: components["parameters"]["008_format"];
					/**
					 * @deprecated
					 * @description Parameter that represents if the wordlist is created from the first 10 milions lines of corpus. One if yes, no if he wordlist is created from the whole corpus.
					 */
					random?: components["parameters"]["074_random"];
					/**
					 * @deprecated
					 * @description Parameter to set the format of ouput. Is it always set to `simple`, for the `struct_wordlist` is another enpoint called `struct_wordlist`.
					 */
					wltype?: components["parameters"]["089_wltype"];
					/** @description The minimum n-gram length. Usually used with `ngrams_max_n` and `usengrams`. */
					ngrams_n?: components["parameters"]["063_ngrams_n"];
					/** @description The maximum n-gram length. The maximum is `6`. */
					ngrams_max_n?: components["parameters"]["087_ngrams_max_n"];
					/** @description N-grams which are sub-ngrams of a longer n-gram will be grouped together with the longer n-gram. Nesting only works when a `ngrams_n` and `ngrams_max_n` are different values. */
					nest_ngrams?: components["parameters"]["112_nest_ngrams"];
					/** @description The smoothing parameter for (simple maths) [https://www.sketchengine.eu/documentation/simple-maths/]. */
					simple_n?: components["parameters"]["057_simple_n"];
					/** @description Represent if n-grams should be extracted or just simple keywords. */
					usengrams?: components["parameters"]["088_usengrams"];
				};
				header?: never;
				path?: never;
				cookie?: never;
			};
			requestBody?: never;
			responses: {
				/** @description `OK` */
				200: {
					headers: Record<string, unknown>;
					content: {
						"application/json": components["schemas"]["02_wordlist"];
					};
				};
			};
		};
		put?: never;
		post?: never;
		delete?: never;
		options?: never;
		head?: never;
		patch?: never;
		trace?: never;
	};
	"/search/struct_wordlist": {
		parameters: {
			query?: never;
			header?: never;
			path?: never;
			cookie?: never;
		};
		/**
		 * Provides a list of frequencies in the specified corpus. Offers more flexibility.
		 * @description The difference from the wordlist is that this enpoint allows to customize how the results are displayed.
		 */
		get: {
			parameters: {
				query: {
					/**
					 * @description Corpus name. To query your own corpus (e.g. username john, corpus mycorpus), `use` value `user/john/mycorpus`.
					 * @example preloaded/bnc2_tt21
					 */
					corpname: components["parameters"]["001_corpname"];
					/**
					 * @description Sets the corpus attribute you want to work with. Some corpora may have specific additional attributes.
					 *
					 *      Basic examples:
					 *      - word
					 *      - lc
					 *      - lemma
					 *      - lemma_lc
					 *      - lempos
					 *      - lempos_lc
					 *      - tag
					 *      - pos
					 *
					 *      For list of available word list attributes see **\/ca/api/corpora/{corpusId}**
					 */
					wlattr: components["parameters"]["010_wlattr"];
					/** @description Sets the attributes used for generating the wordlist. Up to 3 attributes are allowed (see wlstruct_attr2 and wlstruct_attr3). Some corpora may contain additional specific attributes. */
					wlstruct_attr1: components["parameters"]["090_wlstruct_attr1"];
					/** @description Additional optional attribute that should be included in the result. */
					wlstruct_attr2?: components["parameters"]["102_wlstruct_attr2"];
					/** @description Additional optional attribute that should be included in the result. */
					wlstruct_attr3?: components["parameters"]["103_wlstruct_attr3"];
					/** @description The type of frequency. The values stand for: `frq` -> absolute or raw frequency, `docf` -> document frequency, `arf` -> average reduced frequency, `aldf` -> average logarithmic distance frequency. */
					wlnums?: components["parameters"]["011_wlnums"];
					/** @description Sets the maximum frequency limit in the wordlist. */
					wlmaxfreq?: components["parameters"]["072_wlmaxfreq"];
					/** @description Sets the minimum frequency limit. Items with a lower frequency will not be included. */
					wlminfreq?: components["parameters"]["012_wlminfreq"];
					/**
					 * @description Sets the number of items to be returned in the API response. It is not limited for user corpora, in preloaded corpora can be some limitation. This parameter is often used with wlpage to help with pagination in frontend development.
					 * @example 100
					 */
					wlmaxitems?: components["parameters"]["013_wlmaxitems"];
					/**
					 * @description Sets a regex to filter the results. Relevant only in a simple wordlist.
					 * @example .*
					 */
					wlpat?: components["parameters"]["014_wlpat"];
					/** @description Sets the sorting of the results. The values stand for: `frq` -> absolute or raw frequency, `docf` -> document frequency, `arf` -> average reduced frequency, `aldf` -> average logarithmic distance frequency. */
					wlsort?: components["parameters"]["015_wlsort"];
					/** @description A deny list (formerly known as blacklist) is a list of items that should be excluded from the result. The values should be be separated by a newline symbol (without commas between values). In the URL, the newline symbol is `%0A`. */
					wlblacklist?: components["parameters"]["019_wlblacklist"];
					/** @description Includes, or excludes, nonwords in the in the result. Nonwords are tokens which do not start with letter of the alphabet (e.g. numbers, punctuation). The regex to match the nonwords is `[^[:alpha:]].*`. Certain specialized corpora may use their own specific definition of nonwords. */
					include_nonwords?: components["parameters"]["073_include_nonwords"];
					/** @description Includes the relative frequency of each item in the result. */
					relfreq?: components["parameters"]["091_relfreq"];
					/** @description Calculate the document frequency for each item in the result. Must be used with `addfreqs` set to `docf`. */
					reldocf?: components["parameters"]["092_reldocf"];
					/** @description Sets the case sensitivity of the corpus, i.e. the data are extracted from a lowercased version of the corpus. It is used for case insensitive analysis. Parameter "1" means case sensitivity, "0" means case insensitivity. */
					wlicase?: components["parameters"]["071_wlicase"];
					/** @description To select page of the response. The number of items on the page is specified by parameter wlmaxitems. */
					wlpage?: components["parameters"]["093_wlpage"];
					/** @description The `format` of the output. `Empty value` is interpreted as `JSON`. Not every endpoint supports all formats. */
					format?: components["parameters"]["008_format"];
					/**
					 * @deprecated
					 * @description Parameter that represents if the wordlist is created from the first 10 milions lines of corpus. One if yes, no if he wordlist is created from the whole corpus.
					 */
					random?: components["parameters"]["074_random"];
					/**
					 * @deprecated
					 * @description Parameter to set the format of ouput. Is it always set to `simple`, for the `struct_wordlist` is another enpoint called `struct_wordlist`.
					 */
					wltype?: components["parameters"]["089_wltype"];
				};
				header?: never;
				path?: never;
				cookie?: never;
			};
			requestBody?: never;
			responses: {
				/** @description `OK` */
				200: {
					headers: Record<string, unknown>;
					content: {
						"application/json": components["schemas"]["12_struct_wordlist"];
					};
				};
			};
		};
		put?: never;
		post?: never;
		delete?: never;
		options?: never;
		head?: never;
		patch?: never;
		trace?: never;
	};
	"/search/wsketch": {
		parameters: {
			query?: never;
			header?: never;
			path?: never;
			cookie?: never;
		};
		/**
		 * The most typical word combinations (collocations)
		 * @description Generates word combinations (collocations) sorted by typicality or frequency and categorized into grammatical relations.
		 */
		get: {
			parameters: {
				query: {
					/**
					 * @description Corpus name. To query your own corpus (e.g. username john, corpus mycorpus), `use` value `user/john/mycorpus`.
					 * @example preloaded/bnc2_tt21
					 */
					corpname: components["parameters"]["001_corpname"];
					/**
					 * @description The base form of the word.
					 * @example test
					 */
					lemma: components["parameters"]["024_lemma"];
					/** @description The part of speech of the lemma. The concrete values depend on the corpus. If the corpus contains the `lempos` attribute and `lpos` is empty, the result defaults to the most frequent part of speech of the lemma. */
					lpos?: components["parameters"]["025_lpos"];
					/** @description The name of the `subcorpus`. The `default` value `empty string` refers to the entire corpus. An example for `preloaded/bnc2_tt21` can be `Written Academic` or `1960-1974`. */
					usesubcorp?: components["parameters"]["002_usesubcorp"];
					/**
					 * @description The minimum frequency of a collocate.  Collocates with a frequency below this limit will not be displayed. It is an integer, unless set to 'auto'. `Auto` sets the limit based on the frequency of the lemma.
					 * @example auto
					 */
					minfreq?: components["parameters"]["027_minfreq"];
					/** @description The minimum [logDice](https://www.sketchengine.eu/my_keywords/logdice/) score of the collocates. */
					minscore?: components["parameters"]["028_minscore"];
					/** @description The minimum similarity between clustered items. Only used when `clustercolls` is set to 1. Lower values produce more groups containing fewer collocations. Higher values produce fewer groups containing more collocations. The same value can produce different results with different words. */
					minsim?: components["parameters"]["031_minsim"];
					/** @description The maximum number of items in each grammatical relation. */
					maxitems?: components["parameters"]["029_maxitems"];
					/** @description Groups collocates by their similarity in meaning. (see `minsim`) in meanings. In the response, a new key `Clust` is added for `Words` in the `Gramrels` key. */
					clustercolls?: components["parameters"]["030_clustercolls"];
					/** @description Used to expand separate page relations (grammatical relations grouped together, e.g. prepositional phrases, will appear as separate relations). */
					expand_seppage?: components["parameters"]["032_expand_seppage"];
					/** @description Switches the sorting of the collocates between `s` (by the score)and `f` (by the absolute frequency). */
					sort_ws_columns?: components["parameters"]["065_sort_ws_columns"];
					/** @description Swithes between results grouped into grammatical relations (as in the default web interface view) or results presented as one long list of collocations not classified into grammatical relations. */
					structured?: components["parameters"]["066_structured"];
					/** @description Used with `bilingual` word sketches. Selects the second corpus to which the corresponding grammatical relations should be aligned. `Example:` 'preloaded/detenten18_rft3'. */
					bim_corpname?: components["parameters"]["067_bim_corpname"];
					/** @description Used with `bilingual` word sketches. Sets the `lemma` in the second corpus. */
					bim_lemma?: components["parameters"]["068_bim_lemma"];
					/** @description Sets the lpos, the part of speeach of the lemma in the of second corpus. Used in bilingual word sketches. */
					bim_lpos?: components["parameters"]["069_bim_lpos"];
					/** @description The `format` of the output. `Empty value` is interpreted as `JSON`. Not every endpoint supports all formats. */
					format?: components["parameters"]["008_format"];
				};
				header?: never;
				path?: never;
				cookie?: never;
			};
			requestBody?: never;
			responses: {
				/** @description `OK` */
				200: {
					headers: Record<string, unknown>;
					content: {
						"application/json": components["schemas"]["03_wsketch"];
					};
				};
			};
		};
		put?: never;
		post?: never;
		delete?: never;
		options?: never;
		head?: never;
		patch?: never;
		trace?: never;
	};
	"/search/thes": {
		parameters: {
			query?: never;
			header?: never;
			path?: never;
			cookie?: never;
		};
		/**
		 * Thesaurus - a list of synonyms and similar words.
		 * @description Generates a list of words which are similar in meaning or belong to the same semantic group. The synonys are identified fully automatically by the use of statistics.
		 */
		get: {
			parameters: {
				query: {
					/**
					 * @description Corpus name. To query your own corpus (e.g. username john, corpus mycorpus), `use` value `user/john/mycorpus`.
					 * @example preloaded/bnc2_tt21
					 */
					corpname: components["parameters"]["001_corpname"];
					/**
					 * @description The base form of the word.
					 * @example test
					 */
					lemma: components["parameters"]["024_lemma"];
					/** @description The part of speech of the lemma. The concrete values depend on the corpus. If the corpus contains the `lempos` attribute and `lpos` is empty, the result defaults to the most frequent part of speech of the lemma. */
					lpos?: components["parameters"]["025_lpos"];
					/** @description The name of the `subcorpus`. The `default` value `empty string` refers to the entire corpus. An example for `preloaded/bnc2_tt21` can be `Written Academic` or `1960-1974`. */
					usesubcorp?: components["parameters"]["002_usesubcorp"];
					/** @description Parameter to set the minimum score for the thesaurus to be shown. */
					minthesscore?: components["parameters"]["105_minthesscore"];
					/** @description Parameter to set the maximum number of items to be displayed. */
					maxthesitems?: components["parameters"]["037_maxthesitems"];
					/** @description Parameter for placing words into groups based on their similarity (minsim parameter) in meanings. */
					clusteritems?: components["parameters"]["038_clusteritems"];
					/** @description The minimum similarity between clustered items. Only used when `clustercolls` is set to 1. Lower values produce more groups containing fewer collocations. Higher values produce fewer groups containing more collocations. The same value can produce different results with different words. */
					minsim?: components["parameters"]["031_minsim"];
					/** @description The `format` of the output. `Empty value` is interpreted as `JSON`. Not every endpoint supports all formats. */
					format?: components["parameters"]["008_format"];
				};
				header?: never;
				path?: never;
				cookie?: never;
			};
			requestBody?: never;
			responses: {
				/** @description `OK` */
				200: {
					headers: Record<string, unknown>;
					content: {
						"application/json": components["schemas"]["04_thes"];
					};
				};
			};
		};
		put?: never;
		post?: never;
		delete?: never;
		options?: never;
		head?: never;
		patch?: never;
		trace?: never;
	};
	"/search/wsdiff": {
		parameters: {
			query?: never;
			header?: never;
			path?: never;
			cookie?: never;
		};
		/**
		 * Word sketch difference to compare the behaviour of two words.
		 * @description Returns collocations of two lemmas with scores indicating which collocation is more typical of which lemma.  It is used to observe the differences between the lemmas.
		 */
		get: {
			parameters: {
				query: {
					/**
					 * @description Corpus name. To query your own corpus (e.g. username john, corpus mycorpus), `use` value `user/john/mycorpus`.
					 * @example preloaded/bnc2_tt21
					 */
					corpname: components["parameters"]["001_corpname"];
					/**
					 * @description The base form of the word.
					 * @example test
					 */
					lemma: components["parameters"]["024_lemma"];
					/** @description The part of speech of the lemma. The concrete values depend on the corpus. If the corpus contains the `lempos` attribute and `lpos` is empty, the result defaults to the most frequent part of speech of the lemma. */
					lpos?: components["parameters"]["025_lpos"];
					/**
					 * @description The mode of wsdiff.
					 *
					 *     `Lemma` compares two lemmas via their collocates.
					 *
					 *     `Word form` compares two different word forms of the same lemma.
					 *
					 *     `subcorpus` compares the use of the same lemma in two different subcorpora of the same corpus.
					 */
					diff_by?: components["parameters"]["106_diff_by"];
					/** @description The second lemma to compare. Parameter `diff_by` need to be set to `lemma`. */
					lemma2?: components["parameters"]["033_lemma_2"];
					/**
					 * @description The minimum frequency of a collocate.  Collocates with a frequency below this limit will not be displayed. It is an integer, unless set to 'auto'. `Auto` sets the limit based on the frequency of the lemma.
					 * @example auto
					 */
					minfreq?: components["parameters"]["027_minfreq"];
					/** @description The maximum number of collocates to be returned in single table (common block in response). */
					maxcommon?: components["parameters"]["035_maxcommon"];
					/** @description If set to 1, collocations are sorted into blocks, one for each grammatical relation. If set to 0, only one list is returned, sorted by the score, grammatical relations are ignored. */
					separate_blocks?: components["parameters"]["034_separate_blocks"];
					/** @description The maximum number of collocates for individual lemma to be returned (e.g. lemma1_only, lemma2_only in the response). Parameter `separate_blocks` needs to be set to `1`. */
					maxexclusive?: components["parameters"]["036_maxexclusive"];
					/** @description The first [wordform](https://www.sketchengine.eu/my_keywords/word-form/) of the lemma to compare. `diff_by` must be set to `word form`. */
					wordform1?: components["parameters"]["107_wordform1"];
					/** @description The second [wordform](https://www.sketchengine.eu/my_keywords/word-form/) of the lemma to compare. Parameter `diff_by` must be set to `word form`. */
					wordform2?: components["parameters"]["108_wordform2"];
					/** @description The first subcorpus to compare the lemma. `diff_by` must be set to `subcorpus`. */
					subcorp1?: components["parameters"]["109_subcorp1"];
					/** @description The second subcorpus for searching collocates for wsdiff of the lemma you want. `diff_by` must be set to `subcorpus`. */
					subcorp2?: components["parameters"]["110_subcorp2"];
					/** @description The `format` of the output. `Empty value` is interpreted as `JSON`. Not every endpoint supports all formats. */
					format?: components["parameters"]["008_format"];
				};
				header?: never;
				path?: never;
				cookie?: never;
			};
			requestBody?: never;
			responses: {
				/** @description `OK` */
				200: {
					headers: Record<string, unknown>;
					content: {
						"application/json": components["schemas"]["05_wsdiff"];
					};
				};
			};
		};
		put?: never;
		post?: never;
		delete?: never;
		options?: never;
		head?: never;
		patch?: never;
		trace?: never;
	};
	"/search/concordance": {
		parameters: {
			query?: never;
			header?: never;
			path?: never;
			cookie?: never;
		};
		/**
		 * Concordance - shows the search word or phrase in context.
		 * @description The concordance allows complex criteria for searching the corpus. The queries can combine any data, metadata and annotations in the corpus.
		 *
		 *      It is enough to use **corpname** and **q** (or **JSON**) parameters.
		 *
		 *      `How to make parallel concordance: `
		 *
		 *      [Parallel concordance](https://www.sketchengine.eu/guide/parallel-concordance-searching-translations/) only works with aligned parallel corpora like **OS_en**, **OS_de**.
		 *
		 *      **Please use JSON parameter and these subparameters for parallel corpora (see examples also):**
		 *
		 *      - sel_aligned
		 *
		 *      - queryselector_<align_corpname>
		 *
		 *      - iquery_<align_corpname>
		 *
		 *      - pcq_pos_<align_corpname>
		 *
		 *      - filter_nonempty_<align_corpname>
		 */
		get: {
			parameters: {
				query: {
					/**
					 * @description Corpus name. To query your own corpus (e.g. username john, corpus mycorpus), `use` value `user/john/mycorpus`.
					 *
					 *      **Example for parallel concordance:** "preloaded/OS_en"
					 * @example preloaded/bnc2_tt21
					 */
					corpname: components["parameters"]["173_corpname_concordance"];
					/**
					 * @description ---
					 *
					 *      To query the concordance, use the [CQL](https://www.sketchengine.eu/documentation/cql-basics/)(Corpora Query Language) syntax. This parameter works similarly to the JSON parameter, but it's recommended to use the JSON parameter for more complex queries such as queries with filters or sorts. **Don't use q and json parameters simulataneusly.**
					 *
					 *      ---
					 *
					 *
					 *     **Examples:**
					 *
					 *      ---
					 *
					 *      -> **lc** or **lc suffix** indicates that the lowercased version of the corpus is used. To avoid this, remove the suffix or use **word**.
					 *
					 *      q[lemma_lc="test"] -> Query for the lowercased lemma.
					 *
					 *      q[lc="test"] -> Query for the lowercased word.
					 *
					 *      q[lc="car" | lemma_lc="car"] -> Finds words that match the search word(s) or whose lemma matches the search word. Case insensitive.
					 *
					 *      q[lempos_lc="(pretty)-j"] -> adjective.
					 *
					 *      q[lempos_lc="(quickly)-a"] -> adverb.
					 *
					 *      q[lempos_lc="(but)-c"] -> conjunction.
					 *
					 *      q[lempos_lc="(test)-n"] -> noun.
					 *
					 *      q[lempos_lc="(two)-m"] -> numeral.
					 *
					 *      q[lempos_lc="(for)-i"] -> preposition.
					 *
					 *      q[lempos_lc="(they)-d"] -> pronoun.
					 *
					 *      q[lempos_lc="(run)-v"] -> verb.
					 *
					 *      q[lc="marathon"] [lc="runner"] -> Finds a multi-word phrase exactly as typed.
					 *
					 *      q[lc="running" & tag="J.*"] -> adjective.
					 *
					 *      q[lc="quickly" & tag="RB.?"] -> adverb.
					 *
					 *      q[lc="but" & tag="CC"] -> conjunction.
					 *
					 *      q[lc="a" & tag="DT"] -> determiner.
					 *
					 *      q[lc="car" & tag="N.*"] -> noun.
					 *
					 *      q[lc="car" & tag="NN"] -> noun singular.
					 *
					 *      q[lc="cars" & tag="NNS"] -> noun plural.
					 *
					 *      q[lc="two" & tag="CD"] -> numeral.
					 *
					 *      q[word=".*g.*"] -> Finds tokens containing a specific character or a string of characters.
					 * @example q[lc="test"|lemma_lc="test"]
					 */
					q?: components["parameters"]["041_q"];
					/** @description The name of the `subcorpus`. The `default` value `empty string` refers to the entire corpus. An example for `preloaded/bnc2_tt21` can be `Written Academic` or `1960-1974`. */
					usesubcorp?: components["parameters"]["002_usesubcorp"];
					/** @description The part of speech of the lemma. The concrete values depend on the corpus. If the corpus contains the `lempos` attribute and `lpos` is empty, the result defaults to the most frequent part of speech of the lemma. */
					lpos?: components["parameters"]["025_lpos"];
					/** @description The attribute applied to tokens in the query which do not have an attribute specified explicitly as part of the query. */
					default_attr?: components["parameters"]["077_default_attr"];
					/** @description A list of comma-delimited attributes that are returned together with each token. Other examples are:`word, lc, lemma, tag` etc.. */
					attrs?: components["parameters"]["058_attrs"];
					/** @description The text type (metadata) for which statistics should be calculated from the concordance. The default is `bncdoc.alltyp` (all available text types are included). Text types (attributes and there values) differ between corpora). You can find them in the response of `corpus_info` method in `freqttattrs` or `subcorpattrs` keys. Not all of them have attributes to show. */
					refs?: components["parameters"]["078_refs"];
					/** @description Determines which tokens will be returned with additional attributes defined in `attrs`. `kw` will add the attributes to the KWIC only. `all` will return them with all tokens. */
					attr_allpos?: components["parameters"]["079_attr_allpos"];
					/**
					 * @description Switches between sentence view, KWIC view and align.
					 *
					 *     `sen` returns complete sentences without trimming them.
					 *
					 *     `kwic` returns the KWIC view with the query in the centre and some context left and right.
					 *
					 *     `align` parallel concordance
					 */
					viewmode?: components["parameters"]["080_viewmode"];
					/**
					 * @description Only used with error-annotated corpora. It determines what should be highlighted. It is set to 'q' for corpora without error annotation. Meaning of individual options:
					 *
					 *      - `q` -> to higlight query result.
					 *
					 *      - `e` -> to higlight errors.
					 *
					 *      - `c` -> to highlight corrections.
					 *
					 *      - `b` -> to highlight both erros and corrections.
					 *
					 *      Example of such a corpus can be `preloaded/enwiki_error_sample_sentences`.
					 */
					cup_hl?: components["parameters"]["081_cup_hl"];
					/** @description A list of comma-delimited structures (=structure tags) that should be included in the result. */
					structs?: components["parameters"]["082_structs"];
					/** @description The number of the page that should be returned. */
					fromp?: components["parameters"]["083_fromp"];
					/** @description The number of lines in the concordance. */
					pagesize?: components["parameters"]["084_pagesize"];
					/** @description The size of the left context in KWIC view. Number of tokens. */
					kwicleftctx?: components["parameters"]["085_kwicleftctx"];
					/** @description The size of the right context in KWIC view. Number of tokens. */
					kwicrightctx?: components["parameters"]["086_kwicrightctx"];
					/** @description (Only for error-annotated corpora.) Determines what should be highlighted. Corr means **correction** and err means **error**. An **example** of such a corpus is `preloaded/enwiki_error_sample_sentences`. */
					errcorr_switch?: components["parameters"]["134_errcorr_switch"];
					/** @description (Only for error-annotated corpora). Determines which error type to higlight. An example of such a corpus is `preloaded/enwiki_error_sample_sentences`. */
					cup_err_code?: components["parameters"]["135_cup_err_code"];
					/** @description (Only for error-annotated corpora.) An error token to search. */
					cup_err?: components["parameters"]["136_cup_err"];
					/** @description (Only for error-annotated corpora.) A correction token to search. */
					cup_corr?: components["parameters"]["137_cup_corr"];
					/**
					 * @description The **json** parameter provides and alternative way to wrap parameters for concordance queries (instead of q parameter). It allows you to send all query parameters in a single JSON object. If you use **concordance_query** parameter you should not use the **q** parameter.
					 *
					 *      Provided examples are not complete concordance requests but only single parameter from the request.
					 *
					 *
					 *     ---
					 *
					 *      `Concordance:`
					 *
					 *      ---
					 *
					 *
					 *     - **concordance_query** -> A list of concordance parameters.
					 *
					 *      - **lpos** -> Specifies the part of speech for the lemma. Options: **""** (any), **-n** (noun), **-v** (verb), **-j** (adjective), **-a** (adverb), **-d** (pronoun), **-i** (preposition), **-c** (conjunction), **-m** (numeral).
					 *
					 *      - **qmcase** -> Determines [case sensitivity](https://www.sketchengine.eu/blog/case-sensitive-and-insensitive-corpus-analysis/). If set to false, the data are extracted from a lowercased version of the corpus. **Options:** true (case-sensitive), false (case-insensitive).
					 *
					 *      - **queryselector** -> Defines the query type for concordance calculation. Options: **iqueryrow**, **cqlrow**, **lemmarow**, **charrow**, **phraserow**, **wordrow**. Default is **iqueryrow**.
					 *
					 *      	- **iqueryrow:**
					 *
					 *      		- **iquery** -> For **iqueryrow**. Use symbols like `*` for any number of unspecified characters. Use a `?` for exactly one unspecified character. Use the `|` to include more than one word or phrase. Use `--` to find a word which is hyphenated, non-hyphenated or spelt as two separate words.
					 *
					 *      	- **cqlrow:**
					 *
					 *      		- **cql** -> For **cqlrow**. Write queries using [Corpus query language (cql)](https://www.sketchengine.eu/documentation/corpus-querying/).
					 *
					 *      	- **lemmarow:**
					 *
					 *      		- **lemma** -> For **lemmarow**. Input the lemma. Supports regex.
					 *
					 *      	- **charrow:**
					 *
					 *      		- **char** -> For **charrow**. Define characters within tokens. Supports regex.
					 *
					 *      	- **phraserow:**
					 *
					 *      		- **phrase** -> For **phraserow**. Input the phrase. Supports regex.
					 *
					 *      	- **wordrow:**
					 *
					 *      		- **word** -> For **wordrow**. Input word forms. Supports regex.
					 *
					 *      - **default_attr** -> Defines the default attribute (from queryselector subattributes) type for concordance queries.
					 *
					 *      - **random** -> Optional parameter, set to 1 if you want to use Random Sample. By default 0.
					 *
					 *
					 *     **Examples:**
					 *
					 *      json={"concordance_query":[{"queryselector":"cqlrow","cql":"<doc>","default_attr":""}]}
					 *
					 *      json={"concordance_query":[{"queryselector":"lemmarow","lemma":"car","lpos":"-n","qmcase":false}]}
					 *
					 *      json={"concordance_query":[{"queryselector":"iqueryrow","iquery":"car","random":1},{"q":"r200"}]}
					 *
					 *
					 *     ---
					 *
					 *      `Filters:`
					 *
					 *      ---
					 *
					 *
					 *     Filters are applied as a separate JSON sub-objects, enabling more advanced control over the concordance query. Multiple filters can be chained in a single query.
					 *     - **queryselector** -> Same purpose as in non-filtered queries. Options: **iquery**, **cql**, **lemma**, **char**, **phrase**, **word**.
					 *
					 *      - **pnfilter** -> Possitive/negative filter. Options: **p** (positive), **n** (negative).
					 *
					 *      - **inclkwic** -> Include or exclude keyword in context (KWIC). Options: **true**, **false**.
					 *
					 *      - **lpos** -> Filter by lemma part of speech. Options: **""** (any), **-n** (noun), **-v** (verb), **-j** (adjective), **-a** (adverb), **-d** (pronoun), **-i** (preposition), **-c** (conjunction), **-m** (numeral).
					 *
					 *      - **wpos** -> Word part of speech. Options: J.* (adjective), RB.? (adverb), CC (conjunction), DT (determiner), N.* (noun), CD (numeral), RP (particle), IN (preposition), PP.? (pronoun), V.* (verb).
					 *
					 *      - **qmcase** -> Determines [case sensitivity](https://www.sketchengine.eu/blog/case-sensitive-and-insensitive-corpus-analysis/). If set to false, the data are extracted from a lowercased version of the corpus. **Options:** true (case-sensitive), false (case-insensitive).
					 *
					 *      - **filfpos** -> Define the (front) left from KWIC position for filtering. Example: **-5**.
					 *
					 *      - **filtpos** -> Define the (tail) right from KWIC position for filtering. Example: **5**.
					 *
					 *
					 *     **Examples with filters:**
					 *
					 *      json={"concordance_query":[{"queryselector":"iqueryrow","iquery":"test"},{"pnfilter":"p","inclkwic":true,"iquery":"test","filfpos":"-1:s","filtpos":"1:s","queryselector":"iqueryrow"}]}
					 *
					 *      json={"concordance_query":[{"queryselector":"lemmarow","lemma":"mom","lpos":"-n","qmcase":false},{"queryselector":"lemmarow","pnfilter":"p","inclkwic":true,"lpos":"-n","qmcase":false,"filfpos":-4,"filtpos":4,"lemma":"dad"}]}
					 *
					 *
					 *     ---
					 *
					 *      `Sorting:`
					 *
					 *      ---
					 *
					 *
					 *     Sorts are also applied as a separate JSON sub-object within the concordance_query. See examples for usage.
					 *     - **mlsort_options** -> An array or object for sorting options.
					 *
					 *      - **skey** -> Sort key.
					 *
					 *      - **attr** -> Attribute to sort by. The list of available attributes can be retrieved using the **corpinfo endpoint**.
					 *
					 *      - **ctx** -> Context for sorting.
					 *
					 *      - **bward** -> Direction of sorting (backward or forward).
					 *
					 *      - **icase** -> Case sensitivity for sorting. Options: **i** (insensitive) or empty (sensitive). More details on [case sensitivity](https://www.sketchengine.eu/blog/case-sensitive-and-insensitive-corpus-analysis/).
					 *
					 *
					 *     **Examples with sorts:**
					 *
					 *      json={"concordance_query":[{"queryselector":"cqlrow","cql":"[lemma="car"]","default_attr":"lemma"},{"queryselector":"iqueryrow","pnfilter":"n","inclkwic":false,"lpos":"","wpos":"","qmcase":false,"filfpos":"-2:s","filtpos":"2:s","iquery":"test"},{"mlsort_options":[{"skey":"kw","attr":"word","ctx":"0","bward":"","icase":"i"}]}]}
					 *
					 *
					 *     ---
					 *
					 *      `Parallel Concordance:`
					 *
					 *      ---
					 *
					 *
					 *     Parallel concordance queries can handle multiple aligned corpora.
					 *     - **sel_aligned** -> List of aligned corpora for parallel concordance. Example: **["OS_en"**, **"OS_sk"]**.`
					 *
					 *      - **pcq_pos_neg_<aligned_corpora>** -> Positive or negative match. Options: **pos**, **neg**.
					 *
					 *      - **filter_nonempty_<aligned_corpora>** -> Include unmatched results. Options: **on**, **""** (off).
					 *
					 *      - **queryselector_<aligned_corpora>** -> Query type for aligned corpora. Works similarly to non-parallel queries (see descriptions in Concordance part).
					 *
					 *      	- **iquery_<aligned_corpora>**
					 *
					 *      	- **cql_<aligned_corpora>**
					 *
					 *      	- **lemma_<aligned_corpora>**
					 *
					 *      	- **char_<aligned_corpora>**
					 *
					 *      	- **phrase_<aligned_corpora>**
					 *
					 *      	- **word_<aligned_corpora>**
					 *
					 *
					 *     **Example:**
					 *
					 *      json={"concordance_query": [{"queryselector": "iqueryrow","iquery": "cat","sel_aligned": ["OS_sk"],"queryselector_OS_sk": "iqueryrow","iquery_OS_sk": "mačka","pcq_pos_neg_OS_sk": "pos","filter_nonempty_OS_sk": "on"}]}
					 *
					 *
					 *     ---
					 *
					 *      `Random Sample:`
					 *
					 *      ---
					 *
					 *
					 *     Random sample is used to reduce the number of concordance lines while preserving the representativeness of the sample. The number of lines defined by the user will be randomly selected from all parts of the corpus. A random sample with the same number of lines generated from the same concordance will always produce exactly the same concordance lines. This behaviour is intentional so that different users (e.g. students) can follow the same steps and arrive at the same result. To generate a different sample, a different number should be used. Setting the value to 201 instead of 200 will generate a completely different sample.
					 *     - **queryselector** -> Same purpose as in non-filtered queries.
					 *
					 *      - **q** -> To set the number of lines of random sample. The 'r' in front of the number is required.
					 *
					 *
					 *     **Example:**
					 *
					 *      json={"concordance_query":[{"queryselector":"iqueryrow","iquery":"car","random":1},{"q":"r200"}]}
					 */
					json?: components["parameters"]["040_json"];
					/** @description Switches the asynchronous processing on/off. ON = partial results are returned as soon as the first page is filled with results. OFF = results are returned only after the search is completed. Normally, ON is used in the web interface and OFF when using the API. */
					asyn?: components["parameters"]["039_asyn"];
					/** @description The `format` of the output. `Empty value` is interpreted as `JSON`. Not every endpoint supports all formats. */
					format?: components["parameters"]["008_format"];
				};
				header?: never;
				path?: never;
				cookie?: never;
			};
			requestBody?: never;
			responses: {
				/** @description `OK` */
				200: {
					headers: Record<string, unknown>;
					content: {
						"application/json": components["schemas"]["06_concordance"];
					};
				};
			};
		};
		put?: never;
		post?: never;
		delete?: never;
		options?: never;
		head?: never;
		patch?: never;
		trace?: never;
	};
	"/search/fullref": {
		parameters: {
			query?: never;
			header?: never;
			path?: never;
			cookie?: never;
		};
		/**
		 * Returns all metadata of one concordance line.
		 * @description Displays all available text types (metadata) related to the concrete KWIC (hit) defined by its position in the corpus.
		 */
		get: {
			parameters: {
				query: {
					/**
					 * @description Corpus name. To query your own corpus (e.g. username john, corpus mycorpus), `use` value `user/john/mycorpus`.
					 * @example preloaded/bnc2_tt21
					 */
					corpname: components["parameters"]["001_corpname"];
					/** @description The position of the first token of KWIC in the corpus. */
					pos?: components["parameters"]["111_pos"];
				};
				header?: never;
				path?: never;
				cookie?: never;
			};
			requestBody?: never;
			responses: {
				/** @description `OK` */
				200: {
					headers: Record<string, unknown>;
					content: {
						"application/json": components["schemas"]["14_fullref"];
					};
				};
			};
		};
		put?: never;
		post?: never;
		delete?: never;
		options?: never;
		head?: never;
		patch?: never;
		trace?: never;
	};
	"/search/widectx": {
		parameters: {
			query?: never;
			header?: never;
			path?: never;
			cookie?: never;
		};
		/**
		 * Returns extended context the KWIC in a concrete concordance line.
		 * @description This is the equivalent of clicking KWIC in one concordance line which displays a popup with an extended context.
		 */
		get: {
			parameters: {
				query: {
					/**
					 * @description Corpus name. To query your own corpus (e.g. username john, corpus mycorpus), `use` value `user/john/mycorpus`.
					 * @example preloaded/bnc2_tt21
					 */
					corpname: components["parameters"]["001_corpname"];
					/** @description The position of the first token of KWIC in the corpus. */
					pos?: components["parameters"]["111_pos"];
					/** @description Only used by the web interface. Indicates the number of tokens that should be highlighted in red. */
					hitlen?: components["parameters"]["138_hitlen"];
					/** @description A list of comma-delimited structures (=structure tags) that should be included in the result. */
					structs?: components["parameters"]["082_structs"];
					/** @description Size of the left context in tokens. */
					detail_left_ctx?: components["parameters"]["139_detail_left_ctx"];
					/** @description Size of the right context in tokens. */
					detail_right_ctx?: components["parameters"]["140_detail_right_ctx"];
				};
				header?: never;
				path?: never;
				cookie?: never;
			};
			requestBody?: never;
			responses: {
				/** @description `OK` */
				200: {
					headers: Record<string, unknown>;
					content: {
						"application/json": components["schemas"]["16_widectx"];
					};
				};
			};
		};
		put?: never;
		post?: never;
		delete?: never;
		options?: never;
		head?: never;
		patch?: never;
		trace?: never;
	};
	"/search/freqml": {
		parameters: {
			query?: never;
			header?: never;
			path?: never;
			cookie?: never;
		};
		/**
		 * Calculates frequencies of words, lemmas… in the concordance.
		 * @description The frequency of any [positional attribute] (https://www.sketchengine.eu/my_keywords/positional-attribute/) such as word forms, lemmas, tags can be counted with this method. Structure attributes (metadata/text types) can also be counted.
		 */
		get: {
			parameters: {
				query: {
					/**
					 * @description Corpus name. To query your own corpus (e.g. username john, corpus mycorpus), `use` value `user/john/mycorpus`.
					 * @example preloaded/bnc2_tt21
					 */
					corpname: components["parameters"]["001_corpname"];
					/** @description Used to count the frequency of positional attributes [attributes](https://www.sketchengine.eu/my_keywords/positional-attribute/) or structure attributes (metadata/text types) of any token in the concordance.\n\n A maximum of 6 attributes is allowed (e.g. ml2attr, ml3attr). At least one attribute is required. */
					ml1attr: components["parameters"]["100_ml1attr"];
					/**
					 * @description Position of the selected attribute in the concordance. **Minus** means **left** context (-1<0). **Plus** means **right** context (6>0). **At least one attribute is required, the others are optional.** Every attribute (ml1attr, ml2attr. etc.) needs it's **own** context position (e.g. if 3 attributes are selected three context position **needs to** be set ml1ctx, ml2ctx, ml3ctx).
					 *
					 *
					 *     **Positions can be referenced as follows:**
					 *
					 *      `integer number` - where **0** is the first token in **KWIC**, **-1** the rightmost token in the left context etc.
					 *
					 *      `1:x` - where **x** is one of the corpus structures (e.g. “doc” or “s” if the corpus has the particular markup). Its meaning is the first token in the structure, except when it is the right boundary of a range - then it is the last token in the structure. Also, other numbers can be used, e.g. -2:x, 3:x, etc. (-1 is the same as 1 with meaning “structure containing KWIC”)
					 *
					 *      `a<0` - where **a** stands for a position reference as described in the first two points with meaning '**a** positions before/after the firs KWIC position' (so this is equivalent to **a**)
					 *
					 *      `a>0` - where **a** stands for the same position reference with meaning 'positions before/after the last KWIC position'
					 *
					 *      in the previous two points, if **0** is substituted with a natural number **k**, it means 'before/after **k**-th collocation' instead of 'before/after KWIC'. Collocations are special token groups in the context, that can be added using positive filters (see below).
					 *
					 *
					 *     `Ranges` can be referenced as a~b where **a**, **b** stand for token identifiers as above. Examples of positions and ranges:
					 *
					 *      `-1<0` - rightmost token in the left context
					 *
					 *      `3>0` - third token in right context
					 *
					 *      `0>0` - last token in KWIC
					 *
					 *      `0<0` - first token in KWIC
					 *
					 *      `0<0~0>0` - range of KWIC
					 *
					 *      `-1<0~1>0` - range of KWIC with one token from the left context and one from the right context
					 *
					 *      `1:s` - first token in the sentence containing KWIC (or its first token)
					 *
					 *      `1:s>0` - first token in the sentence containing KWIC (or its last token)
					 *
					 *      `0<1` - first token of the first-added collocation.
					 *
					 *
					 *     `Examples:`
					 *
					 *      sword/ **1>0~3>0**
					 *
					 *      sword/ **1>0~3>0**
					 *
					 *      slemma/ **0<0~0>0**
					 *
					 *      sword/i **-1**
					 *
					 *      sword/ **0** word/ir **-1<0** tag/r **-2<0**
					 */
					ml1ctx: components["parameters"]["101_ml1ctx"];
					/** @description Used to count the frequency of positional attributes [attributes](https://www.sketchengine.eu/my_keywords/positional-attribute/). Just like `ml1attr` but optional. */
					ml2attr?: components["parameters"]["141_ml2attr"];
					/** @description Position of the selected attribute in the concordance. Minus means left context (-1<0). Plus means right context (6>0). Just like ml1ctx but optional. */
					ml2ctx?: components["parameters"]["142_ml2ctx"];
					/** @description Used to count the frequency of positional attributes [attributes](https://www.sketchengine.eu/my_keywords/positional-attribute/). Just like `ml1attr` but optional. */
					ml3attr?: components["parameters"]["143_ml3attr"];
					/** @description Position of the selected attribute in the concordance. Minus means left context (-1<0). Plus means right context (6>0). Just like ml1ctx but optional. */
					ml3ctx?: components["parameters"]["144_ml3ctx"];
					/** @description Used to count the frequency of positional attributes [attributes](https://www.sketchengine.eu/my_keywords/positional-attribute/). Just like `ml1attr` but optional. */
					ml4attr?: components["parameters"]["145_ml4attr"];
					/** @description Position of the selected attribute in the concordance. Minus means left context (-1<0). Plus means right context (6>0). Just like ml1ctx but optional. */
					ml4ctx?: components["parameters"]["146_ml4ctx"];
					/** @description Used to count the frequency of positional attributes [attributes](https://www.sketchengine.eu/my_keywords/positional-attribute/). Just like `ml1attr` but optional. */
					ml5attr?: components["parameters"]["147_ml5attr"];
					/** @description Position of the selected attribute in the concordance. Minus means left context (-1<0). Plus means right context (6>0). Just like ml1ctx but optional. */
					ml5ctx?: components["parameters"]["148_ml5ctx"];
					/** @description Used to count the frequency of positional attributes [attributes](https://www.sketchengine.eu/my_keywords/positional-attribute/). Just like `ml1attr` but optional. */
					ml6attr?: components["parameters"]["149_ml6attr"];
					/** @description Position of the selected attribute in the concordance. Minus means left context (-1<0). Plus means right context (6>0). Just like ml1ctx but optional. */
					ml6ctx?: components["parameters"]["150_ml6ctx"];
					/**
					 * @description ---
					 *
					 *      To query the concordance, use the [CQL](https://www.sketchengine.eu/documentation/cql-basics/)(Corpora Query Language) syntax. This parameter works similarly to the JSON parameter, but it's recommended to use the JSON parameter for more complex queries such as queries with filters or sorts. **Don't use q and json parameters simulataneusly.**
					 *
					 *      ---
					 *
					 *
					 *     **Examples:**
					 *
					 *      ---
					 *
					 *      -> **lc** or **lc suffix** indicates that the lowercased version of the corpus is used. To avoid this, remove the suffix or use **word**.
					 *
					 *      q[lemma_lc="test"] -> Query for the lowercased lemma.
					 *
					 *      q[lc="test"] -> Query for the lowercased word.
					 *
					 *      q[lc="car" | lemma_lc="car"] -> Finds words that match the search word(s) or whose lemma matches the search word. Case insensitive.
					 *
					 *      q[lempos_lc="(pretty)-j"] -> adjective.
					 *
					 *      q[lempos_lc="(quickly)-a"] -> adverb.
					 *
					 *      q[lempos_lc="(but)-c"] -> conjunction.
					 *
					 *      q[lempos_lc="(test)-n"] -> noun.
					 *
					 *      q[lempos_lc="(two)-m"] -> numeral.
					 *
					 *      q[lempos_lc="(for)-i"] -> preposition.
					 *
					 *      q[lempos_lc="(they)-d"] -> pronoun.
					 *
					 *      q[lempos_lc="(run)-v"] -> verb.
					 *
					 *      q[lc="marathon"] [lc="runner"] -> Finds a multi-word phrase exactly as typed.
					 *
					 *      q[lc="running" & tag="J.*"] -> adjective.
					 *
					 *      q[lc="quickly" & tag="RB.?"] -> adverb.
					 *
					 *      q[lc="but" & tag="CC"] -> conjunction.
					 *
					 *      q[lc="a" & tag="DT"] -> determiner.
					 *
					 *      q[lc="car" & tag="N.*"] -> noun.
					 *
					 *      q[lc="car" & tag="NN"] -> noun singular.
					 *
					 *      q[lc="cars" & tag="NNS"] -> noun plural.
					 *
					 *      q[lc="two" & tag="CD"] -> numeral.
					 *
					 *      q[word=".*g.*"] -> Finds tokens containing a specific character or a string of characters.
					 * @example q[lc="test"|lemma_lc="test"]
					 */
					q?: components["parameters"]["041_q"];
					/** @description The name of the `subcorpus`. The `default` value `empty string` refers to the entire corpus. An example for `preloaded/bnc2_tt21` can be `Written Academic` or `1960-1974`. */
					usesubcorp?: components["parameters"]["002_usesubcorp"];
					/** @description The number of items in one response. */
					fmaxitems?: components["parameters"]["044_fmaxitems"];
					/** @description The number of the response batch (page). The number of items in each batch is specified by `fmaxitems`. */
					fpage?: components["parameters"]["094_fpage"];
					/** @description If there are more attributes (e.g. m1attr, m2attr), the results can be grouped by the first column/attribute. */
					group?: components["parameters"]["095_group"];
					/** @description Includes the percentage of the concordance in the result. */
					showpoc?: components["parameters"]["096_showpoc"];
					/** @description Includes relative in text types value in the result. */
					showreltt?: components["parameters"]["097_showreltt"];
					/** @description Includes the relative frequency in the result. */
					showrel?: components["parameters"]["098_showrel"];
					/** @description The number of attributes for which the frequencies should be counted. */
					freqlevel?: components["parameters"]["099_freqlevel"];
					/** @description The identifier of the sorted column. Use `frq` (default) to sort by frequency. */
					freq_sort?: components["parameters"]["045_freq_sort"];
					/** @description The `format` of the output. `Empty value` is interpreted as `JSON`. Not every endpoint supports all formats. */
					format?: components["parameters"]["008_format"];
				};
				header?: never;
				path?: never;
				cookie?: never;
			};
			requestBody?: never;
			responses: {
				/** @description `OK` */
				200: {
					headers: Record<string, unknown>;
					content: {
						"application/json": components["schemas"]["11_freqml"];
					};
				};
			};
		};
		put?: never;
		post?: never;
		delete?: never;
		options?: never;
		head?: never;
		patch?: never;
		trace?: never;
	};
	"/search/freq_distrib": {
		parameters: {
			query?: never;
			header?: never;
			path?: never;
			cookie?: never;
		};
		/** Provides the distribution of hits in the corpus */
		get: {
			parameters: {
				query: {
					/**
					 * @description Corpus name. To query your own corpus (e.g. username john, corpus mycorpus), `use` value `user/john/mycorpus`.
					 * @example preloaded/bnc2_tt21
					 */
					corpname: components["parameters"]["001_corpname"];
					res?: components["parameters"]["118_res"];
					/** @description The part of speech of the lemma. The concrete values depend on the corpus. If the corpus contains the `lempos` attribute and `lpos` is empty, the result defaults to the most frequent part of speech of the lemma. */
					lpos?: components["parameters"]["025_lpos"];
					/** @description The attribute applied to tokens in the query which do not have an attribute specified explicitly as part of the query. */
					default_attr?: components["parameters"]["077_default_attr"];
					/** @description A list of comma-delimited attributes that are returned together with each token. Other examples are:`word, lc, lemma, tag` etc.. */
					attrs?: components["parameters"]["058_attrs"];
					/** @description A list of comma-delimited structures (=structure tags) that should be included in the result. */
					structs?: components["parameters"]["082_structs"];
					/** @description The text type (metadata) for which statistics should be calculated from the concordance. The default is `bncdoc.alltyp` (all available text types are included). Text types (attributes and there values) differ between corpora). You can find them in the response of `corpus_info` method in `freqttattrs` or `subcorpattrs` keys. Not all of them have attributes to show. */
					refs?: components["parameters"]["078_refs"];
					/** @description Determines which tokens will be returned with additional attributes defined in `attrs`. `kw` will add the attributes to the KWIC only. `all` will return them with all tokens. */
					attr_allpos?: components["parameters"]["079_attr_allpos"];
					/**
					 * @description Switches between sentence view, KWIC view and align.
					 *
					 *     `sen` returns complete sentences without trimming them.
					 *
					 *     `kwic` returns the KWIC view with the query in the centre and some context left and right.
					 *
					 *     `align` parallel concordance
					 */
					viewmode?: components["parameters"]["080_viewmode"];
					fc_lemword_window_type?: components["parameters"]["120_fc_lemword_window_type"];
					fc_lemword_wsize?: components["parameters"]["121_fc_lemword_wsize"];
					fc_lemword_type?: components["parameters"]["122_fc_lemword_type"];
					fc_pos_window_type?: components["parameters"]["125_fc_pos_window_type"];
					fc_pos_wsize?: components["parameters"]["123_fc_pos_wsize"];
					fc_pos_type?: components["parameters"]["124_fc_pos_type"];
					normalize?: components["parameters"]["119_normalize"];
					/** @description The `format` of the output. `Empty value` is interpreted as `JSON`. Not every endpoint supports all formats. */
					format?: components["parameters"]["008_format"];
				};
				header?: never;
				path?: never;
				cookie?: never;
			};
			requestBody?: never;
			responses: {
				/** @description `OK` */
				200: {
					headers: Record<string, unknown>;
					content: {
						"application/json": components["schemas"]["13_freq_distrib"];
					};
				};
			};
		};
		put?: never;
		post?: never;
		delete?: never;
		options?: never;
		head?: never;
		patch?: never;
		trace?: never;
	};
	"/search/freqdist": {
		parameters: {
			query?: never;
			header?: never;
			path?: never;
			cookie?: never;
		};
		/** Utility for web interface only. Provides relative frequency data for wordlist graphs within a specific time period, only for trend corpora. */
		get: {
			parameters: {
				query: {
					/**
					 * @description Corpus name. To query your own corpus (e.g. username john, corpus mycorpus), `use` value `user/john/mycorpus`.
					 * @example preloaded/trends_et_3
					 */
					corpname: components["parameters"]["166_corpname_freqdist"];
					/**
					 * @description Sets the corpus attribute you want to work with. Some corpora may have specific additional attributes.
					 *
					 *      Basic examples:
					 *      - word
					 *      - lc
					 *      - lemma
					 *      - lemma_lc
					 *      - lempos
					 *      - lempos_lc
					 *      - tag
					 *      - pos
					 *
					 *      For list of available word list attributes see **\/ca/api/corpora/{corpusId}**
					 */
					wlattr: components["parameters"]["010_wlattr"];
					/** @description A diachronic attribute to be selected. Available attributes **can differ** in corpora. Examples can be **doc.year** or **doc.month**. */
					diaattr: components["parameters"]["162_diaattr"];
					/**
					 * @description `1`: display results during calculation
					 *     `0`: display results after all data has been calculated (can take quite a lot of time).
					 */
					sse: components["parameters"]["163_sse"];
					/** @description Determines which periods are included in the results. It signifies the percentage above the average size, acting as a **limit**. When relative frequency (rel_frq) surpasses this limit, it is discarded (moved to **removed_freqdist** object). */
					threshold: components["parameters"]["164_threshold"];
					/**
					 * @description **Minus** means **left** context (-1<0). **Plus** means **right** context (6>0).
					 *
					 *
					 *     **Positions can be referenced as follows:**
					 *
					 *      `integer number` - where **0** is the first token, **-1** the rightmost token in the left context etc.
					 *
					 *      `1:x` - where **x** is one of the corpus structures (e.g. “doc” or “s” if the corpus has the particular markup). Its meaning is the first token in the structure, except when it is the right boundary of a range - then it is the last token in the structure. Also, other numbers can be used, e.g. -2:x, 3:x, etc. (-1 is the same as 1 with meaning “structure containing searched word”)
					 *
					 *      `a<0` - where **a** stands for a position reference as described in the first two points with meaning '**a** positions before/after the first searched word position' (so this is equivalent to **a**)
					 *
					 *      `a>0` - where **a** stands for the same position reference with meaning 'positions before/after the last searched word position'
					 *
					 *      in the previous two points, if **0** is substituted with a natural number **k**, it means 'before/after **k**-th collocation' instead of 'before/after KWIC'. Collocations are special token groups in the context, that can be added using positive filters (see below).
					 *
					 *
					 *     `Ranges` can be referenced as a~b where **a**, **b** stand for token identifiers as above. Examples of positions and ranges:
					 *
					 *      `-1<0` - rightmost token in the left context
					 *
					 *      `3>0` - third token in right context
					 *
					 *      `0>0` - last token
					 *
					 *      `0<0` - first token
					 *
					 *      `0<0~0>0` - range
					 *
					 *      `-1<0~1>0` - range with one token from the left context and one from the right context
					 *
					 *      `1:s` - first token in the sentence containing searched word (or its first token)
					 *
					 *      `1:s>0` - first token in the sentence containing searched word (or its last token)
					 *
					 *      `0<1` - first token of the first-added collocation.
					 *
					 *
					 *     `Examples:`
					 *
					 *      sword/ **1>0~3>0**
					 *
					 *      sword/ **1>0~3>0**
					 *
					 *      slemma/ **0<0~0>0**
					 *
					 *      sword/i **-1**
					 *
					 *      sword/ **0**
					 *
					 *      word/ir **-1<0**
					 *
					 *      tag/r **-2<0**
					 */
					ctx?: components["parameters"]["161_ctx"];
					/** @description A wordlist of words for which the relative frequency should be counted. No exact example is here because it is already set in `JSON` parameter.\Example: ['the','a','lion']. */
					wordlist?: components["parameters"]["167_wordlist"];
					/**
					 * @description An optinal way of **wraping parameters**. It is possible to send all relevant parametres via this parameter only. It is classic JSON format.
					 *
					 *      `wordlist`: words for which the relative frequency should be counted.
					 */
					json?: components["parameters"]["165_json_freqdist"];
				};
				header?: never;
				path?: never;
				cookie?: never;
			};
			requestBody?: never;
			responses: {
				/** @description `OK` */
				200: {
					headers: Record<string, unknown>;
					content: {
						"application/json": components["schemas"]["19_freqdist"];
					};
				};
			};
		};
		put?: never;
		post?: never;
		delete?: never;
		options?: never;
		head?: never;
		patch?: never;
		trace?: never;
	};
	"/search/collx": {
		parameters: {
			query?: never;
			header?: never;
			path?: never;
			cookie?: never;
		};
		/** Computes collocation candidates from a concordance. */
		get: {
			parameters: {
				query: {
					/**
					 * @description Corpus name. To query your own corpus (e.g. username john, corpus mycorpus), `use` value `user/john/mycorpus`.
					 * @example preloaded/bnc2_tt21
					 */
					corpname: components["parameters"]["001_corpname"];
					/**
					 * @description ---
					 *
					 *      To query the concordance, use the [CQL](https://www.sketchengine.eu/documentation/cql-basics/)(Corpora Query Language) syntax. This parameter works similarly to the JSON parameter, but it's recommended to use the JSON parameter for more complex queries such as queries with filters or sorts. **Don't use q and json parameters simulataneusly.**
					 *
					 *      ---
					 *
					 *
					 *     **Examples:**
					 *
					 *      ---
					 *
					 *      -> **lc** or **lc suffix** indicates that the lowercased version of the corpus is used. To avoid this, remove the suffix or use **word**.
					 *
					 *      q[lemma_lc="test"] -> Query for the lowercased lemma.
					 *
					 *      q[lc="test"] -> Query for the lowercased word.
					 *
					 *      q[lc="car" | lemma_lc="car"] -> Finds words that match the search word(s) or whose lemma matches the search word. Case insensitive.
					 *
					 *      q[lempos_lc="(pretty)-j"] -> adjective.
					 *
					 *      q[lempos_lc="(quickly)-a"] -> adverb.
					 *
					 *      q[lempos_lc="(but)-c"] -> conjunction.
					 *
					 *      q[lempos_lc="(test)-n"] -> noun.
					 *
					 *      q[lempos_lc="(two)-m"] -> numeral.
					 *
					 *      q[lempos_lc="(for)-i"] -> preposition.
					 *
					 *      q[lempos_lc="(they)-d"] -> pronoun.
					 *
					 *      q[lempos_lc="(run)-v"] -> verb.
					 *
					 *      q[lc="marathon"] [lc="runner"] -> Finds a multi-word phrase exactly as typed.
					 *
					 *      q[lc="running" & tag="J.*"] -> adjective.
					 *
					 *      q[lc="quickly" & tag="RB.?"] -> adverb.
					 *
					 *      q[lc="but" & tag="CC"] -> conjunction.
					 *
					 *      q[lc="a" & tag="DT"] -> determiner.
					 *
					 *      q[lc="car" & tag="N.*"] -> noun.
					 *
					 *      q[lc="car" & tag="NN"] -> noun singular.
					 *
					 *      q[lc="cars" & tag="NNS"] -> noun plural.
					 *
					 *      q[lc="two" & tag="CD"] -> numeral.
					 *
					 *      q[word=".*g.*"] -> Finds tokens containing a specific character or a string of characters.
					 * @example q[lc="test"|lemma_lc="test"]
					 */
					q?: components["parameters"]["041_q"];
					/** @description The name of the `subcorpus`. The `default` value `empty string` refers to the entire corpus. An example for `preloaded/bnc2_tt21` can be `Written Academic` or `1960-1974`. */
					usesubcorp?: components["parameters"]["002_usesubcorp"];
					/** @description The (positional attribute)[https://www.sketchengine.eu/my_keywords/positional-attribute/] (lemma, word form etc.) used in the computation. */
					cattr?: components["parameters"]["046_cattr"];
					/** @description Function according to which the result is sorted. */
					csortfn?: components["parameters"]["053_csortfn"];
					/**
					 * @description Defines the types of statistics (association measures) to be computed.
					 *
					 *     `t` -> T-score
					 *
					 *     `m` -> MI
					 *
					 *     `3` -> MI3
					 *
					 *     `l` -> log likelihood
					 *
					 *     `s` -> min. sensitivity
					 *
					 *     `p` -> MI.log_f
					 *
					 *     `r` -> relative freq.
					 *
					 *     `f` -> absolute freq.
					 *
					 *     `d` -> logDice.
					 *
					 *      To send one value just type the value for example `t`. If you need to send more values write it as `["t","m","d","3","l","s","p"]`.
					 */
					cbgrfns?: components["parameters"]["052_cbgrfns"];
					/** @description The left boundary of the window in which the collocations should be identified. Defined by the token position left or right of KWIC. */
					cfromw?: components["parameters"]["047_cfromw"];
					/** @description The right boundary of the window in which the collocations should be identified. Defined by the token position left or right of KWIC. */
					ctow?: components["parameters"]["048_ctow"];
					/** @description The minimum frequency of the token in the corpus. */
					cminfreq?: components["parameters"]["049_cminfreq"];
					/** @description The minimum frequency of the token in the window defined by `cfromw` and `ctow`. */
					cminbgr?: components["parameters"]["050_cminbgr"];
					/** @description Sets the maximum number of items in the response. */
					cmaxitems?: components["parameters"]["051_cmaxitems"];
					/**
					 * @description An optinal way of **wraping parameters**. It is possible to send all relevant parametres via this parameter only. It is classic JSON format.
					 *
					 *      The most frequent uses are:
					 *
					 *      `queryselector`: To select the query type. Supported options are: **cqlrow**, **iqueryrow**, **lemmarow**, **charrow**, **phraserow**, **wordrow**.
					 *
					 *      `iquery`: Use with `iqueryrow`.
					 *
					 *      `cql`: Use with `cqlrow`.
					 *
					 *      `lemma`: Use with `lemmarow`.
					 *
					 *      `lpos`: The part of speech of the lemma.
					 *
					 *      `qmcase`: Sets the attribute to its lowercased version, i.e. the data are extracted from a lowercased version of the corpus. It used for case insensitive analysis. 1 = case sensitive), 0 = lowercased corpus/case insensitive).
					 *
					 *      `char`: Use with charrow.
					 *
					 *      `phrase`: Use with phraserow.
					 *
					 *      `word`: Use with wordrow
					 *
					 *      `cbgrfns`: If you need to send more cbgrfnsDefines the types of statistics (association measures) to be computed. See example.
					 */
					json?: components["parameters"]["154_json_collx"];
				};
				header?: never;
				path?: never;
				cookie?: never;
			};
			requestBody?: never;
			responses: {
				/** @description `OK` */
				200: {
					headers: Record<string, unknown>;
					content: {
						"application/json": components["schemas"]["10_collx"];
					};
				};
			};
		};
		put?: never;
		post?: never;
		delete?: never;
		options?: never;
		head?: never;
		patch?: never;
		trace?: never;
	};
	"/search/subcorp": {
		parameters: {
			query?: never;
			header?: never;
			path?: never;
			cookie?: never;
		};
		/**
		 * Get a list of subcorpora in the corpus or create/delete a subcorpus.
		 * @description There is two option how to create subcorpora in Sketch Engine from `text types` => json parameter (corpus must be annotated for text types) or from `concordances` => q + struct parameters.
		 */
		get: {
			parameters: {
				query: {
					/**
					 * @description Corpus name. To query your own corpus (e.g. username john, corpus mycorpus), `use` value `user/john/mycorpus`.
					 * @example preloaded/bnc2_tt21
					 */
					corpname: components["parameters"]["001_corpname"];
					/** @description Name of the subcorpus. */
					subcname: components["parameters"]["054_subcname"];
					/** @description Set to `1` if new subcorpus should be created. Subcorpus will not be created if left empty (default value == 0). */
					create?: components["parameters"]["155_create"];
					/** @description Set to `1` if the corpus should be deleted. Only user subcorpora can be deleted. Nothing will be deleted if left empty (default value == 0). */
					delete?: components["parameters"]["055_delete"];
					/**
					 * @description Query for creating subcorpora from concordance.
					 *
					 *     The search criteria are specified within brackets following a prefix like `alemma` or `aword`. This prefix often indicates the type of linguistic search (e.g., lemma-based, word-based). The criteria within the brackets can include checks for specific words, lemmas, parts of speech and more, using operators like | (OR), & (AND), and regular expressions.
					 *
					 *     The lists of available attributes, pos tags for specific corpus can be obtained via `/search/corp_info`.
					 *
					 *
					 *     `Examples:`
					 *
					 *
					 *      - Simple word or lemma search in the BNC corpus:
					 *
					 *         **q=alemma,[lc="test" | lemma_lc="test"]**
					 *
					 *
					 *      - Search for nouns with the lemma 'test' in a case-sensitive manner:
					 *
					 *         **q=alemma,[lempos_lc="(test)-n"]**
					 *
					 *
					 *      - Search for verbs with the lemma 'test', case-insensitive:
					 *
					 *         **q=alemma,[lempos="(test)-v"]**
					 *
					 *
					 *      - Searching for a specific phrase 'test' in a case-sensitive manner:
					 *
					 *         **q=aword,[word="test"]**
					 *
					 *
					 *      - Searching for the numeral '1955':
					 *
					 *         **q=alemma,[word="1955" & tag="CD"]**
					 *
					 *
					 *      - Regex-based search for words containing the character 'h':
					 *
					 *         **q=alemma,[word=".\*h.\*"]**
					 *
					 *
					 *      - Complex search involving the lemma 'book' followed by up to three words, then a verb:
					 *
					 *         **q=alemma,[lemma="book"][]{1,3}[tag="V.\*"]**
					 */
					q?: components["parameters"]["156_q_subcorp"];
					/** @description Which corpus structure should be used in new subcorpus. Used withim concordance type of subcorpus. The lists of structures can be obtained via `/search/corp_info`. */
					struct?: components["parameters"]["157_struct"];
					/**
					 * @description Used to specify text types for a subcorpus. Takes a JSON object as input, where the key-value pairs define the specific attributes. The attributes can vary depending on the corpus.
					 *
					 *      When using the json parameter in a query, you can define a JSON object with one or more attributes. Each attribute can have a single value or an array of values.
					 *
					 *
					 *     The list of available text types for specific corpus can be obtained via `/search/corp_info` just add `sca_` in front the name (see examples).
					 *
					 *
					 *     `Examples:`
					 *
					 *
					 *      - To create a subcorpus based on a specific spoken text type from the BNC corpus:
					 *
					 *         **{"sca_bncdoc.alltyp":["Spoken context-governed"]}**
					 *
					 *
					 *      - To filter texts from the BNC corpus that are both spoken context-governed and spoken demographic:
					 *
					 *         **{"sca_bncdoc.alltyp":["Spoken context-governed","Spoken demographic"]}**
					 *
					 *
					 *      - To select texts from the BNC corpus from a specific time period (1960-1974):
					 *
					 *         **{"sca_bncdoc.alltim":["1960-1974"]}**
					 *
					 *
					 *      - To create a subcorpus with texts from specific authors and time periods, along with regional specifications:
					 *
					 *         **{"sca_bncdoc.author": ["Author1","Author2",...],"sca_bncdoc.alltim": ["1985-1993","1975-1984"], "sca_bncdoc.wripp": ["UK (unspecific)","Ireland"]}**
					 *
					 *
					 *      - To filter texts from the Ententen corpus based on domain and topic:
					 *
					 *         **{"sca_doc.tld":["org","com"], "sca_doc.topic": ["arts","beauty & fashion","cars & bikes","culture & entertainment"]}**
					 *
					 *
					 *      - For a user-specific corpus, filtering based on document ID and filename:
					 *
					 *         **{"sca_doc.id":["file29173711"],"sca_doc.filename":["Filename.pdf"]}**
					 */
					json?: components["parameters"]["160_json_subcorp"];
					/** @description The `format` of the output. `Empty value` is interpreted as `JSON`. Not every endpoint supports all formats. */
					format?: components["parameters"]["008_format"];
				};
				header?: never;
				path?: never;
				cookie?: never;
			};
			requestBody?: never;
			responses: {
				/** @description `OK` */
				200: {
					headers: Record<string, unknown>;
					content: {
						"application/json": components["schemas"]["07_subcorp"];
					};
				};
			};
		};
		put?: never;
		post?: never;
		delete?: never;
		options?: never;
		head?: never;
		patch?: never;
		trace?: never;
	};
	"/search/subcorpus_rename": {
		parameters: {
			query?: never;
			header?: never;
			path?: never;
			cookie?: never;
		};
		/** Rename subcorpus. */
		get: {
			parameters: {
				query: {
					/**
					 * @description Corpus name. To query your own corpus (e.g. username john, corpus mycorpus), `use` value `user/john/mycorpus`.
					 * @example preloaded/bnc2_tt21
					 */
					corpname: components["parameters"]["001_corpname"];
					/** @description The name of subcorpus you want to rename. */
					subcorp_id: components["parameters"]["158_subcorp_id"];
					/** @description A new name for the subcorpus. */
					new_subcorp_name: components["parameters"]["159_new_subcorp_name"];
				};
				header?: never;
				path?: never;
				cookie?: never;
			};
			requestBody?: never;
			responses: {
				/** @description `OK` */
				200: {
					headers: Record<string, unknown>;
					content: {
						"application/json": components["schemas"]["17_subcorpus_rename"];
					};
				};
			};
		};
		put?: never;
		post?: never;
		delete?: never;
		options?: never;
		head?: never;
		patch?: never;
		trace?: never;
	};
	"/search/subcorp_info": {
		parameters: {
			query?: never;
			header?: never;
			path?: never;
			cookie?: never;
		};
		/** Statistics about the subcorpus. */
		get: {
			parameters: {
				query: {
					/**
					 * @description Corpus name. To query your own corpus (e.g. username john, corpus mycorpus), `use` value `user/john/mycorpus`.
					 * @example preloaded/bnc2_tt21
					 */
					corpname: components["parameters"]["001_corpname"];
					/** @description Name of the subcorpus. */
					subcname: components["parameters"]["054_subcname"];
				};
				header?: never;
				path?: never;
				cookie?: never;
			};
			requestBody?: never;
			responses: {
				/** @description `OK` */
				200: {
					headers: Record<string, unknown>;
					content: {
						"application/json": components["schemas"]["18_subcorp_info"];
					};
				};
			};
		};
		put?: never;
		post?: never;
		delete?: never;
		options?: never;
		head?: never;
		patch?: never;
		trace?: never;
	};
	"/search/extract_keywords": {
		parameters: {
			query?: never;
			header?: never;
			path?: never;
			cookie?: never;
		};
		/**
		 * Identifies keywords, key n-grams, key collocations and terms.
		 * @description Keywords, key n-grams, key collocations and terms are identified by comparing the focus corpus (or a subcorpus) to a refrence corpus (or a subcorpus). It is the equivalent of using the Keywords and terms tool or using the key option in n-grams or the word sketch.
		 */
		get: {
			parameters: {
				query: {
					/**
					 * @description Corpus name. To query your own corpus (e.g. username john, corpus mycorpus), `use` value `user/john/mycorpus`.
					 * @example preloaded/bnc2_tt21
					 */
					corpname: components["parameters"]["001_corpname"];
					/** @description Corpus name of the reference corpus, it must have the same processing (the same attributes, the same term grammar). */
					ref_corpname: components["parameters"]["056_ref_corpname"];
					/** @description The name of the `subcorpus`. The `default` value `empty string` refers to the entire corpus. An example for `preloaded/bnc2_tt21` can be `Written Academic` or `1960-1974`. */
					usesubcorp?: components["parameters"]["002_usesubcorp"];
					/** @description The smoothing parameter for (simple maths) [https://www.sketchengine.eu/documentation/simple-maths/]. */
					simple_n?: components["parameters"]["057_simple_n"];
					/** @description Defines the allow list (formerly known as whitelist), the list of words which should be included in the list. See also `wlblacklist`. */
					wlfile?: components["parameters"]["018_wlfile"];
					/** @description A deny list (formerly known as blacklist) is a list of items that should be excluded from the result. The values should be be separated by a newline symbol (without commas between values). In the URL, the newline symbol is `%0A`. */
					wlblacklist?: components["parameters"]["019_wlblacklist"];
					/**
					 * @description Switches between the computation of keywords, terms, key n-grams and key collocations. With keywords and n-grams, it also sets the attribute to be used for the computation.
					 *
					 *      For keywords, set to the required attribute, usually `lc`, `word` or `lemma`.
					 *
					 *      For n-grams, set to the required attribute and set `usengrams`, `ngrams_n` and `ngrams_max_n`.
					 *
					 *      For terms, set the attribute to `TERM`.
					 *
					 *      For collocations (word sketch triples, equivalent of using the Word Sketch with AS A LIST option in the web interace), set the attribute to `WSCOLLOC`. Consider using `wlpat`.
					 */
					attr?: components["parameters"]["115_attr"];
					/** @description Limits the results to items containing only alphanumeric characters. */
					alnum?: components["parameters"]["060_alnum"];
					/** @description Limits the results to items containing at least one alphanumberic character. Words such as 16-year-old or 3D will be included. */
					onealpha?: components["parameters"]["061_onealpha"];
					/**
					 * @description Sets the minimum frequency of the item.
					 * @example auto
					 */
					minfreq?: components["parameters"]["151_minfreq_extract_keywords"];
					/** @description Sets the maximum frequency of the item. */
					maxfreq?: components["parameters"]["152_maxfreq_extract_keywords"];
					max_keywords?: components["parameters"]["062_max_keywords"];
					/** @description Includes, or excludes, nonwords in the in the result. Nonwords are tokens which do not start with letter of the alphabet (e.g. numbers, punctuation). The regex to match the nonwords is `[^[:alpha:]].*`. Certain specialized corpora may use their own specific definition of nonwords. */
					include_nonwords?: components["parameters"]["073_include_nonwords"];
					/** @description Switches to the `lc` attribute, i.e. the lower-cased version of the corpus to allow case insensitive searching. `1` means that case sensitivity is off, `0` means it is on. */
					icase?: components["parameters"]["104_icase"];
					/**
					 * @description Sets a regex to filter the results. Relevant only in a simple wordlist.
					 * @example .*
					 */
					wlpat?: components["parameters"]["014_wlpat"];
					/** @description Represent what kind of frequnecy should be calculated. When used with `reldocf` it is set to `docf` to calculate document frequency. */
					addfreqs?: components["parameters"]["153_addfreqs"];
					/** @description Calculate the document frequency for each item in the result. Must be used with `addfreqs` set to `docf`. */
					reldocf?: components["parameters"]["092_reldocf"];
					/** @description Represent if n-grams should be extracted or just simple keywords. */
					usengrams?: components["parameters"]["088_usengrams"];
					/** @description The minimum n-gram length. Usually used with `ngrams_max_n` and `usengrams`. */
					ngrams_n?: components["parameters"]["063_ngrams_n"];
					/** @description The maximum n-gram length. The maximum is `6`. */
					ngrams_max_n?: components["parameters"]["087_ngrams_max_n"];
					/** @description The `format` of the output. `Empty value` is interpreted as `JSON`. Not every endpoint supports all formats. */
					format?: components["parameters"]["008_format"];
				};
				header?: never;
				path?: never;
				cookie?: never;
			};
			requestBody?: never;
			responses: {
				/** @description `OK` */
				200: {
					headers: Record<string, unknown>;
					content: {
						"application/json": components["schemas"]["08_extract_keywords"];
					};
				};
			};
		};
		put?: never;
		post?: never;
		delete?: never;
		options?: never;
		head?: never;
		patch?: never;
		trace?: never;
	};
	"/search/textypes_with_norms": {
		parameters: {
			query?: never;
			header?: never;
			path?: never;
			cookie?: never;
		};
		/** Returns a list of text types with values. */
		get: {
			parameters: {
				query: {
					/**
					 * @description Corpus name. To query your own corpus (e.g. username john, corpus mycorpus), `use` value `user/john/mycorpus`.
					 * @example preloaded/bnc2_tt21
					 */
					corpname: components["parameters"]["001_corpname"];
				};
				header?: never;
				path?: never;
				cookie?: never;
			};
			requestBody?: never;
			responses: {
				/** @description `OK` */
				200: {
					headers: Record<string, unknown>;
					content: {
						"application/json": components["schemas"]["15_textypes_with_norms"];
					};
				};
			};
		};
		put?: never;
		post?: never;
		delete?: never;
		options?: never;
		head?: never;
		patch?: never;
		trace?: never;
	};
	"/search/attr_vals": {
		parameters: {
			query?: never;
			header?: never;
			path?: never;
			cookie?: never;
		};
		/**
		 * Utility for web interface only. A list of values for a given structure attribute (avattr).
		 * @description Not to be used outside the web interface. Replaced by a more powerful wordlist.
		 */
		get: {
			parameters: {
				query: {
					/**
					 * @description Corpus name. To query your own corpus (e.g. username john, corpus mycorpus), `use` value `user/john/mycorpus`.
					 * @example preloaded/bnc2_tt21
					 */
					corpname: components["parameters"]["001_corpname"];
					/**
					 * @description Selects a structure attribute (reffered to as text type in the web interface). Corpora have different numbers structure attributes and their values. You can find them in the response of the `corpus_info` method in `freqttattrs` or `subcorpattrs` keys. Not all of them have attributes to show.
					 * @example bncdoc.alltyp
					 */
					avattr: components["parameters"]["020_avattr"];
					/** @description A regex to filter the results. Empty string defaults to `.*` (match everything). */
					avpat?: components["parameters"]["021_avpat"];
					/** @description The starting index from which to return the results. */
					avfrom?: components["parameters"]["023_avfrom"];
					/** @description The number of items to return. */
					avmaxitems?: components["parameters"]["022_avmaxitems"];
					/** @description Switches to the `lc` attribute, i.e. the lower-cased version of the corpus to allow case insensitive searching. `1` means that case sensitivity is off, `0` means it is on. */
					icase?: components["parameters"]["104_icase"];
					/** @description The `format` of the output. `Empty value` is interpreted as `JSON`. Not every endpoint supports all formats. */
					format?: components["parameters"]["008_format"];
				};
				header?: never;
				path?: never;
				cookie?: never;
			};
			requestBody?: never;
			responses: {
				/** @description `OK` */
				200: {
					headers: Record<string, unknown>;
					content: {
						"application/json": components["schemas"]["09_attr_vals"];
					};
				};
			};
		};
		put?: never;
		post?: never;
		delete?: never;
		options?: never;
		head?: never;
		patch?: never;
		trace?: never;
	};
	"/ca/api/corpora": {
		parameters: {
			query?: never;
			header?: never;
			path?: never;
			cookie?: never;
		};
		/** Returns a list of all corpora accessible to you. */
		get: {
			parameters: {
				query?: never;
				header?: never;
				path?: never;
				cookie?: never;
			};
			requestBody?: never;
			responses: {
				/** @description `OK` */
				200: {
					headers: Record<string, unknown>;
					content: {
						"application/json": {
							data?: Array<components["schemas"]["03_corpora_list"]>;
						};
					};
				};
			};
		};
		put?: never;
		/**
		 * Creates a new user corpus.
		 * @description Creates a new empty corpus. Use **Documents** endpoints (and optionally **Filesets**) to add data to the corpus.
		 */
		post: {
			parameters: {
				query?: never;
				header?: never;
				path?: never;
				cookie?: never;
			};
			/**
			 * @description Set the language, corpus name and corpus description.
			 *
			 *      - `info` => The additional information for a newly created corpus. (string)
			 *
			 *      - `language_id` => Language iso-code. `ISO 639-1`. (string)
			 *
			 *      - `name` => Unique `corpus name` for a newly created corpus. (string)
			 */
			requestBody: {
				content: {
					"application/json": components["schemas"]["01_corpora_request"];
				};
			};
			responses: {
				/** @description `Created` */
				201: {
					headers: Record<string, unknown>;
					content: {
						"application/json": {
							data?: components["schemas"]["04_corpora_single"];
						};
					};
				};
			};
		};
		delete?: never;
		options?: never;
		head?: never;
		patch?: never;
		trace?: never;
	};
	"/ca/api/corpora/{corpusId}": {
		parameters: {
			query?: never;
			header?: never;
			path: {
				/** @description Numeric corpus ID. For corpora querying. */
				corpusId: components["parameters"]["01_corpus_id"];
			};
			cookie?: never;
		};
		/** Retrieves a user corpus. */
		get: {
			parameters: {
				query?: never;
				header?: never;
				path: {
					/** @description Numeric corpus ID. For corpora querying. */
					corpusId: components["parameters"]["01_corpus_id"];
				};
				cookie?: never;
			};
			requestBody?: never;
			responses: {
				/** @description `OK` */
				200: {
					headers: Record<string, unknown>;
					content: {
						"application/json": components["schemas"]["04_corpora_single"];
					};
				};
				/** @description `Forbidden` */
				403: {
					headers: Record<string, unknown>;
					content: {
						"application/json": components["schemas"]["27_forbidden_normal"];
					};
				};
				/** @description `Not Found` */
				404: {
					headers: Record<string, unknown>;
					content: {
						"application/json": components["schemas"]["57_not_found_404"];
					};
				};
			};
		};
		/** Updates a user corpus. */
		put: {
			parameters: {
				query?: never;
				header?: never;
				path: {
					/** @description Numeric corpus ID. For corpora querying. */
					corpusId: components["parameters"]["01_corpus_id"];
				};
				cookie?: never;
			};
			/**
			 * @description - `expert_mode` => Set to **True** if you are hard-core. (boolean)
			 *
			 *       - `name` => Corpus name. **Given by user**. (string)
			 *
			 *       - `info` => Additional info about corpus. (string)
			 *
			 *       - `document_order` => Can be set to enforce document order within the corpus. (list of integers)
			 *
			 *       - `structures` => Available structures or tags in the corpus. Structures like **s** (sentence), **g** (glue), **doc** (document).(list)
			 *
			 *         - `name` => Structure name. Example: **s**. (string)
			 *
			 *         - `attributes` => A list of used attributes in corpus. (list)
			 *
			 *           - `name` => The name of used attribute. (string)
			 *
			 *       - `file_structure` => The structure in which individual documents should be wrapped. Usually **doc**. (string)
			 *
			 *       - `onion_structure` => The structure for deduplication. Usually **p** (paragraph) or **Null** (no deduplication). (string)
			 *
			 *       - `docstructure` => Structure in which individual documents should be wrapped. Usually **doc**. (string)
			 *       - `sketch_grammar_id` => Name of sketch grammar file. For sketch grammars querying. Sketch grammar is a series of rules written in the CQL query language that search for collocations in a text corpus and categorize them according to their grammatical relations. Example: **preloaded/english-penn_tt-3.3.wsdef.m4**. (string)
			 *
			 *       - `term_grammar_ir` => Name of term grammar file. Term grammar tells Sketch Engine which words and phrases should indentify as terms. Example: **\/corpora/wsdef/english-penn_tt-terms-3.1.termdef.m4**. (string)
			 */
			requestBody?: {
				content: {
					"application/json": components["schemas"]["07_corpus_update"];
				};
			};
			responses: {
				/** @description `OK` */
				200: {
					headers: Record<string, unknown>;
					content: {
						"application/json": {
							data?: components["schemas"]["04_corpora_single"];
						};
					};
				};
				/** @description `Forbidden` */
				403: {
					headers: Record<string, unknown>;
					content: {
						"application/json": components["schemas"]["27_forbidden_normal"];
					};
				};
				/** @description `Not Found` */
				404: {
					headers: Record<string, unknown>;
					content: {
						"application/json": components["schemas"]["57_not_found_404"];
					};
				};
			};
		};
		post?: never;
		/** Deletes a user corpus. */
		delete: {
			parameters: {
				query?: never;
				header?: never;
				path: {
					/** @description Numeric corpus ID. For corpora querying. */
					corpusId: components["parameters"]["01_corpus_id"];
				};
				cookie?: never;
			};
			requestBody?: never;
			responses: {
				/** @description `No Content` */
				204: {
					headers: Record<string, unknown>;
					content?: never;
				};
				/** @description `Forbidden` */
				403: {
					headers: Record<string, unknown>;
					content: {
						"application/json": components["schemas"]["27_forbidden_normal"];
					};
				};
				/** @description `Not Found` */
				404: {
					headers: Record<string, unknown>;
					content: {
						"application/json": components["schemas"]["57_not_found_404"];
					};
				};
			};
		};
		options?: never;
		head?: never;
		patch?: never;
		trace?: never;
	};
	"/ca/api/corpora/{corpusId}/can_be_compiled": {
		parameters: {
			query?: never;
			header?: never;
			path: {
				/** @description Numeric corpus ID. For corpora querying. */
				corpusId: components["parameters"]["01_corpus_id"];
			};
			cookie?: never;
		};
		get?: never;
		put?: never;
		/** Checks if the corpus fulfills all conditions to be compiled. (RPC) */
		post: {
			parameters: {
				query?: never;
				header?: never;
				path: {
					/** @description Numeric corpus ID. For corpora querying. */
					corpusId: components["parameters"]["01_corpus_id"];
				};
				cookie?: never;
			};
			/** @description In this documentation, an empty request is used mostly used with the **RPC style** method where the content in a request is not needed (in most cases). RPC style endpoints focus on performing **one action** right (procedures, command) easier than **REST API**-based endpoints. It is not as scalable as REST API style. RPC is mostly used with HTTP: GET (to fetch information) and POST (to everything else) in CA api is it used with POST HTTP method. */
			requestBody?: {
				content: {
					"application/json": components["schemas"]["05_empty_request"];
				};
			};
			responses: {
				/** @description `OK` */
				200: {
					headers: Record<string, unknown>;
					content: {
						"application/json": components["schemas"]["05_can_be_compiled"];
					};
				};
				/** @description `Unauthorized` */
				401: {
					headers: Record<string, unknown>;
					content: {
						"application/json": components["schemas"]["20_unauthorized"];
					};
				};
				/** @description `Forbidden` */
				403: {
					headers: Record<string, unknown>;
					content: {
						"application/json": components["schemas"]["21_forbidden"];
					};
				};
				/** @description `Not Found` */
				404: {
					headers: Record<string, unknown>;
					content: {
						"application/json": components["schemas"]["45_not_found_RPC"];
					};
				};
			};
		};
		delete?: never;
		options?: never;
		head?: never;
		patch?: never;
		trace?: never;
	};
	"/ca/api/corpora/{corpusId}/get_progress": {
		parameters: {
			query?: never;
			header?: never;
			path: {
				/** @description Numeric corpus ID. For corpora querying. */
				corpusId: components["parameters"]["01_corpus_id"];
			};
			cookie?: never;
		};
		get?: never;
		put?: never;
		/** Retrieves the current progress of the corpus compilation. (RPC) */
		post: {
			parameters: {
				query?: never;
				header?: never;
				path: {
					/** @description Numeric corpus ID. For corpora querying. */
					corpusId: components["parameters"]["01_corpus_id"];
				};
				cookie?: never;
			};
			/** @description In this documentation, an empty request is used mostly used with the **RPC style** method where the content in a request is not needed (in most cases). RPC style endpoints focus on performing **one action** right (procedures, command) easier than **REST API**-based endpoints. It is not as scalable as REST API style. RPC is mostly used with HTTP: GET (to fetch information) and POST (to everything else) in CA api is it used with POST HTTP method. */
			requestBody: {
				content: {
					"application/json": components["schemas"]["05_empty_request"];
				};
			};
			responses: {
				/** @description `OK` */
				200: {
					headers: Record<string, unknown>;
					content: {
						"application/json": components["schemas"]["06_get_progress"];
					};
				};
				/** @description `Forbidden` */
				403: {
					headers: Record<string, unknown>;
					content: {
						"application/json": components["schemas"]["21_forbidden"];
					};
				};
				/** @description `Not Found` */
				404: {
					headers: Record<string, unknown>;
					content: {
						"application/json": components["schemas"]["45_not_found_RPC"];
					};
				};
			};
		};
		delete?: never;
		options?: never;
		head?: never;
		patch?: never;
		trace?: never;
	};
	"/ca/api/corpora/{corpusId}/compile": {
		parameters: {
			query?: never;
			header?: never;
			path: {
				/** @description Numeric corpus ID. For corpora querying. */
				corpusId: components["parameters"]["01_corpus_id"];
			};
			cookie?: never;
		};
		get?: never;
		put?: never;
		/** Performs the corpus compilation. (RPC) */
		post: {
			parameters: {
				query?: never;
				header?: never;
				path: {
					/** @description Numeric corpus ID. For corpora querying. */
					corpusId: components["parameters"]["01_corpus_id"];
				};
				cookie?: never;
			};
			/** @description `Structures` or `structure attributes` in corpus which should be compiled. Usually: `all`. (string) */
			requestBody: {
				content: {
					"application/json": components["schemas"]["02_compile_request"];
				};
			};
			responses: {
				/** @description `OK` */
				200: {
					headers: Record<string, unknown>;
					content: {
						"application/json": components["schemas"]["69_rpc_style"];
					};
				};
				/** @description `Bad Request` */
				400: {
					headers: Record<string, unknown>;
					content: {
						"application/json": components["schemas"]["29_bad_request_RPC_9"];
					};
				};
				/** @description `Unauthorized` */
				401: {
					headers: Record<string, unknown>;
					content: {
						"application/json": components["schemas"]["23_unauthorized_rpc"];
					};
				};
				/** @description `Forbidden` */
				403: {
					headers: Record<string, unknown>;
					content: {
						"application/json": components["schemas"]["21_forbidden"];
					};
				};
				/** @description `Not Found` */
				404: {
					headers: Record<string, unknown>;
					content: {
						"application/json": components["schemas"]["45_not_found_RPC"];
					};
				};
			};
		};
		delete?: never;
		options?: never;
		head?: never;
		patch?: never;
		trace?: never;
	};
	"/ca/api/corpora/{corpusId}/logs/{logName}": {
		parameters: {
			query?: never;
			header?: never;
			path: {
				/** @description Numeric corpus ID. For corpora querying. */
				corpusId: components["parameters"]["01_corpus_id"];
				/**
				 * @description Name of log file. Name 'last.log' show the newest log for that corpus.
				 * @example last.log
				 */
				logName: components["parameters"]["05_logname"];
			};
			cookie?: never;
		};
		/**
		 * Show the compilation log file '.log' of the corpus.
		 * @description If **logName** == **last.log** it shows the latest version of the log file.
		 */
		get: {
			parameters: {
				query?: never;
				header?: never;
				path: {
					/** @description Numeric corpus ID. For corpora querying. */
					corpusId: components["parameters"]["01_corpus_id"];
					/**
					 * @description Name of log file. Name 'last.log' show the newest log for that corpus.
					 * @example last.log
					 */
					logName: components["parameters"]["05_logname"];
				};
				cookie?: never;
			};
			requestBody?: never;
			responses: {
				/** @description `OK` */
				200: {
					headers: Record<string, unknown>;
					content: {
						"text/plain; charset=utf-8": string;
					};
				};
				/** @description `Unauthorized` */
				401: {
					headers: Record<string, unknown>;
					content?: never;
				};
				/** @description `Forbidden` (you need `read` permission) */
				403: {
					headers: Record<string, unknown>;
					content?: never;
				};
				/** @description `Not Found` */
				404: {
					headers: Record<string, unknown>;
					content?: never;
				};
				/** @description `Method Not Allowed` */
				405: {
					headers: Record<string, unknown>;
					content?: never;
				};
			};
		};
		put?: never;
		post?: never;
		delete?: never;
		options?: never;
		head?: never;
		patch?: never;
		trace?: never;
	};
	"/ca/api/corpora/{corpusId}/download": {
		parameters: {
			query: {
				/**
				 * @description File format in which the corpus should be downloaded. Just three formats are supported.
				 * @example txt, vert, tmx
				 */
				format: components["parameters"]["09_format"];
				/** @description The contents of each file will be enclosed in a XML like structure of the specified name with the filename as its id attribute and the URL (if available) as the url attribute. If empty document boundaries will be lost. Example: `doc`. */
				file_structure?: components["parameters"]["10_file_structure"];
				/** @description Required when you want to download parallel corpora, **when format == tmx.** Specify aligned corpus name. */
				aligned?: components["parameters"]["11_aligned"];
			};
			header?: never;
			path: {
				/** @description Numeric corpus ID. For corpora querying. */
				corpusId: components["parameters"]["01_corpus_id"];
			};
			cookie?: never;
		};
		/**
		 * Downloads the documents from which the corpus was created (the source files).
		 * @description Example call can be: **https://app.sketchengine.eu/ca/api/corpora/{corpusId}/download?format=vert&file_structure=doc**.
		 */
		get: {
			parameters: {
				query: {
					/**
					 * @description File format in which the corpus should be downloaded. Just three formats are supported.
					 * @example txt, vert, tmx
					 */
					format: components["parameters"]["09_format"];
					/** @description The contents of each file will be enclosed in a XML like structure of the specified name with the filename as its id attribute and the URL (if available) as the url attribute. If empty document boundaries will be lost. Example: `doc`. */
					file_structure?: components["parameters"]["10_file_structure"];
					/** @description Required when you want to download parallel corpora, **when format == tmx.** Specify aligned corpus name. */
					aligned?: components["parameters"]["11_aligned"];
				};
				header?: never;
				path: {
					/** @description Numeric corpus ID. For corpora querying. */
					corpusId: components["parameters"]["01_corpus_id"];
				};
				cookie?: never;
			};
			requestBody?: never;
			responses: {
				/** @description `OK` */
				200: {
					headers: Record<string, unknown>;
					content: {
						"text/plain": string;
					};
				};
				/** @description `Bad Request` Examples: `ALIGNED_NOT_FOUND`, `ALIGNED_FORBIDDEN`, `INVALID_FORMAT`. */
				400: {
					headers: Record<string, unknown>;
					content?: never;
				};
				/** @description `Unauthorized` */
				401: {
					headers: Record<string, unknown>;
					content?: never;
				};
				/** @description `Forbidden` */
				403: {
					headers: Record<string, unknown>;
					content?: never;
				};
				/** @description `Not Found` */
				404: {
					headers: Record<string, unknown>;
					content?: never;
				};
			};
		};
		put?: never;
		post?: never;
		delete?: never;
		options?: never;
		head?: never;
		patch?: never;
		trace?: never;
	};
	"/ca/api/corpora/{corpusId}/cancel_job": {
		parameters: {
			query?: never;
			header?: never;
			path: {
				/** @description Numeric corpus ID. For corpora querying. */
				corpusId: components["parameters"]["01_corpus_id"];
			};
			cookie?: never;
		};
		get?: never;
		put?: never;
		/** Cancels running tasks (e.g. compilation) relatated to the corpus. (RPC) */
		post: {
			parameters: {
				query?: never;
				header?: never;
				path: {
					/** @description Numeric corpus ID. For corpora querying. */
					corpusId: components["parameters"]["01_corpus_id"];
				};
				cookie?: never;
			};
			/** @description In this documentation, an empty request is used mostly used with the **RPC style** method where the content in a request is not needed (in most cases). RPC style endpoints focus on performing **one action** right (procedures, command) easier than **REST API**-based endpoints. It is not as scalable as REST API style. RPC is mostly used with HTTP: GET (to fetch information) and POST (to everything else) in CA api is it used with POST HTTP method. */
			requestBody?: {
				content: {
					"application/json": components["schemas"]["05_empty_request"];
				};
			};
			responses: {
				/** @description `OK` */
				200: {
					headers: Record<string, unknown>;
					content: {
						"application/json": components["schemas"]["69_rpc_style"];
					};
				};
				/** @description `Unauthorized` */
				401: {
					headers: Record<string, unknown>;
					content: {
						"application/json": components["schemas"]["23_unauthorized_rpc"];
					};
				};
				/** @description `Forbidden` */
				403: {
					headers: Record<string, unknown>;
					content: {
						"application/json": components["schemas"]["21_forbidden"];
					};
				};
			};
		};
		delete?: never;
		options?: never;
		head?: never;
		patch?: never;
		trace?: never;
	};
	"/ca/api/corpora/compile_aligned": {
		parameters: {
			query?: never;
			header?: never;
			path?: never;
			cookie?: never;
		};
		get?: never;
		put?: never;
		/** Compiles a parallel corpus (consisting of two or more aligned corpora). (RPC) */
		post: {
			parameters: {
				query?: never;
				header?: never;
				path?: never;
				cookie?: never;
			};
			/**
			 * @description List of corpus IDs used in aligned compilation.
			 *
			 *       - `corpus_ids` => A list of **Corpus ID** of multilingual corpora. (integer)
			 *
			 *       - `structures` => Represent if **all** structures should be used during compilation (in that case it should be contain just **all**) or just some of them. (string)
			 */
			requestBody?: {
				content: {
					"application/json": components["schemas"]["03_corpus_ids"];
				};
			};
			responses: {
				/** @description `OK` */
				200: {
					headers: Record<string, unknown>;
					content: {
						"application/json": components["schemas"]["69_rpc_style"];
					};
				};
				/** @description `Bad Request` */
				400: {
					headers: Record<string, unknown>;
					content: {
						"application/json": components["schemas"]["28_bad_request_RPC_8"];
					};
				};
				/** @description `Forbidden` */
				403: {
					headers: Record<string, unknown>;
					content: {
						"application/json": components["schemas"]["21_forbidden"];
					};
				};
			};
		};
		delete?: never;
		options?: never;
		head?: never;
		patch?: never;
		trace?: never;
	};
	"/ca/api/corpora/align": {
		parameters: {
			query?: never;
			header?: never;
			path?: never;
			cookie?: never;
		};
		get?: never;
		put?: never;
		/**
		 * Aligns two documents in different languages for parallel corpus purposes. (RPC)
		 * @description The documents usually contain the same context but in different languages.
		 *
		 *      RPC style endpoints focus on performing one action right (procedures, command). RPC is mostly used with HTTP GET (to fetch information) and POST (to everything else).
		 */
		post: {
			parameters: {
				query?: never;
				header?: never;
				path?: never;
				cookie?: never;
			};
			/**
			 * @description - `alignstruct` => According to which structure the document should be aligned. Usually, **\<s>**. (string)
			 *
			 *       - `auto` => **True**, when documents are not compiled. Sketch Engine will align them automatically. (boolean)
			 *
			 *       - `corpus_ids` => A list of **Corpus ID** of multilingual corpus. (integer)
			 */
			requestBody?: {
				content: {
					"application/json": components["schemas"]["04_align_req"];
				};
			};
			responses: {
				/** @description `OK` */
				200: {
					headers: Record<string, unknown>;
					content: {
						"application/json": components["schemas"]["69_rpc_style"];
					};
				};
				/** @description `Bad Request` */
				400: {
					headers: Record<string, unknown>;
					content: {
						"application/json": components["schemas"]["17_bad_request_RPC_1"];
					};
				};
				/** @description `Unauthorized` */
				401: {
					headers: Record<string, unknown>;
					content: {
						"application/json": components["schemas"]["23_unauthorized_rpc"];
					};
				};
				/** @description `Forbidden` */
				403: {
					headers: Record<string, unknown>;
					content: {
						"application/json": components["schemas"]["21_forbidden"];
					};
				};
			};
		};
		delete?: never;
		options?: never;
		head?: never;
		patch?: never;
		trace?: never;
	};
	"/ca/api/corpora/{corpusId}/documents": {
		parameters: {
			query?: {
				/** @description ID of file subdirectory. **0** stands for default document directory with name `upload`. */
				fileset_id?: components["parameters"]["07_fileset_id_query"];
			};
			header?: never;
			path: {
				/** @description Numeric corpus ID. For corpora querying. */
				corpusId: components["parameters"]["01_corpus_id"];
			};
			cookie?: never;
		};
		/** Retrieves a list of all documents in the corpus. */
		get: {
			parameters: {
				query?: {
					/** @description ID of file subdirectory. **0** stands for default document directory with name `upload`. */
					fileset_id?: components["parameters"]["07_fileset_id_query"];
				};
				header?: never;
				path: {
					/** @description Numeric corpus ID. For corpora querying. */
					corpusId: components["parameters"]["01_corpus_id"];
				};
				cookie?: never;
			};
			requestBody?: never;
			responses: {
				/** @description `OK` */
				200: {
					headers: Record<string, unknown>;
					content: {
						"application/json": components["schemas"]["13_documents_get"];
					};
				};
				/** @description `Unauthorized` */
				401: {
					headers: Record<string, unknown>;
					content: {
						"application/json": components["schemas"]["20_unauthorized"];
					};
				};
				/** @description `Forbidden` (you need `view` permission). */
				403: {
					headers: Record<string, unknown>;
					content: {
						"application/json": components["schemas"]["27_forbidden_normal"];
					};
				};
				/** @description `Not Found` (HTML response). */
				404: {
					headers: Record<string, unknown>;
					content?: never;
				};
			};
		};
		/** Edits the metadata of a document. */
		put: {
			parameters: {
				query?: {
					/** @description ID of file subdirectory. **0** stands for default document directory with name `upload`. */
					fileset_id?: components["parameters"]["07_fileset_id_query"];
				};
				header?: never;
				path: {
					/** @description Numeric corpus ID. For corpora querying. */
					corpusId: components["parameters"]["01_corpus_id"];
				};
				cookie?: never;
			};
			/**
			 * @description - `id` => Unique numeric `document ID`. (integer)
			 *
			 *       - `metadata` => Pairs of `attribute_name`:`value`.
			 */
			requestBody?: {
				content: {
					"application/json": components["schemas"]["11_doc_metadata"];
				};
			};
			responses: {
				/** @description `OK` */
				200: {
					headers: Record<string, unknown>;
					content: {
						"application/json": components["schemas"]["13_documents_get"];
					};
				};
				/** @description `Bad Request` */
				400: {
					headers: Record<string, unknown>;
					content: {
						"application/json": components["schemas"]["31_bad_request_11"];
					};
				};
				/** @description `Unauthorized` */
				401: {
					headers: Record<string, unknown>;
					content: {
						"application/json": components["schemas"]["20_unauthorized"];
					};
				};
				/** @description `Forbidden` (you need `edit` permission). */
				403: {
					headers: Record<string, unknown>;
					content: {
						"application/json": components["schemas"]["27_forbidden_normal"];
					};
				};
				/** @description `Not Found` (HTML response). */
				404: {
					headers: Record<string, unknown>;
					content?: never;
				};
			};
		};
		/** Uploads a new document. */
		post: {
			parameters: {
				query?: {
					/** @description ID of file subdirectory. **0** stands for default document directory with name `upload`. */
					fileset_id?: components["parameters"]["07_fileset_id_query"];
					/** @description Delay tagging by given number of `seconds`. */
					wait_with_tagging?: components["parameters"]["18_wait_with_tagging"];
				};
				header?: never;
				path: {
					/** @description Numeric corpus ID. For corpora querying. */
					corpusId: components["parameters"]["01_corpus_id"];
				};
				cookie?: never;
			};
			/** @description File to upload. */
			requestBody?: {
				content: {
					"multipart/form-data": {
						/**
						 * Format: binary
						 * @description File to upload.
						 */
						file?: string;
					};
				};
			};
			responses: {
				/** @description `Created` */
				201: {
					headers: Record<string, unknown>;
					content: {
						"application/json": components["schemas"]["14_documents_post"];
					};
				};
				/** @description `Bad Request` */
				400: {
					headers: Record<string, unknown>;
					content: {
						"application/json": components["schemas"]["30_bad_request_10"];
					};
				};
				/** @description `Unauthorized` */
				401: {
					headers: Record<string, unknown>;
					content: {
						"application/json": components["schemas"]["20_unauthorized"];
					};
				};
				/** @description `Forbidden` (you need `upload` permission). */
				403: {
					headers: Record<string, unknown>;
					content: {
						"application/json": components["schemas"]["27_forbidden_normal"];
					};
				};
				/** @description `Not Found` (HTML response). */
				404: {
					headers: Record<string, unknown>;
					content?: never;
				};
			};
		};
		delete?: never;
		options?: never;
		head?: never;
		patch?: never;
		trace?: never;
	};
	"/ca/api/corpora/{corpusId}/documents/{documentId}": {
		parameters: {
			query?: never;
			header?: never;
			path: {
				/** @description Numeric corpus ID. For corpora querying. */
				corpusId: components["parameters"]["01_corpus_id"];
				/** @description Document ID. For document querying. */
				documentId: components["parameters"]["02_document_id"];
			};
			cookie?: never;
		};
		/** Retrieves a specific document. */
		get: {
			parameters: {
				query?: never;
				header?: never;
				path: {
					/** @description Numeric corpus ID. For corpora querying. */
					corpusId: components["parameters"]["01_corpus_id"];
					/** @description Document ID. For document querying. */
					documentId: components["parameters"]["02_document_id"];
				};
				cookie?: never;
			};
			requestBody?: never;
			responses: {
				/** @description `OK` */
				200: {
					headers: Record<string, unknown>;
					content: {
						"application/json": components["schemas"]["13_documents_get"];
					};
				};
				/** @description `Unauthorized` */
				401: {
					headers: Record<string, unknown>;
					content: {
						"application/json": components["schemas"]["20_unauthorized"];
					};
				};
				/** @description `Forbidden` (you need `view` permission). */
				403: {
					headers: Record<string, unknown>;
					content: {
						"application/json": components["schemas"]["27_forbidden_normal"];
					};
				};
				/** @description `Not Found` */
				404: {
					headers: Record<string, unknown>;
					content: {
						"application/json": components["schemas"]["57_not_found_404"];
					};
				};
			};
		};
		/** Updates documents to corpus. */
		put: {
			parameters: {
				query?: never;
				header?: never;
				path: {
					/** @description Numeric corpus ID. For corpora querying. */
					corpusId: components["parameters"]["01_corpus_id"];
					/** @description Document ID. For document querying. */
					documentId: components["parameters"]["02_document_id"];
				};
				cookie?: never;
			};
			/**
			 * @description - `filename_display` => Name of documents. (string)
			 *
			 *       - `id` => Unique numeric **document ID** to identify individual documents. (integer)
			 *
			 *       - `inProgress` => Represents whether the currently edited document is in use. (boolean)
			 *
			 *       - `isArchive` => Represents if the updated document is in a format like .zip (created via some archive manager). (boolean)
			 *
			 *       - `metadata` => Metadata of document. For example, additional attributes and values.
			 *
			 *       - `parameters` => Parameters for plaintext extraction.
			 *
			 *         - `encoding` => Encoding standard of the document. Usually, **UTF-8**. (string)
			 *
			 *         - `justext_stoplist` => Represent the list of unimportant words, in a specified language, from an NLP point of view. (string)
			 *
			 *         - `permutation` => Changing the order of columns (applies only to **type=vert**). (integer)
			 *
			 *         - `tmx_lang` => TMX (translation memory exchange). Language of document used for parallel corpus creation. (string)
			 *
			 *         - `tmx_struct` => Alignment structure to be used for multilingual documents, **align** is the most used structure. Used within segment distinction, which sentence is in which language and to put sentences with the same meaning into one segment. (string)
			 *
			 *         - `tmx_untranslated` => Placeholder for empty segments in multilingual documents. The segments which have no counterpart in a second language of parallel corpus. (string)
			 *
			 *         - `type` => File format (.csv, .doc, .docx, .htm, .html). (string)
			 *
			 *         - `unlegalese` => Convert **all-caps** text to **normal case**. (boolean)
			 *
			 *       - `temporary` => Is document temporary or not. (boolean)
			 *
			 *       - `word_count` => Total number of **words** (tokens minus punctuation etc.) in document. (integer)
			 *
			 *       - `vertical_progress` => Progress of **vertical file** creation. (integer)
			 *
			 *       - `vertical_error` => An error occured while creating the vertical file. If the creation was succesfull the value is **Null**. (string)
			 */
			requestBody?: {
				content: {
					"application/json": components["schemas"]["09_doc_put_req"];
				};
			};
			responses: {
				/** @description `OK` */
				200: {
					headers: Record<string, unknown>;
					content: {
						"application/json": components["schemas"]["13_documents_get"];
					};
				};
				/** @description `Bad Request` */
				400: {
					headers: Record<string, unknown>;
					content: {
						"application/json": components["schemas"]["31_bad_request_11"];
					};
				};
				/** @description `Unauthorized` */
				401: {
					headers: Record<string, unknown>;
					content: {
						"application/json": components["schemas"]["20_unauthorized"];
					};
				};
				/** @description `Forbidden` (you need `edit` permission). */
				403: {
					headers: Record<string, unknown>;
					content: {
						"application/json": components["schemas"]["27_forbidden_normal"];
					};
				};
				/** @description `Not Found` */
				404: {
					headers: Record<string, unknown>;
					content: {
						"application/json": components["schemas"]["57_not_found_404"];
					};
				};
			};
		};
		post?: never;
		/**
		 * Deletes one or more documents from the corpus.
		 * @description To delete more documents, separate document ids with commas. Example call: `https://app.sketchengine.eu/ca/api/corpora/{corpusId}/documents/{documentId_1},{documentId_2}`
		 */
		delete: {
			parameters: {
				query?: never;
				header?: never;
				path: {
					/** @description Numeric corpus ID. For corpora querying. */
					corpusId: components["parameters"]["01_corpus_id"];
					/** @description Document ID. For document querying. */
					documentId: components["parameters"]["02_document_id"];
				};
				cookie?: never;
			};
			requestBody?: never;
			responses: {
				/** @description `No Content` */
				204: {
					headers: Record<string, unknown>;
					content?: never;
				};
				/** @description `Bad Request` */
				400: {
					headers: Record<string, unknown>;
					content: {
						"application/json": components["schemas"]["31_bad_request_11"];
					};
				};
				/** @description `Unauthorized` */
				401: {
					headers: Record<string, unknown>;
					content: {
						"application/json": components["schemas"]["20_unauthorized"];
					};
				};
				/** @description `Forbidden` (you need `delete` permission). */
				403: {
					headers: Record<string, unknown>;
					content: {
						"application/json": components["schemas"]["27_forbidden_normal"];
					};
				};
				/** @description `Not Found` */
				404: {
					headers: Record<string, unknown>;
					content: {
						"application/json": components["schemas"]["57_not_found_404"];
					};
				};
			};
		};
		options?: never;
		head?: never;
		patch?: never;
		trace?: never;
	};
	"/ca/api/corpora/{corpusId}/documents/{documentId}/preview": {
		parameters: {
			query?: never;
			header?: never;
			path: {
				/** @description Numeric corpus ID. For corpora querying. */
				corpusId: components["parameters"]["01_corpus_id"];
				/** @description Document ID. For document querying. */
				documentId: components["parameters"]["02_document_id"];
			};
			cookie?: never;
		};
		get?: never;
		put?: never;
		/**
		 * Updates document parameters. (RPC)
		 * @description Updates parameters like: `File Type`, `Encoding`, etc.
		 */
		post: {
			parameters: {
				query?: never;
				header?: never;
				path: {
					/** @description Numeric corpus ID. For corpora querying. */
					corpusId: components["parameters"]["01_corpus_id"];
					/** @description Document ID. For document querying. */
					documentId: components["parameters"]["02_document_id"];
				};
				cookie?: never;
			};
			/**
			 * @description - `auto_paragraphs` => Automatically insert paragraph breaks (**\<p>**) in place of blank lines. (string)
			 *
			 *       - `encoding` => Encoding standard of the document. Usually **UTF-8**. (string)
			 *
			 *       - `justext_stoplist` => Represent the list of unimportant words, in a specified language, from an NLP point of view. (string)
			 *
			 *       - `permutation` => Changing the order of columns (applies only to **type=vert**).
			 *
			 *       - `tmx_lang` => TMX (translation memory exchange). Language of document used for parallel corpus creation. (string)
			 *
			 *       - `tmx_struct` => Alignment structure to be used for multilingual documents, **align** is the most used structure. Used within segment distinction, which sentence is in which language and to put sentences with the same meaning into one segment. (string)
			 *
			 *       - `tmx_untranslated` => Placeholder for empty segments in multilingual documents. The segments which have no counterpart in a second language of parallel corpus. (string)
			 *
			 *       - `type` => File format (.csv, .doc, .docx, .htm, .html). (string)
			 *
			 *       - `unlegalese` => Convert **all-caps** text to **normal case**. (boolean)
			 */
			requestBody?: {
				content: {
					"application/json": components["schemas"]["10_doc_preview"];
				};
			};
			responses: {
				/** @description `OK` */
				200: {
					headers: Record<string, unknown>;
					content: {
						"application/json": components["schemas"]["15_doc_preview"];
					};
				};
				/** @description `Forbidden` */
				403: {
					headers: Record<string, unknown>;
					content: {
						"application/json": components["schemas"]["21_forbidden"];
					};
				};
			};
		};
		delete?: never;
		options?: never;
		head?: never;
		patch?: never;
		trace?: never;
	};
	"/ca/api/corpora/{corpusId}/documents/{documentId}/original": {
		parameters: {
			query?: never;
			header?: never;
			path: {
				/** @description Numeric corpus ID. For corpora querying. */
				corpusId: components["parameters"]["01_corpus_id"];
				/** @description Document ID. For document querying. */
				documentId: components["parameters"]["02_document_id"];
			};
			cookie?: never;
		};
		/** Downloads a corpus file in its original format (the format that was uploaded). This method cannot be simulated in this online documentation. */
		get: {
			parameters: {
				query?: never;
				header?: never;
				path: {
					/** @description Numeric corpus ID. For corpora querying. */
					corpusId: components["parameters"]["01_corpus_id"];
					/** @description Document ID. For document querying. */
					documentId: components["parameters"]["02_document_id"];
				};
				cookie?: never;
			};
			requestBody?: never;
			responses: {
				/** @description `OK` */
				200: {
					headers: Record<string, unknown>;
					content: {
						"application/octet-stream": string;
					};
				};
				/** @description `Unauthorized` */
				401: {
					headers: Record<string, unknown>;
					content?: never;
				};
				/** @description `Forbidden` (you need `view` permission). */
				403: {
					headers: Record<string, unknown>;
					content?: never;
				};
				/** @description `Not Found` (Html response). */
				404: {
					headers: Record<string, unknown>;
					content?: never;
				};
				/** @description `Method Not Allowed` */
				405: {
					headers: Record<string, unknown>;
					content?: never;
				};
			};
		};
		put?: never;
		post?: never;
		delete?: never;
		options?: never;
		head?: never;
		patch?: never;
		trace?: never;
	};
	"/ca/api/corpora/{corpusId}/documents/{documentId}/plaintext": {
		parameters: {
			query?: never;
			header?: never;
			path: {
				/** @description Numeric corpus ID. For corpora querying. */
				corpusId: components["parameters"]["01_corpus_id"];
				/** @description Document ID. For document querying. */
				documentId: components["parameters"]["02_document_id"];
			};
			cookie?: never;
		};
		/** Retrieves 1KB of data in plaintext format. Can load more than 1KB. */
		get: {
			parameters: {
				query?: never;
				header?: never;
				path: {
					/** @description Numeric corpus ID. For corpora querying. */
					corpusId: components["parameters"]["01_corpus_id"];
					/** @description Document ID. For document querying. */
					documentId: components["parameters"]["02_document_id"];
				};
				cookie?: never;
			};
			requestBody?: never;
			responses: {
				/** @description `Partial Content` */
				206: {
					headers: Record<string, unknown>;
					content: {
						"text/plain; charset=utf-8": string;
					};
				};
				/** @description `Unauthorized` */
				401: {
					headers: Record<string, unknown>;
					content?: never;
				};
				/** @description `Forbidden` (you need `view` permission). */
				403: {
					headers: Record<string, unknown>;
					content?: never;
				};
				/** @description `Not Found` (Html response). */
				404: {
					headers: Record<string, unknown>;
					content?: never;
				};
				/** @description `Method Not Allowed` */
				405: {
					headers: Record<string, unknown>;
					content?: never;
				};
				/** @description `Range Not Satisfiable` */
				416: {
					headers: Record<string, unknown>;
					content?: never;
				};
			};
		};
		put?: never;
		post?: never;
		delete?: never;
		options?: never;
		head?: never;
		patch?: never;
		trace?: never;
	};
	"/ca/api/corpora/{corpusId}/documents/{documentId}/vertical": {
		parameters: {
			query?: never;
			header?: never;
			path: {
				/** @description Numeric corpus ID. For corpora querying. */
				corpusId: components["parameters"]["01_corpus_id"];
				/** @description Document ID. For document querying. */
				documentId: components["parameters"]["02_document_id"];
			};
			cookie?: never;
		};
		/** Retrieves 1KB of data in vertical format. Can load more than 1KB. */
		get: {
			parameters: {
				query?: never;
				header?: never;
				path: {
					/** @description Numeric corpus ID. For corpora querying. */
					corpusId: components["parameters"]["01_corpus_id"];
					/** @description Document ID. For document querying. */
					documentId: components["parameters"]["02_document_id"];
				};
				cookie?: never;
			};
			requestBody?: never;
			responses: {
				/** @description `Partial Content` */
				206: {
					headers: Record<string, unknown>;
					content: {
						"text/plain; charset=utf-8": string;
					};
				};
				/** @description `Unauthorized` */
				401: {
					headers: Record<string, unknown>;
					content?: never;
				};
				/** @description `Forbidden` */
				403: {
					headers: Record<string, unknown>;
					content?: never;
				};
				/** @description `Not Found` (Html response). */
				404: {
					headers: Record<string, unknown>;
					content?: never;
				};
				/** @description `Method Not Allowed` */
				405: {
					headers: Record<string, unknown>;
					content?: never;
				};
				/** @description `Range Not Satisfiable` */
				416: {
					headers: Record<string, unknown>;
					content?: never;
				};
			};
		};
		put?: never;
		post?: never;
		delete?: never;
		options?: never;
		head?: never;
		patch?: never;
		trace?: never;
	};
	"/ca/api/corpora/{corpusId}/documents/{documentId}/expand_archive": {
		parameters: {
			query?: never;
			header?: never;
			path: {
				/** @description Numeric corpus ID. For corpora querying. */
				corpusId: components["parameters"]["01_corpus_id"];
				/** @description Document ID. For document querying. */
				documentId: components["parameters"]["02_document_id"];
			};
			cookie?: never;
		};
		get?: never;
		put?: never;
		/** Expands a ZIP file. (if the corpus files were uploaded as a zip archive). Expanding is not necessary for the corpus to work.(RPC) */
		post: {
			parameters: {
				query?: never;
				header?: never;
				path: {
					/** @description Numeric corpus ID. For corpora querying. */
					corpusId: components["parameters"]["01_corpus_id"];
					/** @description Document ID. For document querying. */
					documentId: components["parameters"]["02_document_id"];
				};
				cookie?: never;
			};
			/** @description In this documentation, an empty request is used mostly used with the **RPC style** method where the content in a request is not needed (in most cases). RPC style endpoints focus on performing **one action** right (procedures, command) easier than **REST API**-based endpoints. It is not as scalable as REST API style. RPC is mostly used with HTTP: GET (to fetch information) and POST (to everything else) in CA api is it used with POST HTTP method. */
			requestBody?: {
				content: {
					"application/json": components["schemas"]["05_empty_request"];
				};
			};
			responses: {
				/** @description `OK` */
				200: {
					headers: Record<string, unknown>;
					content: {
						"application/json": components["schemas"]["16_rpc_expand_archive"];
					};
				};
				/** @description `Bad Request` */
				400: {
					headers: Record<string, unknown>;
					content: {
						"application/json": components["schemas"]["33_bad_request_13"];
					};
				};
				/** @description `Forbidden (you need edit permission)` */
				403: {
					headers: Record<string, unknown>;
					content: {
						"application/json": components["schemas"]["21_forbidden"];
					};
				};
				/** @description `Not Found` (Html response). */
				404: {
					headers: Record<string, unknown>;
					content?: never;
				};
			};
		};
		delete?: never;
		options?: never;
		head?: never;
		patch?: never;
		trace?: never;
	};
	"/ca/api/corpora/{corpusId}/documents/{documentId}/cancel_job": {
		parameters: {
			query?: never;
			header?: never;
			path: {
				/** @description Numeric corpus ID. For corpora querying. */
				corpusId: components["parameters"]["01_corpus_id"];
				/** @description Document ID. For document querying. */
				documentId: components["parameters"]["02_document_id"];
			};
			cookie?: never;
		};
		get?: never;
		put?: never;
		/**
		 * Cancels running task which is in direct relation to the document. (RPC)
		 * @description An example task that can be canceled is `uploading file`.
		 */
		post: {
			parameters: {
				query?: never;
				header?: never;
				path: {
					/** @description Numeric corpus ID. For corpora querying. */
					corpusId: components["parameters"]["01_corpus_id"];
					/** @description Document ID. For document querying. */
					documentId: components["parameters"]["02_document_id"];
				};
				cookie?: never;
			};
			/** @description In this documentation, an empty request is used mostly used with the **RPC style** method where the content in a request is not needed (in most cases). RPC style endpoints focus on performing **one action** right (procedures, command) easier than **REST API**-based endpoints. It is not as scalable as REST API style. RPC is mostly used with HTTP: GET (to fetch information) and POST (to everything else) in CA api is it used with POST HTTP method. */
			requestBody?: {
				content: {
					"application/json": components["schemas"]["05_empty_request"];
				};
			};
			responses: {
				/** @description `OK` */
				200: {
					headers: Record<string, unknown>;
					content: {
						"application/json": components["schemas"]["69_rpc_style"];
					};
				};
				/** @description `Forbidden` (you need `edit` permission). */
				403: {
					headers: Record<string, unknown>;
					content: {
						"application/json": components["schemas"]["21_forbidden"];
					};
				};
				/** @description `Not Found` */
				404: {
					headers: Record<string, unknown>;
					content: {
						"application/json": components["schemas"]["45_not_found_RPC"];
					};
				};
			};
		};
		delete?: never;
		options?: never;
		head?: never;
		patch?: never;
		trace?: never;
	};
	"/ca/api/corpora/{corpusId}/documents/{documentId}/get_progress": {
		parameters: {
			query?: never;
			header?: never;
			path: {
				/** @description Numeric corpus ID. For corpora querying. */
				corpusId: components["parameters"]["01_corpus_id"];
				/** @description Document ID. For document querying. */
				documentId: components["parameters"]["02_document_id"];
			};
			cookie?: never;
		};
		get?: never;
		put?: never;
		/**
		 * Shows the actual progress of the currently running task related to documents. (RPC)
		 * @description Used in tasks like `uploading` new files which will be used to create or expand the corpora.
		 */
		post: {
			parameters: {
				query?: never;
				header?: never;
				path: {
					/** @description Numeric corpus ID. For corpora querying. */
					corpusId: components["parameters"]["01_corpus_id"];
					/** @description Document ID. For document querying. */
					documentId: components["parameters"]["02_document_id"];
				};
				cookie?: never;
			};
			/** @description In this documentation, an empty request is used mostly used with the **RPC style** method where the content in a request is not needed (in most cases). RPC style endpoints focus on performing **one action** right (procedures, command) easier than **REST API**-based endpoints. It is not as scalable as REST API style. RPC is mostly used with HTTP: GET (to fetch information) and POST (to everything else) in CA api is it used with POST HTTP method. */
			requestBody?: {
				content: {
					"application/json": components["schemas"]["05_empty_request"];
				};
			};
			responses: {
				/** @description `OK` */
				200: {
					headers: Record<string, unknown>;
					content: {
						"application/json": components["schemas"]["06_get_progress"];
					};
				};
				/** @description `Forbidden` (you need permission to `view` to the specified corpus). */
				403: {
					headers: Record<string, unknown>;
					content: {
						"application/json": components["schemas"]["21_forbidden"];
					};
				};
				/** @description `Not Found` */
				404: {
					headers: Record<string, unknown>;
					content: {
						"application/json": components["schemas"]["45_not_found_RPC"];
					};
				};
			};
		};
		delete?: never;
		options?: never;
		head?: never;
		patch?: never;
		trace?: never;
	};
	"/ca/api/corpora/{corpusId}/filesets": {
		parameters: {
			query?: {
				/** @description Start corpus compiling after web-crawler finishes downloading content from the internet. */
				compile_when_finished?: components["parameters"]["19_compile_when_finished"];
			};
			header?: never;
			path: {
				/** @description Numeric corpus ID. For corpora querying. */
				corpusId: components["parameters"]["01_corpus_id"];
			};
			cookie?: never;
		};
		/** List of "subdirectories", i.e. unzipped archives or WebBootCaT runs for a given corpus. */
		get: {
			parameters: {
				query?: {
					/** @description Start corpus compiling after web-crawler finishes downloading content from the internet. */
					compile_when_finished?: components["parameters"]["19_compile_when_finished"];
				};
				header?: never;
				path: {
					/** @description Numeric corpus ID. For corpora querying. */
					corpusId: components["parameters"]["01_corpus_id"];
				};
				cookie?: never;
			};
			requestBody?: never;
			responses: {
				/** @description `OK` */
				200: {
					headers: Record<string, unknown>;
					content: {
						"application/json": {
							data?: Array<components["schemas"]["58_fileset"]>;
						};
					};
				};
				/** @description `Unauthorized` */
				401: {
					headers: Record<string, unknown>;
					content: {
						"application/json": components["schemas"]["20_unauthorized"];
					};
				};
				/** @description `Forbidden` (you need permission to `upload` to the specified corpus). */
				403: {
					headers: Record<string, unknown>;
					content: {
						"application/json": components["schemas"]["27_forbidden_normal"];
					};
				};
				/** @description `Not Found` */
				404: {
					headers: Record<string, unknown>;
					content: {
						"application/json": components["schemas"]["57_not_found_404"];
					};
				};
			};
		};
		put?: never;
		/**
		 * Creates new fileset by the web-crawler.
		 * @description Used within creating or expanding corpus.
		 */
		post: {
			parameters: {
				query?: {
					/** @description Start corpus compiling after web-crawler finishes downloading content from the internet. */
					compile_when_finished?: components["parameters"]["19_compile_when_finished"];
				};
				header?: never;
				path: {
					/** @description Numeric corpus ID. For corpora querying. */
					corpusId: components["parameters"]["01_corpus_id"];
				};
				cookie?: never;
			};
			/**
			 * @description Setting parameters to improve web-crawler accuracy.
			 *
			 *       - `bl_max_total_kw` => **Blacklist max total keyword**. Means that web page or document will be discarded if it contains more words from the denylist (blacklist) than this limit. (integer)
			 *
			 *       - `bl_max_unique_kw` => **Blacklist max unique keyword**. Means that web page or document will be discarded if it contains more unique words from the denylist (blacklist) than this limit. (integer)
			 *
			 *       - `black_list` => A list (separated by whitespaces) of **blocked words**, words you don't want to see in your future corpus. (string)
			 *
			 *       - `input_type` => Input types the web-crawler will works with. Example: **urls**. (string)
			 *
			 *       - `max_cleaned_file_size` => Web pages and documents with a size **over** this limit (**in kB**) will be ignored. (integer)
			 *
			 *       - `max_file_size` => Web pages and documents with a size **over** this limit (**in kB**) will be ignored. (integer)
			 *
			 *       - `min_cleaned_file_size` => Web pages and documents **smaller** than this limit (**in kB**) after cleaning will be ignored. Cleaning involves conversion to plain text, removing boilerplate text (e.g. navigation menus, legal text, disclaimers and other repetitive content). (integer)
			 *
			 *       - `min_file_size` => Web pages and documents with a **size below** this limit (**in kB**) will be ignored. (integer)
			 *
			 *       - `name` => Texts will be organized into a corpus folder with this name. (string)
			 *
			 *       - `seed_word` => A list of words according to which the URLs were chosen to be searched. (string)
			 *
			 *       - `white_list` => A list (separated by whitespaces) of allowed words, words you want to see in your future corpus. (list of string)
			 *
			 *       - `wl_min_kw_ratio` => **Whitelist minimal keywords ratio**. Means that web page or document will be included only if the percentage of allowlist words compared to total words is higher than this limit. (integer)
			 *
			 *       - `wl_min_total_kw` => **Whitelist minimal total keywords**. Means that web page or document will be included only if it contains more words from the allowlist (whitelist) than this limit. (integer)
			 *
			 *       - `wl_min_unique_kw` => **Whitelist minimal unique keywords**. Means that a web page or document will be included only if it contains more words from the allowlist (whitelist) than this limit. (integer)
			 */
			requestBody: {
				content: {
					"application/json": components["schemas"]["13_filesets_creation"];
				};
			};
			responses: {
				/** @description `Created` */
				201: {
					headers: Record<string, unknown>;
					content: {
						"application/json": {
							data?: components["schemas"]["61_fileset_creation"];
						};
					};
				};
				/** @description `Bad Request` */
				400: {
					headers: Record<string, unknown>;
					content: {
						"application/json": components["schemas"]["34_bad_request_14"];
					};
				};
				/** @description `Unauthorized` */
				401: {
					headers: Record<string, unknown>;
					content: {
						"application/json": components["schemas"]["20_unauthorized"];
					};
				};
				/** @description `Forbidden` (you need permission to `upload` to the specified corpus). */
				403: {
					headers: Record<string, unknown>;
					content: {
						"application/json": components["schemas"]["27_forbidden_normal"];
					};
				};
				/** @description `Not Found` */
				404: {
					headers: Record<string, unknown>;
					content: {
						"application/json": components["schemas"]["57_not_found_404"];
					};
				};
			};
		};
		delete?: never;
		options?: never;
		head?: never;
		patch?: never;
		trace?: never;
	};
	"/ca/api/corpora/{corpusId}/filesets/{filesetId}": {
		parameters: {
			query?: never;
			header?: never;
			path: {
				/** @description Numeric corpus ID. For corpora querying. */
				corpusId: components["parameters"]["01_corpus_id"];
				/** @description ID of file subdirectories. If sets to 0 it will return top-level folder of documents, so if you have a web corpora with folders web1 and web2 it will return web1. */
				filesetId: components["parameters"]["06_fileset_id"];
			};
			cookie?: never;
		};
		/** Returns information about a specific "subdirectory". */
		get: {
			parameters: {
				query?: never;
				header?: never;
				path: {
					/** @description Numeric corpus ID. For corpora querying. */
					corpusId: components["parameters"]["01_corpus_id"];
					/** @description ID of file subdirectories. If sets to 0 it will return top-level folder of documents, so if you have a web corpora with folders web1 and web2 it will return web1. */
					filesetId: components["parameters"]["06_fileset_id"];
				};
				cookie?: never;
			};
			requestBody?: never;
			responses: {
				/** @description `OK` */
				200: {
					headers: Record<string, unknown>;
					content: {
						"application/json": {
							data?: components["schemas"]["58_fileset"];
						};
					};
				};
				/** @description `Unauthorized` */
				401: {
					headers: Record<string, unknown>;
					content: {
						"application/json": components["schemas"]["20_unauthorized"];
					};
				};
				/** @description `Forbidden` (you need permission to `view`). */
				403: {
					headers: Record<string, unknown>;
					content: {
						"application/json": components["schemas"]["27_forbidden_normal"];
					};
				};
				/** @description `Not Found` */
				404: {
					headers: Record<string, unknown>;
					content: {
						"application/json": components["schemas"]["57_not_found_404"];
					};
				};
			};
		};
		put?: never;
		post?: never;
		/** Deletes subdirectory containing document (for creating corpus). */
		delete: {
			parameters: {
				query?: never;
				header?: never;
				path: {
					/** @description Numeric corpus ID. For corpora querying. */
					corpusId: components["parameters"]["01_corpus_id"];
					/** @description ID of file subdirectories. If sets to 0 it will return top-level folder of documents, so if you have a web corpora with folders web1 and web2 it will return web1. */
					filesetId: components["parameters"]["06_fileset_id"];
				};
				cookie?: never;
			};
			requestBody?: never;
			responses: {
				/** @description `No Content` */
				204: {
					headers: Record<string, unknown>;
					content?: never;
				};
				/** @description `Bad Request` */
				400: {
					headers: Record<string, unknown>;
					content: {
						"application/json": components["schemas"]["32_bad_request_12"];
					};
				};
				/** @description `Unauthorized` */
				401: {
					headers: Record<string, unknown>;
					content: {
						"application/json": components["schemas"]["20_unauthorized"];
					};
				};
				/** @description `Forbidden` (you need permission to `delete`). */
				403: {
					headers: Record<string, unknown>;
					content: {
						"application/json": components["schemas"]["27_forbidden_normal"];
					};
				};
				/** @description `Not Found` */
				404: {
					headers: Record<string, unknown>;
					content: {
						"application/json": components["schemas"]["57_not_found_404"];
					};
				};
			};
		};
		options?: never;
		head?: never;
		patch?: never;
		trace?: never;
	};
	"/ca/api/corpora/{corpusId}/filesets/{filesetId}/cancel_job": {
		parameters: {
			query?: never;
			header?: never;
			path: {
				/** @description Numeric corpus ID. For corpora querying. */
				corpusId: components["parameters"]["01_corpus_id"];
				/** @description ID of file subdirectories. If sets to 0 it will return top-level folder of documents, so if you have a web corpora with folders web1 and web2 it will return web1. */
				filesetId: components["parameters"]["06_fileset_id"];
			};
			cookie?: never;
		};
		get?: never;
		put?: never;
		/**
		 * Cancel running task. (RPC)
		 * @description Example: cancel downloading data from websites by web-crawler.
		 */
		post: {
			parameters: {
				query?: never;
				header?: never;
				path: {
					/** @description Numeric corpus ID. For corpora querying. */
					corpusId: components["parameters"]["01_corpus_id"];
					/** @description ID of file subdirectories. If sets to 0 it will return top-level folder of documents, so if you have a web corpora with folders web1 and web2 it will return web1. */
					filesetId: components["parameters"]["06_fileset_id"];
				};
				cookie?: never;
			};
			/** @description In this documentation, an empty request is used mostly used with the **RPC style** method where the content in a request is not needed (in most cases). RPC style endpoints focus on performing **one action** right (procedures, command) easier than **REST API**-based endpoints. It is not as scalable as REST API style. RPC is mostly used with HTTP: GET (to fetch information) and POST (to everything else) in CA api is it used with POST HTTP method. */
			requestBody?: {
				content: {
					"application/json": components["schemas"]["05_empty_request"];
				};
			};
			responses: {
				/** @description `OK` */
				200: {
					headers: Record<string, unknown>;
					content: {
						"application/json": components["schemas"]["69_rpc_style"];
					};
				};
				/** @description `Unauthorized` */
				401: {
					headers: Record<string, unknown>;
					content: {
						"application/json": components["schemas"]["23_unauthorized_rpc"];
					};
				};
				/** @description `Forbidden` (you need `upload` permission). */
				403: {
					headers: Record<string, unknown>;
					content: {
						"application/json": components["schemas"]["21_forbidden"];
					};
				};
				/** @description `Not Found` */
				404: {
					headers: Record<string, unknown>;
					content: {
						"application/json": components["schemas"]["45_not_found_RPC"];
					};
				};
			};
		};
		delete?: never;
		options?: never;
		head?: never;
		patch?: never;
		trace?: never;
	};
	"/ca/api/corpora/{corpusId}/filesets/{filesetId}/get_progress": {
		parameters: {
			query?: never;
			header?: never;
			path: {
				/** @description Numeric corpus ID. For corpora querying. */
				corpusId: components["parameters"]["01_corpus_id"];
				/** @description ID of file subdirectories. If sets to 0 it will return top-level folder of documents, so if you have a web corpora with folders web1 and web2 it will return web1. */
				filesetId: components["parameters"]["06_fileset_id"];
			};
			cookie?: never;
		};
		get?: never;
		put?: never;
		/**
		 * Shows the actual progress of a running task related to filesets. (RPC)
		 * @description A task like `downloading content from web` to create corpus with web crawler.
		 */
		post: {
			parameters: {
				query?: never;
				header?: never;
				path: {
					/** @description Numeric corpus ID. For corpora querying. */
					corpusId: components["parameters"]["01_corpus_id"];
					/** @description ID of file subdirectories. If sets to 0 it will return top-level folder of documents, so if you have a web corpora with folders web1 and web2 it will return web1. */
					filesetId: components["parameters"]["06_fileset_id"];
				};
				cookie?: never;
			};
			/** @description In this documentation, an empty request is used mostly used with the **RPC style** method where the content in a request is not needed (in most cases). RPC style endpoints focus on performing **one action** right (procedures, command) easier than **REST API**-based endpoints. It is not as scalable as REST API style. RPC is mostly used with HTTP: GET (to fetch information) and POST (to everything else) in CA api is it used with POST HTTP method. */
			requestBody?: {
				content: {
					"application/json": components["schemas"]["05_empty_request"];
				};
			};
			responses: {
				/** @description `OK` */
				200: {
					headers: Record<string, unknown>;
					content: {
						"application/json": components["schemas"]["59_filesets_get_progress"];
					};
				};
				/** @description `Forbidden` (you need `view` permission). */
				403: {
					headers: Record<string, unknown>;
					content: {
						"application/json": components["schemas"]["21_forbidden"];
					};
				};
				/** @description `Not Found` */
				404: {
					headers: Record<string, unknown>;
					content: {
						"application/json": components["schemas"]["45_not_found_RPC"];
					};
				};
			};
		};
		delete?: never;
		options?: never;
		head?: never;
		patch?: never;
		trace?: never;
	};
	"/ca/api/languages": {
		parameters: {
			query?: never;
			header?: never;
			path?: never;
			cookie?: never;
		};
		/** Retrieves a list of all languages. */
		get: {
			parameters: {
				query?: never;
				header?: never;
				path?: never;
				cookie?: never;
			};
			requestBody?: never;
			responses: {
				/** @description `OK` */
				200: {
					headers: Record<string, unknown>;
					content: {
						"application/json": {
							data?: Array<components["schemas"]["63_language"]>;
						};
					};
				};
			};
		};
		put?: never;
		post?: never;
		delete?: never;
		options?: never;
		head?: never;
		patch?: never;
		trace?: never;
	};
	"/ca/api/somefiles": {
		parameters: {
			query?: never;
			header?: never;
			path?: never;
			cookie?: never;
		};
		get?: never;
		put?: never;
		/** Upload aligned documents for creating parallel corpus. */
		post: {
			parameters: {
				query?: never;
				header?: never;
				path?: never;
				cookie?: never;
			};
			/** @description Aligned multilingual file (mostly in '.tmx' file type). */
			requestBody?: {
				content: {
					"multipart/form-data; boundary={boundary}": string;
				};
			};
			responses: {
				/** @description `Created` */
				201: {
					headers: Record<string, unknown>;
					content: {
						"application/json": components["schemas"]["78_somefiles_post"];
					};
				};
				/** @description `Bad Request` */
				400: {
					headers: Record<string, unknown>;
					content: {
						"application/json": components["schemas"]["40_bad_request_20"];
					};
				};
			};
		};
		delete?: never;
		options?: never;
		head?: never;
		patch?: never;
		trace?: never;
	};
	"/ca/api/somefiles/{somefileId}": {
		parameters: {
			query?: never;
			header?: never;
			path: {
				/** @description Alphanumeric multilanguage file ID */
				somefileId: components["parameters"]["08_somefile_id"];
			};
			cookie?: never;
		};
		/** Retrieves specific multilingual file metadata. */
		get: {
			parameters: {
				query?: never;
				header?: never;
				path: {
					/** @description Alphanumeric multilanguage file ID */
					somefileId: components["parameters"]["08_somefile_id"];
				};
				cookie?: never;
			};
			requestBody?: never;
			responses: {
				/** @description `OK` */
				200: {
					headers: Record<string, unknown>;
					content: {
						"application/json": components["schemas"]["78_somefiles_post"];
					};
				};
				/** @description `Not Found` */
				404: {
					headers: Record<string, unknown>;
					content: {
						"application/json": components["schemas"]["57_not_found_404"];
					};
				};
			};
		};
		/** Updates multilingual file metadata. */
		put: {
			parameters: {
				query?: never;
				header?: never;
				path: {
					/** @description Alphanumeric multilanguage file ID */
					somefileId: components["parameters"]["08_somefile_id"];
				};
				cookie?: never;
			};
			/**
			 * @description - `corpora`
			 *
			 *         - `guessed_language_code`
			 *
			 *           - `language_id` => Language iso-code. **ISO 639-1**. (string)
			 *
			 *           - `name` => Language name in **English**. (string)
			 */
			requestBody?: {
				content: {
					"application/json": components["schemas"]["19_somefiles_put"];
				};
			};
			responses: {
				/** @description `OK` */
				200: {
					headers: Record<string, unknown>;
					content: {
						"application/json": {
							data?: components["schemas"]["12_corpora_single_full"];
						};
					};
				};
				/** @description `Bad Request` */
				400: {
					headers: Record<string, unknown>;
					content: {
						"application/json": components["schemas"]["33_bad_request_13"];
					};
				};
				/** @description `Not Found` */
				404: {
					headers: Record<string, unknown>;
					content: {
						"application/json": components["schemas"]["57_not_found_404"];
					};
				};
			};
		};
		post?: never;
		delete?: never;
		options?: never;
		head?: never;
		patch?: never;
		trace?: never;
	};
	"/ca/api/tagsets/{templateId}": {
		parameters: {
			query?: never;
			header?: never;
			path: {
				/** @description Numerical template ID, but preloaded templates do not have ID but you can query them by their name. Example: `UNIVERSAL_3`. */
				templateId: components["parameters"]["03_template_id"];
			};
			cookie?: never;
		};
		/** Retrieves details of specified user template / tagset. */
		get: {
			parameters: {
				query?: never;
				header?: never;
				path: {
					/** @description Numerical template ID, but preloaded templates do not have ID but you can query them by their name. Example: `UNIVERSAL_3`. */
					templateId: components["parameters"]["03_template_id"];
				};
				cookie?: never;
			};
			requestBody?: never;
			responses: {
				/** @description `OK` */
				200: {
					headers: Record<string, unknown>;
					content: {
						"application/json": components["schemas"]["79_template"];
					};
				};
				/** @description `Unauthorized` */
				401: {
					headers: Record<string, unknown>;
					content: {
						"application/json": components["schemas"]["20_unauthorized"];
					};
				};
				/** @description `Forbidden` (you need permission to `read`). */
				403: {
					headers: Record<string, unknown>;
					content: {
						"application/json": components["schemas"]["27_forbidden_normal"];
					};
				};
				/** @description `Not Found` */
				404: {
					headers: Record<string, unknown>;
					content: {
						"application/json": components["schemas"]["57_not_found_404"];
					};
				};
			};
		};
		/** Updates specified user template/tagset. */
		put: {
			parameters: {
				query?: never;
				header?: never;
				path: {
					/** @description Numerical template ID, but preloaded templates do not have ID but you can query them by their name. Example: `UNIVERSAL_3`. */
					templateId: components["parameters"]["03_template_id"];
				};
				cookie?: never;
			};
			/**
			 * @description - `id` => Alphanumeric **template/tagset ID**. The terms **tagset** and **templates** are interchangeable. (string)
			 *
			 *       - `name` => Name of **template/tagset** file. (string)
			 *
			 *       - `owner_id` => Unique numeric owner ID (usually you). If tagset/template is preloaded Null. (integer)
			 *
			 *       - `owner_name` => Tagset/template owner name (usually you). If tagset/template is preloaded Null. (string)
			 *
			 *       - `has_pipeline` => Vertical creation is supported. False for legacy templates. (boolean)
			 *
			 *       - `has_tags` => Morphological tagging is supported. (boolean)
			 *
			 *       - `has_lemmas` => Lemmatization is supported. (boolean)
			 *
			 *       - `static_attributes` => A list of attributes which can appear in corpus.
			 *
			 *       - `structures` => A list of used structures. Examples \<s>, \<g>.
			 *
			 *       - `tagsetdoc` => URL leading to template/tagset documentation. (string)
			 *
			 *       - `content` => Content of tagset. (string)
			 *
			 *       - `default_sketchgrammar_id` => Not ID, as you probably imagine, but filename of preselected sketchgrammar (.m4 format). (string)
			 *
			 *       - `default_termgrammar_id` => Not ID, as you probably imagine, but filename of preselected sketchgrammar (.m4 format). (string)
			 *
			 *       - `sharing` => List.
			 *
			 *       - `users` => The ID of user you share template with.
			 *
			 *       - `id` => The ID of group you share template with.
			 */
			requestBody?: {
				content: {
					"application/x-www-form-urlencoded; charset=UTF-8": components["schemas"]["80_template_put"];
				};
			};
			responses: {
				/** @description `OK` */
				200: {
					headers: Record<string, unknown>;
					content: {
						"application/json": {
							data?: components["schemas"]["79_template"];
						};
					};
				};
				/** @description `Bad Request` */
				400: {
					headers: Record<string, unknown>;
					content: {
						"application/json": components["schemas"]["33_bad_request_13"];
					};
				};
				/** @description `Unauthorized` */
				401: {
					headers: Record<string, unknown>;
					content: {
						"application/json": components["schemas"]["20_unauthorized"];
					};
				};
				/** @description `Forbidden` (you need to be `owner` of the template). */
				403: {
					headers: Record<string, unknown>;
					content: {
						"application/json": components["schemas"]["27_forbidden_normal"];
					};
				};
				/** @description `Not Found` */
				404: {
					headers: Record<string, unknown>;
					content: {
						"application/json": components["schemas"]["57_not_found_404"];
					};
				};
			};
		};
		post?: never;
		/** Deletes user template. */
		delete: {
			parameters: {
				query?: never;
				header?: never;
				path: {
					/** @description Numerical template ID, but preloaded templates do not have ID but you can query them by their name. Example: `UNIVERSAL_3`. */
					templateId: components["parameters"]["03_template_id"];
				};
				cookie?: never;
			};
			requestBody?: never;
			responses: {
				/** @description `No Content` */
				204: {
					headers: Record<string, unknown>;
					content?: never;
				};
				/** @description `Bad Request` */
				400: {
					headers: Record<string, unknown>;
					content: {
						"application/json": components["schemas"]["33_bad_request_13"];
					};
				};
				/** @description `Unauthorized` */
				401: {
					headers: Record<string, unknown>;
					content: {
						"application/json": components["schemas"]["20_unauthorized"];
					};
				};
				/** @description `Forbidden` (you need to be `owner` of the template). */
				403: {
					headers: Record<string, unknown>;
					content: {
						"application/json": components["schemas"]["27_forbidden_normal"];
					};
				};
				/** @description `Not Found` */
				404: {
					headers: Record<string, unknown>;
					content: {
						"application/json": components["schemas"]["57_not_found_404"];
					};
				};
			};
		};
		options?: never;
		head?: never;
		patch?: never;
		trace?: never;
	};
	"/ca/api/users/me/get_used_space": {
		parameters: {
			query?: never;
			header?: never;
			path?: never;
			cookie?: never;
		};
		get?: never;
		put?: never;
		/** Retrieves the user's current word space state (default 1 000 000 words). (RPC) */
		post: {
			parameters: {
				query?: never;
				header?: never;
				path?: never;
				cookie?: never;
			};
			/** @description In this documentation, an empty request is used mostly used with the **RPC style** method where the content in a request is not needed (in most cases). RPC style endpoints focus on performing **one action** right (procedures, command) easier than **REST API**-based endpoints. It is not as scalable as REST API style. RPC is mostly used with HTTP: GET (to fetch information) and POST (to everything else) in CA api is it used with POST HTTP method. */
			requestBody?: {
				content: {
					"application/json": components["schemas"]["05_empty_request"];
				};
			};
			responses: {
				/** @description `OK` */
				200: {
					headers: Record<string, unknown>;
					content: {
						"application/json": components["schemas"]["81_get_used_space"];
					};
				};
				/** @description `Unauthorized` */
				401: {
					headers: Record<string, unknown>;
					content: {
						"application/json": components["schemas"]["23_unauthorized_rpc"];
					};
				};
			};
		};
		delete?: never;
		options?: never;
		head?: never;
		patch?: never;
		trace?: never;
	};
	"/ca/api/octerms/datasets": {
		parameters: {
			query?: never;
			header?: never;
			path?: never;
			cookie?: never;
		};
		get?: never;
		put?: never;
		/** Upload new dataset */
		post: {
			parameters: {
				query?: never;
				header?: never;
				path?: never;
				cookie?: never;
			};
			requestBody?: {
				content: {
					"multipart/form-data": {
						/**
						 * @description 'mono' for monolingual data
						 *
						 *     'aligned' for already aligned bilingual data like TMX
						 *
						 *     'translated' for bilingual data requiring automatic alignment
						 * @enum {string}
						 */
						mode?: "mono" | "aligned" | "translated";
						/** @description language code (only in monolingual mode) */
						l0?: string;
						/** @description source language code (only in bilingual modes) */
						l1?: string;
						/** @description target language code (only in bilingual modes) */
						l2?: string;
						/**
						 * Format: binary
						 * @description input data, can be multiple files for 'mono' mode; 'aligned' mode only takes first
						 */
						file?: string;
						/**
						 * Format: binary
						 * @description input data for source language in 'translated' mode
						 */
						files1?: string;
						/**
						 * Format: binary
						 * @description input data for target language in 'translated' mode
						 */
						files2?: string;
						/** @description if true, dataset will be deleted automatically in a few days, otherwise corpora will be visible in Sketch Engine and take up storage quota */
						temporary?: boolean;
						/** @description Human-readable corpus name for non-temporary corpora */
						corpus_name?: string;
						/** @description optional URL to be "pinged" by a HTTP GET request when results are ready */
						pingback?: string;
						/** @description source language identifier in aligned data */
						tmx1?: string;
						/** @description target language identifier in aligned data */
						tmx2?: string;
					};
				};
			};
			responses: {
				/** @description Created */
				201: {
					headers: Record<string, unknown>;
					content: {
						"application/json": {
							data?: components["schemas"]["octerms_dataset"];
						};
					};
				};
			};
		};
		delete?: never;
		options?: never;
		head?: never;
		patch?: never;
		trace?: never;
	};
	"/ca/api/octerms/datasets/{datasetId}": {
		parameters: {
			query?: never;
			header?: never;
			path: {
				/** @description Unique dataset ID */
				datasetId: string;
			};
			cookie?: never;
		};
		/** Retrieves a dataset, showing progress and results (if ready) */
		get: {
			parameters: {
				query?: never;
				header?: never;
				path: {
					/** @description Unique dataset ID */
					datasetId: string;
				};
				cookie?: never;
			};
			requestBody?: never;
			responses: {
				/** @description `OK` */
				200: {
					headers: Record<string, unknown>;
					content: {
						"application/json": components["schemas"]["octerms_dataset"];
					};
				};
			};
		};
		put?: never;
		post?: never;
		delete?: never;
		options?: never;
		head?: never;
		patch?: never;
		trace?: never;
	};
}
export type webhooks = Record<string, never>;
export interface components {
	schemas: {
		"01_corp_info": {
			/** @description A list of WPOS (`Word Part Of Speech`). Presented as a pair of WPOS names and regular expression matching the WPOS tags. */
			wposlist?: Array<Array<string>>;
			/** @description A list of LPOS (`Lemma Part Of Speech`). Presented as a pair of LPOS names and LPOS tags. Used in Concordance forms. */
			lposlist?: Array<Array<string>>;
			/** @description Has the same format as LPOSLIST but WSPOSLIST is used in Word Sketch and Thesaurus forms. */
			wsposlist?: Array<Array<string>>;
			/** @description A list of objects containing detailed information about attributes occuring in specified corpora. */
			attributes?: Array<{
				/**
				 * @description Name of attribute. Lempos_lc = Lemma part of speech lowercase.
				 * @example lempos_lc
				 */
				name?: string;
				/**
				 * @description The number of attributes of the given name in the corpus. Each is counted only once even if it appears in corpus many times.
				 * @example 524493
				 */
				id_range?: number;
				/**
				 * @description An extra description.
				 * @example lempos(lowercase)
				 */
				label?: string;
				/**
				 * @description Represents the rule according which the attribute should be derived from the original attribute. The attribute `lempos_lc` is derived from `lempos` to save disk space etc. [Read more](https://www.sketchengine.eu/documentation/corpus-configuration-file-all-features/#Dynamicattributes).
				 * @example utf8lowercase
				 */
				dynamic?: string;
				/**
				 * @description A name of attribute this attribute is derived from. Empty string if the attribute is not derived from any.
				 * @example lempos
				 */
				fromattr?: string;
			}>;
			/** @description A list of structures in the corpus. */
			structs?: Array<string>;
			/**
			 * @description The full name of the corpus.
			 * @example British National Corpus (BNC)
			 */
			name?: string;
			/**
			 * @description The language of the corpus.
			 * @example English
			 */
			lang?: string;
			/**
			 * @description An URL with more information about the corpus. Empty string if none.
			 * @example https://www.sketchengine.eu/british-national-corpus/
			 */
			infohref?: string;
			/**
			 * @description More information about the corpus.
			 * @example A balanced English corpus of samples of a written and spoken language of British English from the later part of the 20th century (1969–1994). The spoken part is accompanied by audio recordings.
			 */
			info?: string;
			/**
			 * @description The used character encoding in the corpus.
			 * @example UTF-8
			 */
			encoding?: string;
			/**
			 * @description An URL with more information about the POS tagger used in the corpus. Information like meanings of POS tags, comparition with other tagsets for specified language etc.
			 * @example https://www.sketchengine.eu/english-treetagger-pipeline-2/
			 */
			tagsetdoc?: string;
			/**
			 * @description The default attribute for the corpus. Usually `word` or `lc`.
			 * @example lc
			 */
			defaultattr?: string;
			starattr?: string;
			/**
			 * @description A boolean value indicating if the corpus is unicameral (not distinguishing between upper and lower case).
			 * @example false
			 */
			unicameral?: boolean;
			/**
			 * @description The order of writing in language of the used corpus.
			 * @example false
			 */
			righttoleft?: boolean;
			errsetdoc?: string;
			/**
			 * @description The attribute name for which word sketches are computed, e.g. `lempos`.
			 * @example lempos_lc
			 */
			wsattr?: string;
			/**
			 * @description A path to the `used` word sketches grammar definition file.
			 * @example /corpora/wsdef/english-penn_tt-3.1.wsdef.m4
			 */
			wsdef?: string;
			/**
			 * @description A path to the term grammar definition file used in the corpus.
			 * @example /corpora/wsdef/english-penn_tt-terms-3.1.termdef.m4
			 */
			termdef?: string;
			/** @description A list of diachronic subcorporas. Diachronic corpus is corpus with timestamps to watch development of the language in time. */
			diachronic?: Array<string>;
			/** @description A list of aligned corpora names. `Example used here is from different corpus because BNC corpus is not parallel.` */
			aligned?: Array<Array<string>>;
			/** @description Is shown only if the specified corpus is parallel. A list of dictionaries containing detailed information about each aligned subcorpus. */
			aligned_details?: Array<{
				/**
				 * @description The name of the aligned corpus.
				 * @example Example_1
				 */
				name?: string;
				/**
				 * @description The language of the aligned corpus.
				 * @example Czech
				 */
				language_name?: string;
				Wposlist?: Array<{
					/**
					 * @description The name of the part of speech category
					 * @example noun
					 */
					n?: string;
					/**
					 * @description A regex matching the category
					 * @example k1.*
					 */
					v?: string;
				}>;
				Lposlist?: Array<{
					/**
					 * @description Name of part of speech category
					 * @example noun
					 */
					n?: string;
					/**
					 * @description The shortcut for better representation of a speech category.
					 * @example -n
					 */
					v?: string;
				}>;
				/** @description Represent if the language of aligned corpus differentiates between upper case and lower case. */
				has_case?: boolean;
				/** @description Represent if the language of aligned corpus has lemma of not. */
				has_lemma?: boolean;
				/**
				 * @description URL with closer information.
				 * @example https://www.sketchengine.eu/tagset-reference-for-czech
				 */
				tagsetdoc?: string;
			}>;
			/** @description A list of attributes (text types) that will be used for Frequency. Text types are metadata attached to the corpus structures. You can access it via Sketch Engine dashboard -> Corpus Info -> Text Type Analysis. */
			freqttattrs?: Array<string>;
			/** @description A list of subcorpus attributes for the corpus. */
			subcorpattrs?: Array<string>;
			/**
			 * @description The attribute of a structure to display as a default reference in the left-hand column of a concordance. The syntax is like `=structure.attribute`, e.g. `=doc.id` for displaying only the value of `doc.id`.
			 * @example =bncdoc.alltyp
			 */
			shortref?: string;
			/**
			 * @description A structures that is considered as individual documents. Usually `doc`.
			 * @example bncdoc
			 */
			docstructure?: string;
			/**
			 * @description Information about the new version of the corpus, if available. Empty string if not.
			 * @example
			 */
			newversion?: string;
			/** @description A list of structures appearing in corpus. */
			structures?: Array<{
				/**
				 * @description The name of the structure.
				 * @example head
				 */
				name?: string;
				/** @description Just some extra information. Empty string if none. */
				label?: string;
				/** @description More detailed information. */
				attributes?: Array<{
					/**
					 * @description Name of attribute.
					 * @example rend
					 */
					name?: string;
					/**
					 * @description An extra information about attribute. Empty string if none.
					 * @example
					 */
					label?: string;
					/** @description Dynamic (derived) attribute. Empty string if none. [Read more](https://www.sketchengine.eu/documentation/corpus-configuration-file-all-features/#Dynamicattributes) */
					dynamic?: string;
					/** @description A name of attribute this attribute is derived from. Empty string if none. */
					fromattr?: string;
					/**
					 * @description Number of occurences.
					 * @example 5
					 */
					size?: number;
				}>;
				/**
				 * @description Number of occurences of `head` structure in this case.
				 * @example 14868944
				 */
				size?: string;
			}>;
			/**
			 * @description A boolean value indicating if the corpus is an error corpus (not compiled etc.).
			 * @example false
			 */
			is_error_corpus?: boolean;
			/**
			 * @description The structural context for the corpus. Empty string if none.
			 * @example
			 */
			structctx?: string;
			/**
			 * @description A boolean value indicating if the default filter link is enabled.
			 * @example false
			 */
			deffilerlink?: boolean;
			/** @description A list of default structures for the corpus. */
			defaultstructs?: Array<string>;
			/** @description The text types for which the word highlights [Read more](https://www.sketchengine.eu/find-x-word-highlights/) are computed. */
			wsttattrs?: string;
			/**
			 * @description A boolean value indicating if the terms file is compiled.
			 * @example true
			 */
			terms_compiled?: boolean;
			/**
			 * @description A date of compilation in format `mm/dd/yyyy hh:mm:ss`.
			 * @example 06/30/2017 07:34:25
			 */
			compiled?: string;
			gramrels?: Array<string>;
			sizes?: {
				/** @example 112345722 */
				tokencount?: string;
				/** @example 96134547 */
				wordcount?: string;
				/**
				 * @description Document counter.
				 * @example 4054
				 */
				doccount?: string;
				/**
				 * @description Paragraph counter.
				 * @example 1514906
				 */
				parcount?: string;
				/**
				 * @description Sentences counter.
				 * @example 6052190
				 */
				sentcount?: string;
				/**
				 * @description Wordcount after normalization.
				 * @example 96134547
				 */
				normsum?: string;
			};
			/** @description A list of tuples containing sizes of aligned corpora. */
			alsizes?: Array<string>;
			/** @description The registry dump for the corpus (detailed information about corpus setting), if the registry parameter is set. */
			registry_dump?: string;
			/** @description The registry text for the corpus (detailed information about corpus setting), if the registry parameter is set. */
			registry_text?: string;
			subcorpora?: Array<{
				/**
				 * @description A name of subcorpus.
				 * @example Test
				 */
				n?: string;
				/**
				 * @description A name of subcorpus.
				 * @example Test
				 */
				name?: string;
				/**
				 * @description Represent if subcorpora is created by user or not.
				 * @example 1
				 */
				user?: number;
				/**
				 * @description Number of tokens in subcorpus.
				 * @example 271454
				 */
				tokens?: number;
				/**
				 * @description The percentage of subcorpus size from total corpus size.
				 * @example 0.24162379765559744
				 */
				relsize?: number;
				/**
				 * @description Number of words in subcorpus.
				 * @example 232283
				 */
				words?: number;
				/** @example s */
				struct?: string;
				/** @example Q:q[lc="dog" | lemma_lc="dog"] */
				query?: string;
			}>;
			/**
			 * @description Current API version.
			 * @example 5.62.3
			 */
			api_version?: string;
			/**
			 * @description Current version of Manatee.
			 * @example 2.36.7-SkE-2.219.2
			 */
			manatee_version?: string;
			/** @description Just summary section of parsed query parameters used in this endpoint call. These parameters are all documented in the beggining of every endpoint box (after you unwrap the endpoint). */
			request?: {
				/** @example 1 */
				subcorpora?: string;
				/** @example 1 */
				struct_attr_stats?: string;
				/** @example preloaded/bnc2_tt21 */
				corpname?: string;
			};
		};
		"02_wordlist": {
			/** @example 20000 */
			new_maxitems?: number;
			/**
			 * @description Word list limit, amount of words to be display.
			 * @example 1000
			 */
			wllimit?: number;
			/** @example 0 */
			lastpage?: number;
			/**
			 * @description An additional note to displayed results.
			 * @example You are allowed to see only 1000 items.
			 */
			note?: string;
			/**
			 * @description Number if displayed items (word - frequency).
			 * @example 165953
			 */
			total?: number;
			/**
			 * @description Sum of all frequencies.
			 * @example 111680004
			 */
			totalfrq?: number;
			/** @description A result list. */
			items?: Array<{
				/**
				 * @description The word to which the frequency has been calculated.
				 * @example the
				 */
				str?: string;
				/**
				 * @description The word frequency.
				 * @example 6054939
				 */
				frq?: number;
				/**
				 * @description The relative word frequency. Relative frequency is a way of expressing how often something happens compared to other events or items in a given group.
				 * @example 53895.59026
				 */
				relfreq?: number;
			}>;
			/** @example word(lowercase) */
			wlattr_label?: string;
			/**
			 * @description Frequency type. Other possible values can be `average reduced frequency`, `document frequency`, `score`.
			 * @example frequency
			 */
			frtp?: string;
			/** @example 5.62.3 */
			api_version?: string;
			/** @example 2.36.7-SkE-2.219.2 */
			manatee_version?: string;
			/** @description Just summary section of parsed query parameters used in this endpoint call. These parameters are all documented in the beggining of every endpoint box. */
			request?: {
				/** @example 5 */
				wlminfreq?: string;
				/** @example 0 */
				random?: string;
				/** @example 1 */
				include_nonwords?: string;
				/** @example simple */
				wltype?: string;
				/** @example 20000 */
				wlmaxitems?: string;
				/** @example frq */
				wlsort?: string;
				/** @example 1 */
				wlicase?: string;
				/** @example 1 */
				wlpage?: string;
				/** @example 1 */
				reldocf?: string;
				/** @example .* */
				wlpat?: string;
				/** @example 1 */
				relfreq?: string;
				/** @example lc */
				wlattr?: string;
				/** @example 0 */
				wlmaxfreq?: string;
				/** @example preloaded/bnc2_tt21 */
				corpname?: string;
			};
		};
		"03_wsketch": {
			Gramrels?: Array<{
				/**
				 * @description Name of grammar relation. `\"%w"\` == inputted lemma.
				 * @example modifiers of \"%w\"
				 */
				name?: string;
				/**
				 * @description Number of all occurences for specified grammar relation.
				 * @example 3374
				 */
				count?: number;
				/**
				 * @description Calculated LogDice score for specified grammar relation.
				 * @example 28.61
				 */
				score?: number;
				/** @example [ws(1, 213269638)] */
				conclink?: string;
				/** @example 213269638 */
				seek?: string;
				/** @description List af all modifiers of searched word. */
				Words?: Array<{
					/**
					 * @description The modifier of inputted lemma.
					 * @example guide
					 */
					word?: string;
					/**
					 * @description Lempos of the modifier.
					 * @example guide-n
					 */
					lempos?: string;
					/**
					 * @description Number of occurences.
					 * @example 74
					 */
					count?: number;
					/**
					 * @description LogDice score for this combination.
					 * @example 9.22
					 */
					score?: number;
					/** @example 18083018751 */
					seek?: string;
					/**
					 * @description An ID of that combination.
					 * @example 10651
					 */
					id?: number;
					/**
					 * @description Combined with given lemma.
					 * @example guide dog
					 */
					cm?: string;
					/** @example 20 */
					cmf?: number;
					High?: Array<string>;
					/**
					 * @description An unique identifier for future processing, the first number is level of immersion (2) and second is seek number (18083018751). Example of usage as concordance query: json={"concordance_query":[{"queryselector":"cqlrow","cql":"[ws(2, 18081564043)]","default_attr":""}]}).
					 * @example [ws(2, 18083018751)]
					 */
					conclink?: string;
					/** @example 18083018751 */
					filterseek?: string;
					/** @example guide-n */
					filterwords?: string;
				}>;
				/**
				 * @description The inputted lemma.
				 * @example dog
				 */
				headword?: string;
			}>;
			Grnowords?: Array<{
				/**
				 * @description Name of grammar relation. `\"%w"\` == inputted lemma.
				 * @example verbs with particle "off" and "%w" as object
				 */
				name?: string;
				/**
				 * @description Number of occurences.
				 * @example 12
				 */
				count?: number;
				/**
				 * @description LogDice score for this combination.
				 * @example 0.1
				 */
				score?: number;
				/** @example [ws(1, 213484912)] */
				conclink?: string;
				/** @example 213484912 */
				seek?: string;
				/** @description List af all modifiers of searched word. */
				Words?: Array<string>;
				/**
				 * @description The inputted lemma.
				 * @example dog
				 */
				headword?: string;
			}>;
			restriction?: string;
			seppage?: string;
			/**
			 * @description The lemma part of speech.
			 * @example -n
			 */
			lpos?: string;
			/** @description Alternative lemma part of speech. */
			alt_lposes?: Array<{
				/**
				 * @description Lemma part of speech. In this example the dog is used as verb.
				 * @example dog-v
				 */
				lempos?: string;
				/**
				 * @description Shortcut for `verb`.
				 * @example -v
				 */
				pos?: string;
				/**
				 * @description Frequency.
				 * @example 292
				 */
				frq?: number;
				/**
				 * @description The inputted lemma.
				 * @example dog
				 */
				lemma?: string;
				/**
				 * @description Represent if the word sketch was successful.
				 * @example ok
				 */
				ws_status?: string;
			}>;
			/**
			 * @description The inputted lemma.
			 * @example dog
			 */
			lemma?: string;
			/**
			 * @description Concordance query.
			 * @example q=alempos_lc%2C%22dog-n%22
			 */
			lemma_conc_link?: string;
			/**
			 * @description Represent if the word sketch was successful.
			 * @example ok
			 */
			ws_status?: string;
			/**
			 * @description Frequency.
			 * @example 11795
			 */
			freq?: number;
			/**
			 * @description The relative word frequency. Relative frequency is a way of expressing how often something happens compared to other events or items in a given group.
			 * @example 104.98842136596888
			 */
			relfreq?: number;
			/** @example British National Corpus (BNC) */
			corp_full_name?: string;
			/**
			 * @description Collocates with a frequency below this limit will not be displayed.
			 * @example 5
			 */
			minfreq?: number;
			/** @example 100 */
			wllimit?: number;
			Histograms?: Array<number>;
			filterwords?: string;
			filterseek?: string;
			gramrels?: string;
			/** @example false */
			combined?: boolean;
			/** @description A dictionary of lemma part of speech and it's shortcuts. */
			lpos_dict?: {
				/** @example -n */
				noun?: string;
				/** @example -v */
				verb?: string;
				/** @example -j */
				adjective?: string;
				/** @example -a */
				adverb?: string;
				/** @example -d */
				pronoun?: string;
			};
			Bimmenu?: Array<Array<string>>;
			/**
			 * @description The second lemma for word sketch. This attribute is shown only when `bim` parameters are inserted, its like `Translate` activated in Sketch Engine UI.
			 * @example Hund
			 */
			lemma2?: string;
			/**
			 * @description The name of second corpus. This attribute is shown only when `bim` parameters are inserted, its like `Translate` activated in Sketch Engine UI.
			 * @example German Web 2020 (deTenTen20)
			 */
			corp_full__name2?: string;
			/**
			 * @description Frequency for second lemma. This attribute is shown only when `bim` parameters are inserted, its like `Translate` activated in Sketch Engine UI.
			 * @example 2246813
			 */
			freq2?: number;
			/**
			 * @description Relative frequency for second lemma. This attribute is shown only when `bim` parameters are inserted, its like `Translate` activated in Sketch Engine UI.
			 * @example 106.99313991266335
			 */
			relfreq2?: number;
			/**
			 * @description Concordance query 2. This attribute is shown only when `bim` parameters are inserted, its like `Translate` activated in Sketch Engine UI.
			 * @example q=alempos%2C%22Hund-n%22
			 */
			lemma_conc_link2?: string;
			/** @description Bilingual grammar relations fo second lemma. This attribute is shown only when `bim` parameters are inserted, its like `Translate` activated in Sketch Engine UI. */
			bimgramrels?: string;
			/** @description Shown only when `translation` is activated in UI or `bim` parameters inserted. */
			Lemmatrans?: Array<Array<string>>;
			/**
			 * @description Source language. Shown only when `translation` is activated in UI or `bim` parameters inserted.
			 * @example English
			 */
			src_lang?: string;
			/**
			 * @description Target language. Shown only when `translation` is activated in UI or `bim` parameters inserted.
			 * @example German
			 */
			tgt_lang?: string;
			/** @description A dictionary of lemma2 part of speech and it's shortcuts. Shown only when `translation` is activated in UI or `bim` parameters inserted. */
			bim_lpos_dict?: {
				/** @example -n */
				noun?: string;
				/** @example -v */
				verb?: string;
				/** @example -j */
				adjective?: string;
				/** @example -a */
				adverb?: string;
				/** @example -i */
				preposition?: string;
			};
			/** @example 5.63.1 */
			api_version?: string;
			/** @example 2.36.7-SkE-2.221 */
			manatee_version?: string;
			/** @description Just summary section of parsed query parameters used in this endpoint call. These parameters are all documented in the beggining of every endpoint box (after you unwrap the endpoint). */
			request?: {
				/** @example 0.15 */
				minsim?: string;
				/** @example preloaded/bnc2_tt21 */
				corpname?: string;
				/** @example dog */
				lemma?: string;
				/** @example s */
				sort_vs_colums?: string;
				/** @example 0 */
				minscore?: string;
				/** @example 1 */
				structured?: string;
				/** @example 12 */
				maxitems?: string;
				/** @example 0 */
				clustercolls?: string;
				/** @example 1 */
				sort_gramrels?: string;
				/** @example auto */
				minfreq?: string;
			};
		};
		"04_thes": {
			Words?: Array<{
				/**
				 * @description The found synonym.
				 * @example horse
				 */
				word?: string;
				/**
				 * @description The score which represent how precise is the synonym. The higher = better.
				 * @example 0.295
				 */
				score?: number;
				/**
				 * @description The frequency of found synonym in corpus.
				 * @example 12168
				 */
				freq?: number;
				/** @example 5753 */
				id?: number;
			}>;
			/**
			 * @description Lemma for which the synonyms are searched.
			 * @example dog
			 */
			lemma?: string;
			/**
			 * @description Concordance query.
			 * @example q=alempos_lc%2C%22dog-n%22
			 */
			lemma_conc_link?: string;
			/**
			 * @description The POS type of lemma for which the synonyms are searched.
			 * @example -n
			 */
			lpos?: string;
			/**
			 * @description The frequency of the lemma.
			 * @example 11795
			 */
			freq?: number;
			/** @example ok */
			ws_status?: string;
			/**
			 * @description Relative frequency.
			 * @example 104.98842136596888
			 */
			relfreq?: number;
			/** @example British National Corpus (BNC) */
			corp_full_name?: string;
			/** @example 3 */
			minfreq?: number;
			/** @example lempos_lc */
			wsattr?: string;
			/** @description The other possible POS types. */
			alt_lposes?: Array<{
				/** @example dog-v */
				lempos?: string;
				/** @example -v */
				pos?: string;
				/** @example 292 */
				frq?: number;
				/** @example dog */
				lemma?: string;
				/** @example OK */
				ws_status?: string;
			}>;
			wspos_dict?: {
				/** @example noun */
				"-n"?: string;
				/** @example verb */
				"-v"?: string;
				/** @example adjective */
				"-j"?: string;
				/** @example adverb */
				"-a"?: string;
				/** @example pronoun */
				"-d"?: string;
			};
			/** @example 5.63.1 */
			api_version?: string;
			/** @example 2.36.7-SkE-2.221 */
			manatee_version?: string;
			/** @description Just summary section of parsed query parameters used in this endpoint call. These parameters are all documented in the beggining of every endpoint box (after you unwrap the endpoint). */
			request?: {
				/** @example 0 */
				minthesscore?: string;
				/** @example 1000 */
				maxthesitems?: string;
				/** @example 0.3 */
				minsim?: string;
				/** @example dog */
				lemma?: string;
				/** @example 0 */
				clusteritems?: string;
				/** @example preloaded/bnc2_tt21 */
				corpname?: string;
			};
		};
		"05_wsdiff": {
			/** @example 1 */
			diff_step?: number;
			/** @example 2.0 */
			diff_step_str?: string;
			content?: {
				lemma1_only?: Array<{
					/** @example  */
					break?: string;
					table?: {
						rows?: Array<{
							/** @example 147517412085 */
							seek1?: string;
							/** @example 0 */
							seek2?: string;
							/** @example 25954 */
							cnt1?: number;
							/** @example 0 */
							cnt2?: number;
							/** @example 7.432 */
							rnk1?: number;
							/** @example 0 */
							rnk2?: number;
							/** @example sized */
							word?: string;
							/** @example w */
							q_prefix1?: string;
							/** @example w */
							q_prefix2?: string;
							/** @example  */
							class?: string;
						}>;
						header?: Array<string>;
					};
				}>;
				lemma2_only?: Array<{
					/** @example  */
					break?: string;
					table?: {
						rows?: Array<{
							/** @example 0 */
							seek1?: string;
							/** @example 971957758033 */
							seek2?: string;
							/** @example 0 */
							cnt1?: number;
							/** @example 742 */
							cnt2?: number;
							/** @example 0 */
							rnk1?: number;
							/** @example 4.421 */
							rnk2?: number;
							/** @example guilty */
							word?: string;
							/** @example w */
							q_prefix1?: string;
							/** @example w */
							q_prefix2?: string;
							/** @example  */
							class?: string;
						}>;
						header?: Array<string>;
					};
				}>;
				common?: Array<{
					break?: string;
					table?: {
						rows?: Array<{
							/** @example 18075240564 */
							seek1?: string;
							/** @example 0 */
							seek2?: string;
							/** @example 17 */
							cnt1?: number;
							/** @example 0 */
							cnt2?: number;
							/** @example 7.725 */
							mk1?: number;
							/** @example 0 */
							mk2?: number;
							/** @example gun */
							word?: string;
							/** @example w */
							q_prefix1?: string;
							/** @example w */
							q_prefix2?: string;
							/** @example 3 */
							class?: string;
						}>;
						header?: Array<string>;
					};
				}>;
			};
			/** @example 11795 */
			frq1?: number;
			/** @example 5242 */
			frq2?: number;
			/** @example dog */
			lbl1?: string;
			/** @example cat */
			lbl2?: string;
			/** @example -n */
			lpos?: string;
			/** @example q=alempos_lc%2C%22dog-n%22 */
			conc_link1?: string;
			/** @example q=alempos_lc%2C%22cat-n%22 */
			conc_link2?: string;
			alt_lposes?: Array<{
				/** @example dog-v */
				lempos?: string;
				/** @example -v */
				pos?: string;
				/** @example 292 */
				frq?: number;
				/** @example dog */
				lemma?: string;
				/** @example ok */
				ws_status?: string;
			}>;
			/** @example dog */
			lemma?: string;
			/** @example alempos_lc,\"dog-n\" */
			lemma_conc_link?: string;
			/** @example ok */
			ws_status?: string;
			/** @example 11795 */
			freq?: number;
			/** @example 104.98842136596888 */
			relfreq?: number;
			/** @example British National Corpus (BNC) */
			corp_full_name?: string;
			/** @example 4 */
			minfreq?: number;
			/** @example 100 */
			wlimit?: number;
			wspos_dict?: {
				/** @example noun */
				"-n"?: string;
				/** @example verb */
				"-v"?: string;
				/** @example adjective */
				"-j"?: string;
				/** @example adverb */
				"-a"?: string;
				/** @example pronoun */
				"-d"?: string;
			};
			/** @example  */
			gramrels?: string;
			/** @example 5.63.1 */
			api_version?: string;
			/** @example 2.36.7-SkE-2.221 */
			manatee_version?: string;
			request?: {
				/** @example preloaded/bnc2_tt21 */
				corpname?: string;
				/** @example dog */
				lemma?: string;
				/** @example lemma */
				diff_by?: string;
				/** @example 12 */
				maxcommon?: string;
				/** @example auto */
				minfreq?: string;
				/** @example cat */
				lemma_2?: string;
				/** @example 5 */
				maxexclusive?: string;
				/** @example 1 */
				separate_blocks?: string;
			};
		};
		"06_concordance": {
			Lines?: Array<{
				/** @example 10336 */
				toknum?: number;
				/** @example 1 */
				hitlen?: number;
				Refs?: Array<string>;
				Tbl_refs?: Array<string>;
				Left?: Array<{
					/** @example <s> */
					strc?: string;
				}>;
				Kwic?: Array<{
					/** @example dogs */
					str?: string;
					/** @example 1 */
					coll?: number;
				}>;
				Right?: Array<{
					/** @example </s> */
					str?: string;
				}>;
				Links?: Array<string>;
				/** @example _ */
				linegroup?: string;
				/** @example 0 */
				linegroup_id?: number;
			}>;
			/** @example 1 */
			fromp?: number;
			/** @example 12087 */
			concsize?: number;
			/** @example 10000 */
			concordance_size_limit?: number;
			Sort_idx?: Array<string>;
			/** @example false */
			righttoleft?: boolean;
			Aligned_rtl?: Array<string>;
			/** @example 0 */
			numofcolls?: number;
			/** @example 1 */
			finished?: number;
			/** @example 12087 */
			fullsize?: number;
			/** @example 107.59 */
			relsize?: number;
			/** @description Relevant only in parallel_concordance. */
			Aligned?: Array<{
				n?: string;
				label?: string;
			}>;
			q?: Array<string>;
			Desc?: {
				/** @example Query */
				op?: string;
				/** @example [lc=\"dog\" | lemma_lc=\"dog\"] */
				arg?: string;
				/** @example dog */
				nicearg?: string;
				/** @example 107.59 */
				rel?: number;
				/** @example 12087 */
				size?: number;
				/** @example q=q%5Blc%3D%22dog%22+%7C+lemma_lc%3D%22dog%22%5D */
				tourl?: string;
			};
			/** @example 0 */
			port?: number;
			gdex_scores?: Array<string>;
			sc_strcts?: Array<Array<string>>;
			/** @example 5.63.1 */
			api_version?: string;
			/** @example 2.36.7-SkE-2.221 */
			manatee_version?: string;
			request?: {
				concordance_query?: Array<{
					/** @example iqueryrow */
					queryselector?: string;
					/** @example dog */
					iquery?: string;
				}>;
				/** @example preloaded/bnc2_tt21 */
				corpname?: string;
				/** @example 100# */
				kwicleftctx?: string;
				/** @example s,g */
				structs?: string;
				/** @example sen */
				viewmode?: string;
				/** @example all */
				attr_allpos?: string;
				/** @example 1 */
				fromp?: string;
				/** @example {\"concordance_query\":[{\"queryselector\":\"iqueryrow\",\"iquery\":\"dog\"}]} */
				json?: string;
				/** @example 100# */
				kwicrightctx?: string;
				/** @example =bncdoc.alltyp */
				refs?: string;
				/** @example q */
				cup_hl?: string;
				/** @example word */
				attrs?: string;
				/** @example 20 */
				pagesize?: string;
			};
		};
		"07_subcorp": {
			/** @example Australian domain .au */
			subcname?: string;
			SubcorpList?: Array<{
				/** @example Australian domain .au */
				n?: string;
				/** @example Australian domain .au */
				name?: string;
				/** @example 0 */
				user?: number;
			}>;
			/** @example 5.63.1 */
			api_version?: string;
			/** @example 2.36.7-SkE-2.221 */
			manatee_version?: string;
			request?: {
				/** @example preloaded/ententen13_tt2_1 */
				corpname?: string;
			};
		};
		"08_extract_keywords": {
			keywords?: Array<{
				/** @example galský */
				item?: string;
				/** @example 2411.16 */
				score?: number;
				/** @example 2 */
				frq1?: number;
				/** @example 512 */
				frq2?: number;
				/** @example 4291.8457 */
				rel_frq1?: number;
				/** @example 0.78041 */
				rel_frq2?: number;
				/** @example [lemma=\"galský\"] */
				query?: string;
			}>;
			/** @example Slovak Web 2011 (skTenTen11) */
			referece_corpus_name?: string;
			/** @example 656067998 */
			reference_corpus_size?: number;
			/** @example 656067998 */
			reference_subcorpus_size?: number;
			/** @example 466 */
			subcorpus_size?: number;
			/** @example 466 */
			corpus_size?: number;
			/** @example 175 */
			total?: number;
			/** @example 466 */
			totalfrq1?: number;
			/** @example 250525622 */
			totalfrq2?: number;
			/** @example 1000 */
			wllimit?: number;
			/** @example  */
			note?: string;
			/** @example 5.63.1 */
			api_version?: string;
			/** @example 2.36.7-SkE-2.221 */
			manatee_version?: string;
			request?: {
				/** @example 1 */
				alnum?: string;
				/** @example 0 */
				maxfreq?: string;
				/** @example 1 */
				minfreq?: string;
				/** @example .* */
				wlpat?: string;
				/** @example lemma */
				attr?: string;
				/** @example 1 */
				keywords?: string;
				/** @example preloaded/sktenten11_rft1 */
				ref_corpname?: string;
				/** @example 1 */
				simple_n?: string;
				/** @example lemma */
				k_attr?: string;
				/** @example 0 */
				include_nonwords?: string;
				/** @example :"0" */
				reldocf?: string;
				/** @example 1 */
				icase?: string;
				/** @example 1 */
				onealpha?: string;
				/** @example 1000 */
				max_keywords?: string;
				/** @example user/matuskostka1/aaaaa_slovak */
				corpname?: string;
			};
		};
		"09_attr_vals": {
			/**
			 * @description The regular expression from query parameter `avpat`.
			 * @example .*
			 */
			query?: string;
			/** @description Suggestions for avattr `bncdoc.author`. */
			suggestions?: Array<string>;
			/**
			 * @description Represent if the `suggestion` list is complete.
			 * @example false
			 */
			no_more_values?: boolean;
			/** @example 5.63.1 */
			api_version?: string;
			/** @example 2.36.7-SkE-2.221 */
			manatee_version?: string;
			request?: {
				/** @example .* */
				avpat?: string;
				/** @example 15 */
				avmaxitems?: string;
				/** @example 1 */
				ajax?: string;
				/** @example preloaded/bnc2_tt21 */
				corpname?: string;
				/** @example 0 */
				avfrom?: string;
				/** @example 1 */
				icase?: string;
				/** @example bncdoc.author */
				avattr?: string;
			};
		};
		"10_collx": {
			Head?: Array<{
				/** @example Cooccurrence count */
				n?: string;
				/** @example f */
				s?: string;
				/** @example style="word-wrap: break-word; width: 5em;" */
				style?: string;
			}>;
			Items?: Array<{
				/** @example Belvin */
				str?: string;
				/** @example 7 */
				freq?: number;
				/** @example 5 */
				coll_freq?: number;
				Stats?: Array<{
					/** @example 2.64537 */
					s?: string;
					/** @example t */
					n?: string;
				}>;
				/** @example q=P-5+5+1+%5Bword%3D%22Belvin%22%5D */
				pfilter?: string;
				/** @example q=N-5+5+1+%5Bword%3D%22Belvin%22%5D */
				nfilter?: string;
			}>;
			/** @example 0 */
			lastpage?: number;
			/** @example 1000 */
			wllimit?: number;
			/** @example 22685 */
			concsize?: number;
			Desc?: Array<{
				/** @example Query */
				op?: string;
				/** @example [lemma="test"] */
				arg?: string;
				/** @example test */
				nicearg?: string;
				/** @example 202.22 */
				rel?: number;
				/** @example 22685 */
				size?: number;
				/** @example q=q%5Blemma%3D%22test%22%5D */
				tourl?: string;
			}>;
			/** @example 5.63.1 */
			api_version?: string;
			/** @example 2.36.7-SkE-2.223.6 */
			manatee_version?: string;
			/** @description Just summary section of parsed query parameters used in this endpoint call. These parameters are all documented in the beggining of every endpoint box (after you unwrap the endpoint). */
			request?: {
				/** @example m */
				csortfn?: string;
				/** @example preloaded/bnc2 */
				corpname?: string;
				/** @example q[lemma="test"] */
				q?: string;
			};
		};
		"11_freqml": {
			/** @example fcrit=word%2Fe+-1%3C0+lemma%2Fe+-1%3C0 */
			fcrit?: string;
			FCrit?: Array<{
				/** @example word/e -1<0 lemma/e -1<0 */
				fcrit?: string;
			}>;
			Blocks?: Array<{
				Head?: Array<{
					/** @example word */
					n?: string;
					/** @example 0 */
					s?: number;
					/** @example word/e */
					id?: string;
				}>;
				/** @example 1700 */
				total?: number;
				/** @example 12087 */
				totalfrq?: number;
				Items?: Array<{
					Word?: Array<{
						/** @example the */
						n?: string;
					}>;
					/** @example 2621 */
					frq?: number;
					/** @example 0 */
					rel?: number;
					/** @example 0 */
					reltt?: number;
					/** @example 0 */
					norm?: number;
					/** @example 301 */
					fbar?: number;
					/** @example 0 */
					relbar?: number;
					/** @example 0 */
					freqbar?: number;
					/** @example ;q=p-1%3C0+-1%3C0+0+%5Bword%3D%22the%22%5D;q=p-1%3C0+-1%3C0+0+%5Blemma%3D%22the%22%5D */
					pfilter?: string;
					nfilter?: string;
					pfilter_list?: Array<Array<string>>;
					/** @example 21.684454372466284 */
					poc?: number;
					/** @example 23.329771292938062 */
					fpm?: number;
				}>;
			}>;
			/** @example 1 */
			paging?: number;
			/** @example 12087 */
			concsize?: number;
			/** @example 14297 */
			fullsize?: number;
			Desc?: Array<{
				/** @example Query */
				op?: string;
				/** @example [lc="dog" | lemma_lc="dog"] */
				arg?: string;
				/** @example dog */
				nicearg?: string;
				/** @example 107.59 */
				rel?: number;
				/** @example 12087 */
				size?: number;
				/** @example q=q%5Blc%3D%22dog%22+%7C+lemma_lc%3D%22dog%22%5D */
				tourl?: string;
			}>;
			/** @example 0 */
			numofcolls?: number;
			/** @example 1 */
			hitlen?: number;
			/** @example 1000 */
			wllimit?: number;
			/** @example 0 */
			lastpage?: number;
			/** @example true */
			ml?: boolean;
			/** @example 5.63.12 */
			api_version?: string;
			/** @example 2.36.7-SkE-2.223.6 */
			manatee_version?: string;
			/** @description Just summary section of parsed query parameters used in this endpoint call. These parameters are all documented in the beggining of every endpoint box. */
			request?: {
				concordance_query?: Array<{
					/** @example iqueryrow */
					queryselector?: string;
					/** @example dog */
					iquery?: string;
				}>;
				/** @example json */
				format?: string;
				/** @example 1 */
				fpage?: string;
				/** @example 1 */
				showpoc?: string;
				/** @example 2 */
				freqlevel?: string;
				/** @example 1 */
				group?: string;
				/** @example freq */
				freq_sort?: string;
				/** @example -1<0 */
				ml1ctx?: string;
				/** @example 1 */
				showreltt?: string;
				/** @example lemma */
				ml2attr?: string;
				/** @example word */
				ml1attr?: string;
				/** @example -1<0 */
				ml2ctx?: string;
				/** @example 5000 */
				fmaxitems?: string;
				/** @example preloaded/bnc2_tt21 */
				corpname?: string;
				/** @example 1 */
				showrel?: string;
			};
		};
		"12_struct_wordlist": {
			/** @example fcrit=lemma%2Fe+0+word%2Fe+0+lempos%2Fe+0 */
			fcrit?: string;
			FCrit?: Array<{
				/** @example lemma/e 0 word/e 0 lempos/e 0 */
				fcrit?: string;
			}>;
			Blocks?: Array<{
				Head?: Array<{
					/** @example lemma */
					n?: string;
					/** @example 0 */
					s?: number;
					/** @example lemma/e */
					id?: string;
				}>;
				/** @example 77 */
				total?: number;
				/** @example 13931 */
				totalfrq?: number;
				Items?: Array<{
					Word?: Array<{
						/** @example dog */
						n?: string;
					}>;
					/** @example 6829 */
					frq?: number;
					/** @example 0 */
					rel?: number;
					/** @example 0 */
					reltt?: number;
					/** @example 0 */
					norm?: number;
					/** @example 301 */
					fbar?: number;
					/** @example 0 */
					relbar?: number;
					/** @example 0 */
					freqbar?: number;
					/** @example ;q=p0+0+0+%5Blemma%3D%22dog%22%5D;q=p0+0+0+%5Bword%3D%22dog%22%5D;q=p0+0+0+%5Blempos%3D%22dog-n%22%5D */
					pfilter?: string;
					nfilter?: string;
					pfilter_list?: Array<Array<string>>;
					/** @example 47.765265440302166 */
					poc?: number;
					/** @example 60.78558113676994 */
					fpm?: number;
				}>;
			}>;
			/** @example 1 */
			paging?: number;
			/** @example 14297 */
			concsize?: number;
			/** @example 14297 */
			fullsize?: number;
			Desc?: Array<{
				/** @example Query */
				op?: string;
				/** @example [lemma_lc="(dog.*)"] */
				arg?: string;
				/** @example (dog.*) */
				nicearg?: string;
				/** @example 127.26 */
				rel?: number;
				/** @example 14297 */
				size?: number;
				/** @example q=q%5Blemma_lc%3D%22%28dog.%2A%29%22%5D */
				tourl?: string;
			}>;
			/** @example 0 */
			numofcolls?: number;
			/** @example 1 */
			hitlen?: number;
			/** @example 1000 */
			wllimit?: number;
			/** @example 1 */
			lastpage?: number;
			/** @example true */
			ml?: boolean;
			/** @example 5.63.12 */
			api_version?: string;
			/** @example 2.36.7-SkE-2.223.6 */
			manatee_version?: string;
			/** @description Just summary section of parsed query parameters used in this endpoint call. These parameters are all documented in the beggining of every endpoint box. */
			request?: {
				/** @example 0 */
				wlmaxfreq?: string;
				/** @example 1 */
				wlpage?: string;
				/** @example 0 */
				random?: string;
				/** @example lemma */
				wlstruct_attr1?: string;
				/** @example struct_wordlist */
				wltype?: string;
				/** @example 20000 */
				fmaxitems?: string;
				/** @example (dog.*) */
				wlpat?: string;
				/** @example frq */
				wlnums?: string;
				/** @example lemma_lc */
				wlattr?: string;
				/** @example 1 */
				wlicase?: string;
				/** @example 20000 */
				wlmaxitems?: string;
				/** @example lempos */
				wlstruct_attr3?: string;
				/** @example 1 */
				relfreq?: string;
				/** @example 1 */
				include_nonwords?: string;
				/** @example frq */
				wlsort?: string;
				/** @example word */
				wlstruct_attr2?: string;
				/** @example preloaded/bnc2_tt21 */
				corpname?: string;
				/** @example 1 */
				reldocf?: string;
				/** @example 5 */
				wlminfreq?: string;
			};
		};
		"13_freq_distrib": {
			dots?: Array<{
				/** @example 64 */
				frq?: number;
				/** @example 0 */
				pos?: number;
				/** @example 72053 */
				beg?: number;
				/** @example 2475660 */
				end?: number;
			}>;
			/** @example 50 */
			granularity?: number;
			/** @example 5.63.12 */
			api_version?: string;
			/** @example 2.36-7-SkE-2.223.6 */
			manatee_version?: string;
			request?: {
				concordance_query?: Array<{
					/** @example lemmarow */
					queryselector?: string;
					/** @example cat */
					lemma?: string;
					/** @example -n */
					lpos?: string;
					/** @example 0 */
					qmcase?: number;
				}>;
				/** @example s,g */
				structs?: string;
				/** @example all */
				fc_lemword_type?: string;
				/** @example word */
				attrs?: string;
				/** @example {"concordance_query":[{"queryselector":"lemmarow","lemma":"cat","lpos":"-n","qmcase":false}]} */
				json?: string;
				/** @example 50 */
				res?: string;
				/** @example both */
				fc_lemword_window_type?: string;
				/** @example 0 */
				normalize?: string;
				/** @example json */
				format?: string;
				/** @example all */
				attr_allpos?: string;
				/** @example all */
				fc_pos_type?: string;
				/** @example 5 */
				fc_pos_wsize?: string;
				/** @example =bncdoc.alltyp */
				refs?: string;
				/** @example sen */
				viewmode?: string;
				/** @example -n */
				lpos?: string;
				/** @example preloaded/bnc2_tt21 */
				corpname?: string;
				/** @example lemma */
				default_attr?: string;
				/** @example 5 */
				fc_lemword_wsize?: string;
				/** @example both */
				fc_pos_window_type?: string;
			};
		};
		"14_fullref": {
			Refs?: Array<{
				/** @example Token number */
				name?: string;
				/** @example # */
				id?: string;
				/** @example 6270887 */
				val?: string;
			}>;
			/** @example J1C */
			bncdoc_id?: string;
			/** @example ===NONE=== */
			bncdoc_author?: string;
			/** @example ===NONE=== */
			bncdoc_year?: string;
			/** @example [Leeds United e-mail list] */
			bncdoc_title?: string;
			/** @example [Leeds United e-mail list]. Sample containing about 41810 words of unpublished miscellanea (domain: leisure) */
			bncdoc_info?: string;
			/** @example Ownership has not been claimed */
			bncdoc_allava?: string;
			/** @example 1985-1993 */
			bncdoc_alltim?: string;
			/** @example Written miscellaneous */
			bncdoc_alltyp?: string;
			/** @example W_email */
			bncdoc_genre?: string;
			/** @example  */
			u_who?: string;
			/** @example ===NONE=== */
			s_audio?: string;
			/** @example 5.63.12 */
			api_version?: string;
			/** @example 2.36.7-SkE-2.223.6 */
			manatee_version?: string;
			request?: {
				/** @example preloaded/bnc2_tt21 */
				corpname?: string;
				/** @example 6270887 */
				pos?: string;
			};
		};
		"15_textypes_with_norms": {
			Blocks?: Array<{
				Line?: Array<{
					/** @example bncdoc.alltyp */
					name?: string;
					/** @example Text type */
					label?: string;
					/** @example  */
					attr_doc?: string;
					/** @example  */
					attr_doc_label?: string;
					Values?: Array<{
						/** @example Spoken context-governed */
						v?: string;
						/** @example 757 */
						xcnt?: string;
					}>;
				}>;
			}>;
			Normlist?: Array<{
				/** @example freq */
				n?: string;
				/** @example Document counts */
				label?: string;
			}>;
			/** @example 5.63.12 */
			api_version?: string;
			/** @example 2.36.7-SkE-2.223.6 */
			manatee_version?: string;
			request?: {
				/** @example preloaded/bnc2_tt21 */
				corpname?: string;
			};
		};
		"16_widectx": {
			/** @example <p> */
			wrapdetail?: string;
			/** @example true */
			deletewrap?: boolean;
			content?: Array<{
				/** @example , in Hungarian also means "majority of", "its belongings", "its goods", "its best portion", a type of pork, and may also be incorrectly identified as an agglutination of a frequent abbreviation in mailing lists. */
				str?: string;
				/** @example  */
				class?: string;
			}>;
			/** @example pos=831745;detail_left_ctx=110;detail_right_ctx=50 */
			leftlink?: string;
			/** @example pos=831745;detail_left_ctx=50;detail_right_ctx=110 */
			rightlink?: string;
			/** @example 831745 */
			pos?: number;
			/** @example 200 */
			maxcontent?: number;
			/** @example 5.63.12 */
			api_version?: string;
			/** @example 2.36.7-SkE-2.223.6 */
			manatee_version?: string;
			request?: {
				/** @example 50 */
				detail_left_ctx?: string;
				/** @example preloaded/ententen21_tt31 */
				corpname?: string;
				/** @example 1 */
				hitlen?: string;
				/** @example 831745 */
				pos?: string;
				/** @example 50 */
				detail_right_ctx?: string;
				/** @example s,g */
				structs?: string;
			};
		};
		"17_subcorpus_rename": {
			/** @example OK */
			status?: string;
			/** @example preloaded/bnc2_tt31 */
			corpus?: string;
			subcorp_id2name?: {
				/** @example test_2 */
				test?: string;
			};
			/** @example 5.66.5 */
			api_version?: string;
			/** @example 2.36.7-SkE-2.225.6 */
			manatee_version?: string;
			request?: {
				/** @example test */
				subcorp_id?: string;
				/** @example test_2 */
				new_subcorp_name?: string;
				/** @example preloaded/bnc2_tt31 */
				corpname?: string;
			};
		};
		"18_subcorp_info": {
			/** @example test_4 */
			subcorp?: string;
			/** @example 112338376 */
			corpsize?: number;
			/** @example 112338376 */
			subcsize?: number;
			/** @example 5.66.5 */
			api_version?: string;
			/** @example 2.36.7-SkE-2.225.6 */
			manatee_version?: string;
			request?: {
				/** @example test_2 */
				subcname?: string;
				/** @example preloaded/bnc2_tt31 */
				corpname?: string;
			};
		};
		"19_freqdist": {
			/** @example the */
			lc?: string;
			freqdist?: {
				/** @description The name is variable according selected period (wlattr). */
				"2021-11"?: {
					/** @example 679638 */
					frq?: number;
					/**
					 * Format: float
					 * @example 3022.91023457566
					 */
					rel_frq?: number;
					/**
					 * Format: float
					 * @example 224829038
					 */
					period_size?: number;
				};
			};
			removed_freqdist?: {
				/** @description The name is variable according selected period (wlattr). */
				"2023-08"?: {
					/** @example 0 */
					frq?: number;
					/**
					 * Format: float
					 * @example 0
					 */
					rel_frq?: number;
					/**
					 * Format: float
					 * @example 429451774
					 */
					period_size?: number;
				};
			};
			/**
			 * Format: float
			 * @example 136780187.25
			 */
			average_norm?: number;
			/**
			 * Format: float
			 * @example 6839009.362500001
			 */
			norm_limit?: number;
		};
		/** @description Request for post method to set `name`, `language`, `tagset`, and additional information to the corpus. */
		"01_corpora_request": {
			/**
			 * @description The additional information for a newly created corpus.
			 * @example Example description of user corpus.
			 */
			info?: string;
			/**
			 * @description Language iso-code. `ISO 639-1`.
			 * @example en
			 */
			language_id?: string;
			/**
			 * @description Unique `corpus name` for a newly created corpus.
			 * @example Example corpus
			 */
			name?: string;
			/**
			 * @description Name of used tagset.
			 * @example TT_ENG_V3
			 */
			tagset_id?: string;
		};
		"02_compile_request": {
			/**
			 * @description `Structures` and `structure attributes` in corpus which should be compiled. Usually: `all`.
			 * @example all
			 */
			structures?: string;
		};
		"03_corpus_ids": {
			/**
			 * @example [
			 *       842464,
			 *       842463
			 *     ]
			 */
			corpus_ids?: Array<number>;
		};
		"04_align_req": {
			/**
			 * @description According to which structure the document should be aligned. Usually, `/<s>`.
			 * @example s
			 */
			alignstruct?: string;
			/**
			 * @description True, when documents are not compiled. Sketch Engine will align them automatically.
			 * @example true
			 */
			auto?: boolean;
			/**
			 * @example [
			 *       842464,
			 *       842463
			 *     ]
			 */
			corpus_ids?: Array<number>;
		};
		/** @description In this documentation, an empty request is used mostly used with the `RPC style` method where the content in a request is not needed (in most cases). RPC style endpoints focus on `performing` one action right (procedures, command) easier than REST API-based endpoints. It is not as scalable as REST API style. RPC is mostly used with HTTP: GET (to fetch information) and POST (to everything else) in CA api is it used with POST HTTP method. */
		"05_empty_request": Record<string, never>;
		/** @description All possible paramaters that can be changed in user corpus. In corpus update `you don't have to use all parameters`, just the parameters you change. */
		"07_corpus_update": {
			/**
			 * @description Set to `True` if you are hard-core.
			 * @example false
			 */
			expert_mode?: boolean;
			/**
			 * @description Corpus name. `Given by user`.
			 * @example Example corpus 2
			 */
			name?: string;
			/**
			 * @description Additional info about corpus.
			 * @example Example description of user corpus 2
			 */
			info?: string;
			/** @description Can be set to enforce document order within the corpus. */
			document_order?: Array<number>;
			/** @example true */
			lang_filter?: boolean;
			/** @description Available structures or tags in the corpus. Structures like `s` (sentence), `g` (glue), `doc` (document). */
			structures?: Array<{
				/** @description Structure name. Example: `s` */
				name?: string;
				/** @description A list of used attributes in corpus. */
				attributes?: Array<{
					/** @description The name of used attribute. */
					name?: string;
				}>;
			}>;
			/**
			 * @description The structure in which individual documents should be wrapped. Usually `doc`.
			 * @example doc
			 */
			file_structure?: string;
			/**
			 * @description The structure for deduplication. Usually `p` (paragraph), `doc` or `Null` (no deduplication).'
			 * @example doc
			 */
			onion_structure?: string;
			/**
			 * @description Structure in which individual documents should be wrapped. Usually `doc`.
			 * @example doc
			 */
			docstructure?: string;
			/**
			 * @description `Sketch grammar ID` (name of sketch grammar file). For sketch grammars querying. Sketch grammar is a series of rules written in the CQL query language that search for collocations in a text corpus and categorize them according to their grammatical relations. Example: `preloaded/english-penn_tt-3.3.wsdef.m4`.
			 * @example preloaded/english-penn_tt-3.3.wsdef.m4
			 */
			sketch_grammar_id?: string;
			/**
			 * @description `Term grammar ID` (name of term grammar file). Term grammar tells Sketch Engine which words and phrases should indentify as terms. Example: `/corpora/wsdef/english-penn_tt-terms-3.1.termdef.m4`.
			 * @example preloaded/english-penn_tt-terms-3.1.termdef.m4
			 */
			term_grammar_id?: string;
		};
		"09_doc_put_req": {
			/** @description Name of documents. */
			filename_display?: string;
			/** @description Unique numeric `document ID` to identify individual documents. */
			id?: number;
			/** @description Represents whether the currently edited document is in use. */
			inProgress?: boolean;
			/** @description Represents if the updated document is in a format like .zip (created via some archive manager). */
			isArchive?: boolean;
			/** @description Metadata of document. For example, additional `attributes and values`. */
			metadata?: Record<string, never>;
			/** @description Parameters for plaintext extraction. */
			parameters?: {
				/** @description Encoding standard of the document. Usually, `UTF-8`. */
				encoding?: string;
				/** @description Represent the list of unimportant words, in a specified language, from an NLP point of view. */
				justext_stoplist?: string;
				permutation?: Array<number>;
				/** @description TMX (translation memory exchange). Language of document used for parallel corpus creation. */
				tmx_lang?: string;
				/** @description Alignment structure to be used for multilingual documents, `align` is the most used structure. Used within segment distinction, which sentence is in which language and to put sentences with the same meaning into one segment. */
				tmx_struct?: string;
				/** @description Placeholder for empty segments in multilingual documents. The segments which have no counterpart in a second language of parallel corpus. */
				tmx_untranslated?: string;
				/** @description File format (.csv, .doc, .docx, .htm, .html etc.). */
				type?: string;
				/** @description Convert `all-caps` text to `normal case`. */
				unlegalese?: boolean;
			};
			/** @description Is document temporary or not. */
			temporary?: boolean;
			/** @description Total number of `words` (tokens minus punctuation etc.) in document. */
			word_count?: number;
			/** @description Progress of `vertical file` creation. */
			vertical_progress?: number;
			/** @description An error occured while creating the vertical file. If the creation was succesfull the value is `Null`. */
			vertical_error?: string;
		};
		"10_doc_preview": {
			/** @description Automatically insert paragraph breaks (`\<p>`) in place of blank lines. */
			auto_paragraphs?: string;
			/** @description Encoding standard of the document. Usually `UTF-8`. */
			encoding?: string;
			/** @description Represent the list of unimportant words, in a specified language, from an NLP point of view. */
			justext_stoplist?: string;
			/** @description Changing the order of columns (applies only to `type=vert`). */
			permutation?: Array<number>;
			/** @description TMX (translation memory exchange). Language of document used for parallel corpus creation. */
			tmx_lang?: string;
			/** @description Alignment structure to be used for multilingual documents, `align` is the most used structure. Used within segment distinction, which sentence is in which language and to put sentences with the same meaning into one segment. */
			tmx_struct?: string;
			/** @description Placeholder for empty segments in multilingual documents. The segments which have no counterpart in a second language of parallel corpus. */
			tmx_untranslated?: string;
			/** @description File format (`.csv`, `.doc`, `.docx`, `.htm`, `.html` etc.). */
			type?: string;
			/** @description Convert `all-caps` text to `normal case`. */
			unlegalese?: boolean;
		};
		"11_doc_metadata": Array<{
			/** @description Unique numeric `document ID`. */
			id?: number;
			/** @description Pairs of `attribute_name`:`value`. */
			metadata?: Record<string, never>;
		}>;
		"13_filesets_creation": {
			/** @description Stands for: `blacklist max total keyword`. Means that web page or document will be discarded if it contains more words from the denylist (blacklist) than this limit. */
			bl_max_total_kw?: number;
			/** @description Stands for: `blacklist max unique keyword`. Means that web page or document will be discarded if it contains more unique words from the denylist (blacklist) than this limit. */
			bl_max_unique_kw?: number;
			/** @description A list (separated by whitespaces) of `blocked words`, words you don't want to see in your future corpus. */
			black_list?: string;
			/** @description Input types the web-crawler will works with. Example: `urls` */
			input_type?: string;
			/** @description Web pages and documents with a size `over` this limit (`in kB`) will be ignored. */
			max_cleaned_file_size?: number;
			/** @description Web pages and documents with a size `over` this limit (`in kB`) will be ignored. */
			max_file_size?: number;
			/** @description Web pages and documents `smaller` than this limit (`in kB`) after cleaning will be ignored. Cleaning involves conversion to plain text, removing boilerplate text (e.g. navigation menus, legal text, disclaimers and other repetitive content). */
			min_cleaned_file_size?: number;
			/** @description Web pages and documents with a `size below` this limit (`in kB`) will be ignored. */
			min_file_size?: number;
			/** @description Texts will be organized into a corpus folder `with this name`. */
			name?: number;
			/** @description A list of words according to which the `URLs` were chosen to be searched. */
			seed_words?: Array<string>;
			/** @description A list (separated by whitespaces) of `allowed words`, words you want to see in your future corpus. */
			white_list?: string;
			/** @description Stands for: `whitelist minimal keywords ratio`. Means that web page or document will be included only if the `percentage` of allowlist words compared to total words is `higher` than this limit. */
			wl_min_kw_ratio?: number;
			/** @description Stands for: `whitelist minimal total keywords`. Means that web page or document will be included only if it contains `more words` from the `allowlist` (whitelist) than this limit. */
			wl_min_total_kw?: number;
			/** @description Stands for: `whitelist minimal unique keywords`. Means that a web page or document will be included only if it contains `more words` from the `allowlist` (whitelist) than this limit. */
			wl_min_unique_kw?: number;
		};
		"19_somefiles_put": {
			corpora?: {
				guessed_language_code?: {
					/** @description Language iso-code. `ISO 639-1`. */
					language_id?: string;
					/** @description Language name in `English`. */
					name?: string;
				};
			};
		};
		"03_corpora_list": {
			/** @description Unique numeric `corpus ID` for corpus building. */
			id?: number;
			/** @description Unique numeric `owner ID` (usually you). */
			owner_id?: number;
			/** @description Corpus `owner name` (usually you). */
			owner_name?: string;
			/** @description Unique `corpus name` for corpus querying. */
			corpname?: string;
			/** @description Language iso-code. `ISO 639-1`. */
			language_id?: string;
			/** @description Language name in `English`. */
			language_name?: string;
			/** @description `Tagset ID`. Tagset is list of part-of-speech tags (POS tags) for specified language. They are `preselected` to the most relevant one and can be changed only in user corpora. `Tagsets` can be refered also as `templates`. */
			tagset_id?: number;
			/** @description `Sketch grammar ID`. Sketch grammar is a series of rules written in the CQL query language that search for collocations in a text corpus and categorize them according to their grammatical relations. */
			sketch_grammar_id?: string;
			/** @description `Term grammar ID`. Term grammar tells Sketch Engine which words and phrases should indentify as terms. */
			term_grammar_id?: string;
			/** @description Corpus sizes. `Null` if corpus is not compiled. */
			sizes?: {
				/** @description Total number of `documents` in corpus. */
				doccount?: number;
				/** @description Total number of `paragraphs` in corpus. */
				parcount?: number;
				/** @description Total number of `sentences` in corpus. */
				sentcount?: number;
				/** @description Total number of `words` (tokens minus punctuation etc.) in corpus. */
				wordcount?: number;
				/** @description Total number of `tokens` in corpus. */
				tokencount?: number;
			};
			/** @description Date and time of corpus creation in format `YYYY-MM-DD HH:MM:SS`. */
			created?: string;
			/** @description `True` if corpus documents have been altered since last compilation. */
			needs_recompiling?: boolean;
			/** @description Corpus can be queried a `specific user`. Ignore all corpora where this is false. */
			user_can_read?: boolean;
			/** @description Corpus can be used as a `reference corpus` even by anonymous users. */
			user_can_refer?: boolean;
			/** @description Corpus is owned by you or shared with you. You can upload documents to it. */
			user_can_upload?: boolean;
			/** @description Corpus is owned by you or shared with you with `full privileges`. */
			user_can_manage?: boolean;
			/** @description True if corpus is shared with other users. */
			is_shared?: boolean;
			/** @description If set, the old corpus is deprecated in favor of a new one. */
			new_version?: string;
			/** @description Corpus name. `Given by user.` */
			name?: string;
			/** @description Additional info about corpus. */
			info?: string;
			/** @description List of other corpora (corpus ID) within the `same` multi-lingual set (parallel corpus). */
			aligned?: Array<string>;
			/** @description Structure in which individual documents should be wrapped. Usually `doc`. */
			docstructure?: string;
		};
		"04_corpora_single": {
			/** @description Unique numeric `corpus ID` for corpus building. */
			id?: number;
			/** @description Unique numeric `owner ID` (usually you). */
			owner_id?: number;
			/** @description Corpus `owner name` (usually you). */
			owner_name?: string;
			/** @description Unique `corpus name` for corpus querying. */
			corpname?: string;
			/** @description Language iso-code. `ISO 639-1`. */
			language_id?: string;
			/** @description Language name in `English`. */
			language_name?: string;
			/** @description `Sketch grammar ID` (name of sketch grammar file). Sketch grammar is a series of rules written in the CQL query language that search for collocations in a text corpus and categorize them according to their grammatical relations. Example: `preloaded/english-penn_tt-3.3.wsdef.m4`. */
			sketch_grammar_id?: string;
			/** @description `Term grammar ID` (name of term grammar file). Term grammar tells Sketch Engine which words and phrases should indentify as terms. Example: `/corpora/wsdef/english-penn_tt-terms-3.1.termdef.m4`. */
			term_grammar_id?: string;
			/** @description Corpus sizes. `Null` if corpus is not compiled. */
			sizes?: {
				/** @description Total number of `documents` in corpus. */
				doccount?: number;
				/** @description Total number of `paragraphs` in corpus. */
				parcount?: number;
				/** @description Total number of `sentences` in corpus. */
				sentcount?: number;
				/** @description Total number of `words` (tokens minus punctuation etc.) in corpus. */
				wordcount?: number;
				/** @description Total number of `tokens` in corpus. */
				tokencount?: number;
			};
			/** @description Date and time of corpus creation in format `YYYY-MM-DD HH:MM:SS`. */
			created?: string;
			/** @description True if corpus documents have been altered since last compilation. */
			needs_recompiling?: boolean;
			/** @description Corpus can be queried by a `specific user`. Ignore all corpora where this is false. */
			user_can_read?: boolean;
			/** @description Corpus can be used as a `reference corpus` even by anonymous users. */
			user_can_refer?: boolean;
			/** @description Corpus is owned by you or shared with and you can upload documents to it. */
			user_can_upload?: boolean;
			/** @description Corpus is owned by you or shared with you with `full privileges`. */
			user_can_manage?: boolean;
			/** @description `True` if corpus is shared with other users. */
			is_shared?: boolean;
			/** @description If set, the old corpus is deprecated in favor of a new one. */
			new_version?: string;
			/** @description Corpus name. `Given by user`. */
			name?: string;
			/** @description Additional info about corpus. */
			info?: string;
			/** @description Other corpora within the `same` multi-lingual set (parallel corpus). */
			aligned?: Array<string>;
			/** @description Structure in which individual documents should be wrapped. Usually `doc`. */
			docstructure?: string;
			/** @description Current state of corpus. */
			is_error_corpus?: boolean;
			/** @description Attributes appearing in corpus documents. Attributes like: `word`, `tag`, `lempos`, `pos`, `lemma`, etc. */
			attrlist?: Array<string>;
			/** @description Tagset ID. Tagset is list of part-of-speech tags (POS tags) for specified language. They are `preselected` to the most relevant one and can be changed only in user corpora. The terms `tagset` and `templates` are interchangeable. */
			tagset_id?: number;
			/** @description Default reference corpus for `keyword extraction`. */
			reference_corpus?: string;
			/** @description Compilation status: `0` if not compiled, `100` if compiled successfully, `-1` if failed, otherwise in progress. */
			progress?: number;
			/** @description Informs about last compilation error, if any error `None`. */
			error?: string;
			/** @description The amount of documents the corpus was build from. */
			document_count?: number;
			/** @description `True` if corpus template is outdated and can be upgraded. The terms `tagset` and `templates` are interchangeable. */
			can_be_upgraded?: boolean;
			/** @description All `structures`/`attributes` that appear in corpus documents. */
			available_structures?: Array<{
				/** @description Structure name. */
				name?: string;
				/** @description Frequency of structure. */
				freq?: number;
				attributes?: Array<string>;
			}>;
			/** @description The structure in which individual documents should be wrapped. Usually `doc`. */
			file_structure?: string;
			/** @description The structure for deduplication. Usually `p` (paragraph) or `Null` (no deduplication).' */
			onion_structure?: string;
			/** @description Set to `True` if you are hard-core. */
			expert_mode?: boolean;
			/** @description Not mandatory. Can be set to enforce document order within the corpus. */
			document_order?: Array<number>;
			/** @description Use `all` structures available in corpus. */
			use_all_structure?: boolean;
			/** @description Available `structures` or `tags` in the corpus. Structures like `s` (sentence), `g` (glue), `doc` (document). */
			structures?: Array<{
				/** @description Structure name. */
				name?: string;
				/** @description A list of used attributes in corpus. */
				attributes?: Array<{
					/** @description The name of used attribute. */
					name?: string;
				}>;
			}>;
		};
		"05_can_be_compiled": {
			result?: {
				/** @description True, if the corpus does not contain any potential error, which can break compilation. */
				can_be_compiled?: boolean;
				/** @description Description of problem why it cannot be compiled. If none Null. Example: `QUOTA_EXCEEDED` or `EMPTY`. */
				reason?: string;
			};
			/** @description Unexpected server error. If none Null. */
			error?: string;
		};
		"06_get_progress": {
			result?: {
				/** @description Compilation status: `0` if not compiled, `100` if compiled successfully, `-1` if failed, otherwise in progress. */
				progress?: number;
				/**
				 * @description Problem description. If none Null.
				 * @example null
				 */
				error?: string;
			};
			/**
			 * @description Unexpected server error. If none Null.
			 * @example null
			 */
			error?: string;
		};
		"12_corpora_single_full": {
			/** @description Unique numeric `corpus ID` for corpus building. */
			id?: number;
			/** @description Unique numeric `owner ID` (usually you). */
			owner_id?: number;
			/** @description Corpus `owner name` (usually you). */
			owner_name?: string;
			/** @description Unique `corpus name` for corpus querying. */
			corpname?: string;
			/** @description Language iso-code. `ISO 639-1`. */
			language_id?: string;
			/** @description Language name in `English`. */
			language_name?: string;
			/** @description `Sketch grammar ID` (name of sketch grammar file). Sketch grammar is a series of rules written in the CQL query language that search for collocations in a text corpus and categorize them according to their grammatical relations. Example: `preloaded/english-penn_tt-3.3.wsdef.m4`. */
			sketch_grammar_id?: string;
			/** @description `Term grammar ID` (name of term grammar file). Term grammar tells Sketch Engine which words and phrases should indentify as terms. Example: `/corpora/wsdef/english-penn_tt-terms-3.1.termdef.m4`. */
			term_grammar_id?: string;
			/** @description Corpus sizes. `Null` if corpus is not compiled. */
			sizes?: {
				/** @description Total number of `documents` in corpus. */
				doccount?: number;
				/** @description Total number of `paragraphs` in corpus. */
				parcount?: number;
				/** @description Total number of `sentences` in corpus. */
				sentcount?: number;
				/** @description Total number of `words` (tokens minus punctuation etc.) in corpus. */
				wordcount?: number;
				/** @description Total number of `tokens` in corpus. */
				tokencount?: number;
			};
			/** @description TODO */
			is_sgdev?: boolean;
			/** @description TOOD */
			is_featured?: boolean;
			/** @description TODO */
			access_level?: boolean;
			/** @description TODO */
			access_on_demand?: boolean;
			/** @description TODO */
			terms_of_use?: string;
			/** @description TODO */
			sort_to_end?: boolean;
			tags?: Array<string>;
			/** @description Date and time of corpus creation in format `YYYY-MM-DD HH:MM:SS`. */
			created?: string;
			/** @description True if corpus documents have been altered since last compilation. */
			needs_recompiling?: boolean;
			/** @description Corpus can be queried by a `specific user`. Ignore all corpora where this is false. */
			user_can_read?: boolean;
			/** @description Corpus can be used as a `reference corpus` even by anonymous users. */
			user_can_refer?: boolean;
			/** @description Corpus is owned by you or shared with and you can upload documents to it. */
			user_can_upload?: boolean;
			/** @description Corpus is owned by you or shared with you with `full privileges`. */
			user_can_manage?: boolean;
			/** @description `True` if corpus is shared with other users. */
			is_shared?: boolean;
			/** @description If set, the old corpus is deprecated in favor of a new one. */
			new_version?: string;
			/** @description Corpus name. `Given by user`. */
			name?: string;
			/** @description Additional info about corpus. */
			info?: string;
			/** @description Default word sketch definition. Example: `/corpora/wsdef/serbian-multext-rft1-1.0.wsdef.txt`. */
			wsdef?: string;
			/** @description Default term definition. */
			termdef?: string;
			/** @description Is this corpus developing over time to keep track in vocabulary changes, grammar and language usage. If yes what time period does the corpus cover. */
			diachronic?: string;
			/** @description Other corpora within the `same` multi-lingual set (parallel corpus). */
			aligned?: Array<string>;
			/** @description Structure in which individual documents should be wrapped. Usually `doc`. */
			docstructure?: string;
			/** @description Current state of corpus. */
			is_error_corpus?: boolean;
			/** @description Attributes appearing in corpus documents. Attributes like: `word`, `tag`, `lempos`, `pos`, `lemma`, etc. */
			attrlist?: Array<string>;
			/** @description Tagset ID. Tagset is list of part-of-speech tags (POS tags) for specified language. They are `preselected` to the most relevant one and can be changed only in user corpora. The terms `tagset` and `templates` are interchangeable. */
			tagset_id?: number;
			/** @description Default reference corpus for `keyword extraction`. */
			reference_corpus?: string;
			/** @description Compilation status: `0` if not compiled, `100` if compiled successfully, `-1` if failed, otherwise in progress. */
			progress?: number;
			/** @description Informs about last compilation error, if any error `None`. */
			error?: string;
			/** @description The amount of documents the corpus was build from. */
			document_count?: number;
			/** @description `True` if corpus template is outdated and can be upgraded. The terms `tagset` and `templates` are interchangeable. */
			can_be_upgraded?: boolean;
			/** @description All `structures`/`attributes` that appear in corpus documents. */
			available_structures?: Array<{
				/** @description Structure name. */
				name?: string;
				/** @description Frequency of structure. */
				freq?: number;
				attributes?: Array<string>;
			}>;
			/** @description The structure in which individual documents should be wrapped. Usually `doc`. */
			file_structure?: string;
			/** @description The structure for deduplication. Usually `p` (paragraph) or `Null` (no deduplication).' */
			onion_structure?: string;
			/** @description Set to `True` if you are hard-core. */
			expert_mode?: boolean;
			/** @description Not mandatory. Can be set to enforce document order within the corpus. */
			document_order?: Array<number>;
			/** @description Use `all` structures available in corpus. */
			use_all_structure?: boolean;
			/** @description Available `structures` or `tags` in the corpus. Structures like `s` (sentence), `g` (glue), `doc` (document). */
			structures?: Array<{
				/** @description Structure name. */
				name?: string;
				/** @description A list of used attributes in corpus. */
				attributes?: Array<{
					/** @description The name of used attribute. */
					name?: string;
				}>;
			}>;
		};
		"13_documents_get": Array<{
			/** @description Unique numeric `document ID` to identify individual documents from which the corpus was created. */
			id?: number;
			/** @description The name of the document. */
			filename_display?: string;
			/** @description Parameters for plaintext extraction. */
			parameters?: {
				/** @description File format. Possible formats: `.csv`, `.doc`, `.docx`, `.htm`, `.html` etc.. */
				type?: string;
				/** @description Encoding standard of the document. Usually `UTF-8`. */
				encoding?: string;
				/** @description TMX (translation memory exchange). Language of document used for parallel corpus creation. */
				tmx_lang?: string;
				/** @description Alignment structure to be used for multilingual documents, `align` is the most used structure. Used within segment distinction, which sentence is in which language and to put sentences with the same meaning into one segment. */
				tmx_struct?: string;
				/** @description Convert `all-caps` text to `normal case`. */
				unlegalese?: boolean;
				permutation?: Array<number>;
				/** @description Automatically insert paragraph breaks (\<p>) in place of blank lines. */
				auto_paragraphs?: string;
				/** @description Represent the list of unimportant words, in a specified language, from an NLP point of view. */
				justext_stoplist?: string;
				/** @description Placeholder for empty segments in multilingual documents. The segments which have no counterpart in a second language of parallel corpus. */
				tmx_untranslated?: string;
			};
			/** @description Is document temporary or not. */
			temporary?: boolean;
			/** @description Total number of `words` (tokens minus punctuation etc.) in document. */
			word_count?: number;
			/** @description Progress of `vertical file` creation. */
			vertical_progress?: number;
			/** @description An error occured while creating the vertical file. If the creation was succesfull the value is `Null`. */
			vertical_error?: string;
			/** @description Metadata of document. For example, additional `attributes and values`. */
			metadata?: Record<string, never>;
		}>;
		"14_documents_post": Array<{
			/** @description Unique numeric `document ID` to identify individual documents from which the corpus was created. */
			id?: number;
			/** @description The name of the document. */
			filename_display?: string;
			/** @description Parameters for plaintext extraction. */
			parameters?: {
				/** @description File format. Possible formats: `.csv`, `.doc`, `.docx`, `.htm`, `.html` etc.. */
				type?: string;
				/** @description Alignment structure to be used for multilingual documents, `align` is the most used structure. Used within segment distinction, which sentence is in which language and to put sentences with the same meaning into one segment. */
				tmx_struct?: string;
				/** @description Placeholder for empty segments in multilingual documents. The segments which have no counterpart in a second language of parallel corpus. */
				tmx_untranslated?: string;
				/** @description Convert `all-caps` text to `normal case`. */
				unlegalese?: boolean;
				/** @description Represent the list of unimportant words, in a specified language, from an NLP point of view. */
				justext_stoplist?: string;
				/** @description TMX (translation memory exchange). Language of document used for parallel corpus creation. */
				tmx_lang?: string;
				permutation?: Array<number>;
			};
			/** @description Is document temporary or not. */
			temporary?: boolean;
			/** @description Total number of `words` (tokens minus punctuation etc.) in document. */
			word_count?: number;
			/** @description Progress of `vertical file` creation. */
			vertical_progress?: number;
			/** @description An error occured while creating the vertical file. If the creation was succesfull the value is `Null`. */
			vertical_error?: string;
			/** @description Metadata of document. For example, additional `attributes and values`. */
			metadata?: Record<string, never>;
		}>;
		"15_doc_preview": {
			/** @description Showcase of few lines from the file the corpus was created from (1kB). */
			result?: string;
			/** @description Unexpected server error. If none Null. */
			error?: string;
		};
		"16_rpc_expand_archive": {
			/** @description Returns fileset ID. */
			result?: number;
			/** @description Unexpected server error. If none Null. */
			error?: string;
		};
		"57_not_found_404": {
			/** @example PreloadedCorpus/UserCorpus/Document/Tagset/Grammar/GdexConf matching query does not exist. */
			error?: string;
		};
		"45_not_found_RPC": {
			/** @description Result of succesfully finished request otherwise Null. */
			result?: boolean;
			/** @example PreloadedCorpus/UserCorpus/Document/Tagset/Grammar/GdexConf/SiteLicence matching query does not exist. */
			error?: string;
		};
		"17_bad_request_RPC_1": {
			/**
			 * @description Result of succesfully finished request otherwise Null.
			 * @example null
			 */
			result?: boolean;
			/** @description Examples: `READ_ONLY`, `INVALID_CORPUS_IDS`. You do not have permissions for it or you inputted IDs are not correct. */
			error?: string;
		};
		"28_bad_request_RPC_8": {
			/** @description Result of succesfully finished request otherwise Null. */
			result?: boolean;
			/** @description Examples: `QUOTA_EXCEEDED`, `READ_ONLY`, `INVALID_CORPUS_IDS`, `CORPUS_BUSY`. */
			error?: string;
		};
		"29_bad_request_RPC_9": {
			/** @description Result of succesfully finished request otherwise Null. */
			result?: boolean;
			/** @description Examples: `QUOTA_EXCEEDED`, `READ_ONLY`. */
			error?: string;
		};
		"30_bad_request_10": {
			/** @description Examples: `QUOTA_EXCEEDED`, `READ_ONLY`, `CORPUS_BUSY`, `DAILY_TAGGING_EXCEEDED`, `INVALID_URL`, `NO_DATA`. */
			error?: string;
		};
		"31_bad_request_11": {
			/** @description Examples: `READ_ONLY`, `CORPUS_BUSY`, `INVALID_METADATA`. */
			error?: string;
		};
		"32_bad_request_12": {
			/** @description Examples: `READ_ONLY`, `CORPUS_BUSY`. */
			error?: string;
		};
		"33_bad_request_13": {
			/** @description Examples: `READ_ONLY`. */
			error?: string;
		};
		"34_bad_request_14": {
			/** @description Examples: `READ_ONLY`, `QUOTA_EXCEEDED`, `DAILY_TAGGING_EXCEEDED`, `CORPUS_BUSY`. */
			error?: string;
		};
		"40_bad_request_20": {
			/** @description Examples: `READ_ONLY`, `QUOTA_EXCEEDED`, `DAILY_TAGGING_EXCEEDED`, `NO_DATA`. */
			error?: string;
		};
		"27_forbidden_normal": {
			/** @description Example: `Permission denied`. You do not have required permissions for specified corpus, document, template or other stuff. Permissions like (read, manage, edit, delete, superuser, etc.). */
			error?: string;
		};
		"21_forbidden": {
			/** @description Result of succesfully finished request otherwise Null. */
			result?: boolean;
			/** @description Example: `Permission denied`. You do not have required permissions for specified corpus, document, template or other stuff. Permissions like (read, manage, edit, delete, superuser, etc.). */
			error?: string;
		};
		"20_unauthorized": {
			/** @description Exampple: `Unauthorized`. You need to authorize first, use API key from Sketch Engine. */
			error?: string;
		};
		"23_unauthorized_rpc": {
			/** @description Result of succesfully finished request otherwise Null. */
			result?: boolean;
			/** @description Example: `Unauthorized`. You need to authorize first, use API key from Sketch Engine. */
			error?: string;
		};
		"58_fileset": {
			/** @description Fileset creation status: `0` if not started, `100` if finished succesfully, -1 if failed, otherwise in progress. Example: downloading content for corpus creation from the Internet. */
			progress?: number;
			/** @description Duration of action with filesets (in seconds). */
			time_elapsed?: number;
			/** @description Description of problem. If none Null. */
			error?: string;
			/** @description Fileset ID. */
			id?: number;
			/** @description Fileset name. `Given by user (except the main one with ID = 0).` */
			name?: string;
			/** @description Total number of `words` (tokens minus punctuation etc.) in document. */
			word_count?: number;
			web_crawl?: {
				/** @description `Source URL` from where the words were downloaded/extracted: website, documents... */
				input_type?: string;
				seed_words?: Array<string>;
				urls?: Array<string>;
				site?: Array<string>;
				/** @description The amount of data `downloaded` by a web-crawler to create corpus. */
				data_downloaded?: number;
				/** @description Counter of files found by web-crawler during crawling, `waiting` to be processed. */
				remaining_files_count?: number;
				/** @description Counter of `already processed` files. */
				processed_files_count?: number;
				/** @description Counter of files which `cannot` be processed because `invalid content type`, `size`, `duplication` etc.. */
				unprocessed_files_count?: number;
				/** @description Counter of files containing content like `navigation links`, `advertisement`, `headers`, `footers` etc.. */
				invalid_content_types_count?: number;
				/** @description Counter for files whose format cannot be converted to one of the supported formats. */
				unable_to_convert_count?: number;
				/** @description Counter for files with repeating content. */
				duplicate_count?: number;
				/** @description Duration of words gathering with web-crawler (in seconds). */
				time_elapsed?: number;
				/** @description Average time to process single file (in seconds). */
				average_file_processing_time?: number;
			};
		};
		"59_filesets_get_progress": {
			result?: {
				/** @description Fileset creation status: `0` if not started, `100` if finished succesfully, -1 if failed, otherwise in progress. Example: downloading content for corpus creation from the Internet. */
				progress?: number;
				/**
				 * Format: float
				 * @description Duration of action with filesets (in second).
				 */
				time_elapsed?: number;
				/** @description Description of problem why it cannot be done. */
				error?: string;
				/** @description Amount of words(tokens minus punctuation etc.) downloaded by web-crawler. */
				word_count?: number;
			};
			/** @description Unexpected server error. */
			error?: string;
		};
		"61_fileset_creation": {
			/** @description Fileset creation status: `0` if not started, `100` if finished succesfully, -1 if failed, otherwise in progress. Example: downloading content for corpus creation from the Internet. */
			progress?: number;
			/** @description Duration of action with filesets (in seconds). */
			time_elapsed?: number;
			/** @description Description of problem. If none Null. */
			error?: string;
			/** @description Fileset ID. */
			id?: number;
			/** @description Fileset name. `Given by user (except the main one with ID = 0).` */
			name?: string;
			/** @description Total number of `words` (tokens minus punctuation etc.) in document. */
			word_count?: number;
			web_crawl?: {
				/** @description `Source URL` from where the words were downloaded/extracted: website, documents... */
				input_type?: string;
				seed_words?: Array<string>;
				urls?: Array<string>;
				site?: Array<string>;
				/** @description The amount of data `downloaded` by a web-crawler to create corpus. */
				data_downloaded?: number;
				/** @description Counter of files found by web-crawler during crawling, `waiting` to be processed. */
				remaining_files_count?: number;
				/** @description Counter of `already processed` files. */
				processed_files_count?: number;
				/** @description Counter of files which `cannot` be processed because `invalid content type`, `size`, `duplication` etc.. */
				unprocessed_files_count?: number;
				/** @description Counter of files containing content like `navigation links`, `advertisement`, `headers`, `footers` etc.. */
				invalid_content_types_count?: number;
				/** @description Cannot return count. */
				unable_to_retrieve_count?: number;
				/** @description Counter for sizes that are bigger or smaller as defined limits (max_file_size, min_file_size). */
				invalid_size_count?: number;
				/** @description Counter for sizes that are bigger or smaller as defined limits (max_file_size, min_file_size). */
				invalid_cleaned_size_count?: number;
				/** @description Amounth of filter usage. */
				keywords_filter_applied_count?: number;
				/** @description Counter for files whose format cannot be converted to one of the supported formats. */
				unable_to_convert_count?: number;
				/** @description Counter for files with repeating content. */
				duplicate_count?: number;
				/** @description Duration of words gathering with web-crawler (in seconds). */
				time_elapsed?: number;
				/** @description Average time to process single file (in seconds). */
				average_file_processing_time?: number;
			};
		};
		"63_language": {
			/** @description Language iso-code. `ISO 639-1`. */
			id?: string;
			/** @description Language name in `English`. */
			name?: string;
			/** @description Language name in that language. */
			autonym?: string;
			/** @description `Tagset ID.` Tagset is list of part-of-speech tags (POS tags) for specified language. Defaulty preselected to the most relevant one. For user corpora can be changed. The terms `tagset` and `templates` are interchangeable. */
			default_tagset_id?: string;
			/** @description Default `reference` corpus. */
			reference_corpus?: string;
			/** @description True if `term extraction` is supported. */
			has_term_grammar?: boolean;
			/** @description Used script. Example: `Latin`, `Cyrillic`, etc. */
			script?: string;
		};
		"69_rpc_style": {
			/** @description Represent whether request was finished successfully or not. */
			result?: boolean;
			/** @description Unexpected server error. Example: `QUOTA_EXCEEDED`. If none Null. */
			error?: string;
		};
		"78_somefiles_post": {
			data?: {
				/** @description An alphanumeric `somefile ID`. */
				id?: string;
				/** @description Name of `multilingual file`. */
				name?: string;
				/** @description File type of multilingual file: `.tmx`, .`.xlsx`, etc. */
				file_type?: string;
				/** @description Unique numeric `owner ID` (usually you). */
				owner_id?: number;
				/** @description Is document temporary or not. */
				temporary?: boolean;
				/** @description Encoding standard of the document. Usually `UTF-8`. */
				encoding?: string;
				/** @description An object of automatically guessed languages of inserted files during multilingual corpus creation. Maximum: `2`, because Sketch Engine support multilingual corpora only from 2 languages yet. */
				guessed_languages?: {
					language_1?: string;
					language_2?: string;
				};
			};
		};
		"79_template": {
			/** @description Alphanumeric `template/tagset ID`. The terms `tagset` and `templates` are interchangeable. */
			id?: string;
			/** @description Name of `template/tagset file`. */
			name?: string;
			/** @description Unique numeric `owner ID` (usually you). If tagset/template is preloaded `Null`. */
			owner_id?: number;
			/** @description Tagset/template `owner name` (usually you). If tagset/template is preloaded `Null`. */
			owner_name?: string;
			/** @description Vertical creation is supported. False for legacy templates. */
			has_pipeline?: boolean;
			/** @description Morphological tagging is supported. */
			has_tags?: boolean;
			/** @description Lemmatization is supported. */
			has_lemmas?: boolean;
			/** @description A list of attributes which can appear in corpus. */
			static_attributes?: Array<string>;
			/** @description A list of used structures. Examples `<s>`, `<g>`. */
			structures?: Array<string>;
			/** @description `URL` leading to template/tagset documentation. */
			tagsetdoc?: string;
			/** @description Content of tagset. */
			content?: string;
			/** @description Not ID, as you probably imagine, but filename of preselected sketchgrammar (`.m4` format). */
			default_sketchgrammar_id?: string;
			/** @description Not ID, as you probably imagine, but filename of preselected sketchgrammar (`.m4` format). */
			default_termgrammar_id?: string;
			sharing?: {
				users?: Array<{
					/** @description The ID of user you share template with. */
					id?: number;
					/** @description The name of user you share template with. */
					name?: string;
					/** @description The email of user you share template with. */
					email?: string;
				}>;
				user_group?: Array<{
					/** @description The ID of group you share template with. */
					id?: number;
					/** @description The name of group you share template with. */
					name?: string;
				}>;
			};
		};
		"80_template_put": {
			/** @description Alphanumeric `template/tagset ID`. The terms `tagset` and `templates` are interchangeable. */
			id?: string;
			/** @description Name of `template/tagset file`. */
			name?: string;
			/** @description Unique numeric `owner ID` (usually you). If tagset/template is preloaded `Null`. */
			owner_id?: number;
			/** @description Tagset/template `owner name` (usually you). If tagset/template is preloaded `Null`. */
			owner_name?: string;
			/** @description Vertical creation is supported. False for legacy templates. */
			has_pipeline?: boolean;
			/** @description Morphological tagging is supported. */
			has_tags?: boolean;
			/** @description Lemmatization is supported. */
			has_lemmas?: boolean;
			/** @description A list of attributes which can appear in corpus. */
			static_attributes?: Array<string>;
			/** @description A list of used structures. Examples `<s>`, `<g>`. */
			structures?: Array<string>;
			/** @description `URL` leading to template/tagset documentation. */
			tagsetdoc?: string;
			/** @description Content of tagset. */
			content?: string;
			/** @description Not ID, as you probably imagine, but filename of preselected sketchgrammar (`.m4` format). */
			default_sketchgrammar_id?: string;
			/** @description Not ID, as you probably imagine, but filename of preselected sketchgrammar (`.m4` format). */
			default_termgrammar_id?: string;
			sharing?: {
				users?: Array<{
					/** @description The ID of user you share template with. */
					id?: number;
				}>;
				user_group?: Array<{
					/** @description The ID of group you share template with. */
					id?: number;
				}>;
			};
		};
		"81_get_used_space": {
			result?: {
				/** @description The current maximal amount of words in the user's corpora. */
				space_used?: number;
				/**
				 * @description Default maximal amount of words. The default is set to `1 000 000` words. It can be changed.
				 * @example 1000000
				 */
				space_total?: number;
			};
			/** @description Unexpected server error. If none Null. */
			error?: string;
		};
		octerms_dataset: {
			/** @description Randomly generated dataset ID */
			id?: string;
			/** @enum {string} */
			status?: "RUNNING" | "DONE" | "FAILED";
			/** @description progress as percentage */
			progress?: number;
			/** @description error message, if any */
			error?: string;
			results?: {
				/** @description monolingual keywords / source language keywords */
				keywords_1?: Array<unknown>;
				/** @description monolingual terms / source language terms */
				terms_1?: Array<unknown>;
				/** @description target language keywords */
				keywords_2?: Array<unknown>;
				/** @description target language terms */
				terms_2?: Array<unknown>;
				/** @description bilingual terms */
				biterms?: Array<unknown>;
			};
			/** @description bonito parameters */
			settings?: Record<string, never>;
			/** @description dataset options */
			options?: Record<string, never>;
		};
	};
	responses: never;
	parameters: {
		/**
		 * @description Corpus name. To query your own corpus (e.g. username john, corpus mycorpus), `use` value `user/john/mycorpus`.
		 * @example preloaded/bnc2_tt21
		 */
		"001_corpname": string;
		/** @description The name of the `subcorpus`. The `default` value `empty string` refers to the entire corpus. An example for `preloaded/bnc2_tt21` can be `Written Academic` or `1960-1974`. */
		"002_usesubcorp": string;
		/** @description A list of grammatical relations from the correspoding `sketch grammar`. */
		"003_gramrels": 0 | 1;
		/** @description Results of the last corpcheck (if available in the compilation log). */
		"004_corpcheck": 0 | 1;
		/** @description The content of the registry file (registry_dump and registry_text). */
		"005_registry": 0 | 1;
		/** @description A parameter to obtain the list of subcorpora and their sizes (e.g. number of tokens, words). */
		"006_subcorpora": 0 | 1;
		/** @description The lexicon sizes of structure attributes. */
		"007_struct_attr_stats": 0 | 1;
		/** @description The `format` of the output. `Empty value` is interpreted as `JSON`. Not every endpoint supports all formats. */
		"008_format": "json" | "xml" | "csv" | "tsv" | "txt" | "xls";
		/**
		 * @description Sets the corpus attribute you want to work with. Some corpora may have specific additional attributes.
		 *
		 *      Basic examples:
		 *      - word
		 *      - lc
		 *      - lemma
		 *      - lemma_lc
		 *      - lempos
		 *      - lempos_lc
		 *      - tag
		 *      - pos
		 *
		 *      For list of available word list attributes see **\/ca/api/corpora/{corpusId}**
		 */
		"010_wlattr": string;
		/** @description The type of frequency. The values stand for: `frq` -> absolute or raw frequency, `docf` -> document frequency, `arf` -> average reduced frequency, `aldf` -> average logarithmic distance frequency. */
		"011_wlnums": "frq" | "docf" | "arf" | "aldf";
		/** @description Sets the minimum frequency limit. Items with a lower frequency will not be included. */
		"012_wlminfreq": number;
		/**
		 * @description Sets the number of items to be returned in the API response. It is not limited for user corpora, in preloaded corpora can be some limitation. This parameter is often used with wlpage to help with pagination in frontend development.
		 * @example 100
		 */
		"013_wlmaxitems": number;
		/**
		 * @description Sets a regex to filter the results. Relevant only in a simple wordlist.
		 * @example .*
		 */
		"014_wlpat": string;
		/** @description Sets the sorting of the results. The values stand for: `frq` -> absolute or raw frequency, `docf` -> document frequency, `arf` -> average reduced frequency, `aldf` -> average logarithmic distance frequency. */
		"015_wlsort": "frq" | "docf" | "arf" | "aldf";
		/** @description The name of the reference corpus against which the keywords, key n-grams, key collocations or terms will be identified. */
		"016_ref_corpname": string;
		/** @description The name of the reference subcorpus against which the keywords, key n-grams, key collocations or terms will be identified. */
		"017_ref_usesubcorp": string;
		/** @description Defines the allow list (formerly known as whitelist), the list of words which should be included in the list. See also `wlblacklist`. */
		"018_wlfile": string;
		/** @description A deny list (formerly known as blacklist) is a list of items that should be excluded from the result. The values should be be separated by a newline symbol (without commas between values). In the URL, the newline symbol is `%0A`. */
		"019_wlblacklist": string;
		/**
		 * @description Selects a structure attribute (reffered to as text type in the web interface). Corpora have different numbers structure attributes and their values. You can find them in the response of the `corpus_info` method in `freqttattrs` or `subcorpattrs` keys. Not all of them have attributes to show.
		 * @example bncdoc.alltyp
		 */
		"020_avattr": string;
		/** @description A regex to filter the results. Empty string defaults to `.*` (match everything). */
		"021_avpat": string;
		/** @description The number of items to return. */
		"022_avmaxitems": number;
		/** @description The starting index from which to return the results. */
		"023_avfrom": number;
		/**
		 * @description The base form of the word.
		 * @example test
		 */
		"024_lemma": string;
		/** @description The part of speech of the lemma. The concrete values depend on the corpus. If the corpus contains the `lempos` attribute and `lpos` is empty, the result defaults to the most frequent part of speech of the lemma. */
		"025_lpos": "-n" | "-v" | "-j" | "-a" | "-d" | "-i";
		/**
		 * @description The minimum frequency of a collocate.  Collocates with a frequency below this limit will not be displayed. It is an integer, unless set to 'auto'. `Auto` sets the limit based on the frequency of the lemma.
		 * @example auto
		 */
		"027_minfreq": string;
		/** @description The minimum [logDice](https://www.sketchengine.eu/my_keywords/logdice/) score of the collocates. */
		"028_minscore": number;
		/** @description The maximum number of items in each grammatical relation. */
		"029_maxitems": number;
		/** @description Groups collocates by their similarity in meaning. (see `minsim`) in meanings. In the response, a new key `Clust` is added for `Words` in the `Gramrels` key. */
		"030_clustercolls": 0 | 1;
		/** @description The minimum similarity between clustered items. Only used when `clustercolls` is set to 1. Lower values produce more groups containing fewer collocations. Higher values produce fewer groups containing more collocations. The same value can produce different results with different words. */
		"031_minsim": number;
		/** @description Used to expand separate page relations (grammatical relations grouped together, e.g. prepositional phrases, will appear as separate relations). */
		"032_expand_seppage": 0 | 1;
		/** @description The second lemma to compare. Parameter `diff_by` need to be set to `lemma`. */
		"033_lemma_2": string;
		/** @description If set to 1, collocations are sorted into blocks, one for each grammatical relation. If set to 0, only one list is returned, sorted by the score, grammatical relations are ignored. */
		"034_separate_blocks": number;
		/** @description The maximum number of collocates to be returned in single table (common block in response). */
		"035_maxcommon": number;
		/** @description The maximum number of collocates for individual lemma to be returned (e.g. lemma1_only, lemma2_only in the response). Parameter `separate_blocks` needs to be set to `1`. */
		"036_maxexclusive": number;
		/** @description Parameter to set the maximum number of items to be displayed. */
		"037_maxthesitems": number;
		/** @description Parameter for placing words into groups based on their similarity (minsim parameter) in meanings. */
		"038_clusteritems": 0 | 1;
		/** @description Switches the asynchronous processing on/off. ON = partial results are returned as soon as the first page is filled with results. OFF = results are returned only after the search is completed. Normally, ON is used in the web interface and OFF when using the API. */
		"039_asyn": 0 | 1;
		/**
		 * @description The **json** parameter provides and alternative way to wrap parameters for concordance queries (instead of q parameter). It allows you to send all query parameters in a single JSON object. If you use **concordance_query** parameter you should not use the **q** parameter.
		 *
		 *      Provided examples are not complete concordance requests but only single parameter from the request.
		 *
		 *
		 *     ---
		 *
		 *      `Concordance:`
		 *
		 *      ---
		 *
		 *
		 *     - **concordance_query** -> A list of concordance parameters.
		 *
		 *      - **lpos** -> Specifies the part of speech for the lemma. Options: **""** (any), **-n** (noun), **-v** (verb), **-j** (adjective), **-a** (adverb), **-d** (pronoun), **-i** (preposition), **-c** (conjunction), **-m** (numeral).
		 *
		 *      - **qmcase** -> Determines [case sensitivity](https://www.sketchengine.eu/blog/case-sensitive-and-insensitive-corpus-analysis/). If set to false, the data are extracted from a lowercased version of the corpus. **Options:** true (case-sensitive), false (case-insensitive).
		 *
		 *      - **queryselector** -> Defines the query type for concordance calculation. Options: **iqueryrow**, **cqlrow**, **lemmarow**, **charrow**, **phraserow**, **wordrow**. Default is **iqueryrow**.
		 *
		 *      	- **iqueryrow:**
		 *
		 *      		- **iquery** -> For **iqueryrow**. Use symbols like `*` for any number of unspecified characters. Use a `?` for exactly one unspecified character. Use the `|` to include more than one word or phrase. Use `--` to find a word which is hyphenated, non-hyphenated or spelt as two separate words.
		 *
		 *      	- **cqlrow:**
		 *
		 *      		- **cql** -> For **cqlrow**. Write queries using [Corpus query language (cql)](https://www.sketchengine.eu/documentation/corpus-querying/).
		 *
		 *      	- **lemmarow:**
		 *
		 *      		- **lemma** -> For **lemmarow**. Input the lemma. Supports regex.
		 *
		 *      	- **charrow:**
		 *
		 *      		- **char** -> For **charrow**. Define characters within tokens. Supports regex.
		 *
		 *      	- **phraserow:**
		 *
		 *      		- **phrase** -> For **phraserow**. Input the phrase. Supports regex.
		 *
		 *      	- **wordrow:**
		 *
		 *      		- **word** -> For **wordrow**. Input word forms. Supports regex.
		 *
		 *      - **default_attr** -> Defines the default attribute (from queryselector subattributes) type for concordance queries.
		 *
		 *      - **random** -> Optional parameter, set to 1 if you want to use Random Sample. By default 0.
		 *
		 *
		 *     **Examples:**
		 *
		 *      json={"concordance_query":[{"queryselector":"cqlrow","cql":"<doc>","default_attr":""}]}
		 *
		 *      json={"concordance_query":[{"queryselector":"lemmarow","lemma":"car","lpos":"-n","qmcase":false}]}
		 *
		 *      json={"concordance_query":[{"queryselector":"iqueryrow","iquery":"car","random":1},{"q":"r200"}]}
		 *
		 *
		 *     ---
		 *
		 *      `Filters:`
		 *
		 *      ---
		 *
		 *
		 *     Filters are applied as a separate JSON sub-objects, enabling more advanced control over the concordance query. Multiple filters can be chained in a single query.
		 *     - **queryselector** -> Same purpose as in non-filtered queries. Options: **iquery**, **cql**, **lemma**, **char**, **phrase**, **word**.
		 *
		 *      - **pnfilter** -> Possitive/negative filter. Options: **p** (positive), **n** (negative).
		 *
		 *      - **inclkwic** -> Include or exclude keyword in context (KWIC). Options: **true**, **false**.
		 *
		 *      - **lpos** -> Filter by lemma part of speech. Options: **""** (any), **-n** (noun), **-v** (verb), **-j** (adjective), **-a** (adverb), **-d** (pronoun), **-i** (preposition), **-c** (conjunction), **-m** (numeral).
		 *
		 *      - **wpos** -> Word part of speech. Options: J.* (adjective), RB.? (adverb), CC (conjunction), DT (determiner), N.* (noun), CD (numeral), RP (particle), IN (preposition), PP.? (pronoun), V.* (verb).
		 *
		 *      - **qmcase** -> Determines [case sensitivity](https://www.sketchengine.eu/blog/case-sensitive-and-insensitive-corpus-analysis/). If set to false, the data are extracted from a lowercased version of the corpus. **Options:** true (case-sensitive), false (case-insensitive).
		 *
		 *      - **filfpos** -> Define the (front) left from KWIC position for filtering. Example: **-5**.
		 *
		 *      - **filtpos** -> Define the (tail) right from KWIC position for filtering. Example: **5**.
		 *
		 *
		 *     **Examples with filters:**
		 *
		 *      json={"concordance_query":[{"queryselector":"iqueryrow","iquery":"test"},{"pnfilter":"p","inclkwic":true,"iquery":"test","filfpos":"-1:s","filtpos":"1:s","queryselector":"iqueryrow"}]}
		 *
		 *      json={"concordance_query":[{"queryselector":"lemmarow","lemma":"mom","lpos":"-n","qmcase":false},{"queryselector":"lemmarow","pnfilter":"p","inclkwic":true,"lpos":"-n","qmcase":false,"filfpos":-4,"filtpos":4,"lemma":"dad"}]}
		 *
		 *
		 *     ---
		 *
		 *      `Sorting:`
		 *
		 *      ---
		 *
		 *
		 *     Sorts are also applied as a separate JSON sub-object within the concordance_query. See examples for usage.
		 *     - **mlsort_options** -> An array or object for sorting options.
		 *
		 *      - **skey** -> Sort key.
		 *
		 *      - **attr** -> Attribute to sort by. The list of available attributes can be retrieved using the **corpinfo endpoint**.
		 *
		 *      - **ctx** -> Context for sorting.
		 *
		 *      - **bward** -> Direction of sorting (backward or forward).
		 *
		 *      - **icase** -> Case sensitivity for sorting. Options: **i** (insensitive) or empty (sensitive). More details on [case sensitivity](https://www.sketchengine.eu/blog/case-sensitive-and-insensitive-corpus-analysis/).
		 *
		 *
		 *     **Examples with sorts:**
		 *
		 *      json={"concordance_query":[{"queryselector":"cqlrow","cql":"[lemma="car"]","default_attr":"lemma"},{"queryselector":"iqueryrow","pnfilter":"n","inclkwic":false,"lpos":"","wpos":"","qmcase":false,"filfpos":"-2:s","filtpos":"2:s","iquery":"test"},{"mlsort_options":[{"skey":"kw","attr":"word","ctx":"0","bward":"","icase":"i"}]}]}
		 *
		 *
		 *     ---
		 *
		 *      `Parallel Concordance:`
		 *
		 *      ---
		 *
		 *
		 *     Parallel concordance queries can handle multiple aligned corpora.
		 *     - **sel_aligned** -> List of aligned corpora for parallel concordance. Example: **["OS_en"**, **"OS_sk"]**.`
		 *
		 *      - **pcq_pos_neg_<aligned_corpora>** -> Positive or negative match. Options: **pos**, **neg**.
		 *
		 *      - **filter_nonempty_<aligned_corpora>** -> Include unmatched results. Options: **on**, **""** (off).
		 *
		 *      - **queryselector_<aligned_corpora>** -> Query type for aligned corpora. Works similarly to non-parallel queries (see descriptions in Concordance part).
		 *
		 *      	- **iquery_<aligned_corpora>**
		 *
		 *      	- **cql_<aligned_corpora>**
		 *
		 *      	- **lemma_<aligned_corpora>**
		 *
		 *      	- **char_<aligned_corpora>**
		 *
		 *      	- **phrase_<aligned_corpora>**
		 *
		 *      	- **word_<aligned_corpora>**
		 *
		 *
		 *     **Example:**
		 *
		 *      json={"concordance_query": [{"queryselector": "iqueryrow","iquery": "cat","sel_aligned": ["OS_sk"],"queryselector_OS_sk": "iqueryrow","iquery_OS_sk": "mačka","pcq_pos_neg_OS_sk": "pos","filter_nonempty_OS_sk": "on"}]}
		 *
		 *
		 *     ---
		 *
		 *      `Random Sample:`
		 *
		 *      ---
		 *
		 *
		 *     Random sample is used to reduce the number of concordance lines while preserving the representativeness of the sample. The number of lines defined by the user will be randomly selected from all parts of the corpus. A random sample with the same number of lines generated from the same concordance will always produce exactly the same concordance lines. This behaviour is intentional so that different users (e.g. students) can follow the same steps and arrive at the same result. To generate a different sample, a different number should be used. Setting the value to 201 instead of 200 will generate a completely different sample.
		 *     - **queryselector** -> Same purpose as in non-filtered queries.
		 *
		 *      - **q** -> To set the number of lines of random sample. The 'r' in front of the number is required.
		 *
		 *
		 *     **Example:**
		 *
		 *      json={"concordance_query":[{"queryselector":"iqueryrow","iquery":"car","random":1},{"q":"r200"}]}
		 */
		"040_json": string;
		/**
		 * @description ---
		 *
		 *      To query the concordance, use the [CQL](https://www.sketchengine.eu/documentation/cql-basics/)(Corpora Query Language) syntax. This parameter works similarly to the JSON parameter, but it's recommended to use the JSON parameter for more complex queries such as queries with filters or sorts. **Don't use q and json parameters simulataneusly.**
		 *
		 *      ---
		 *
		 *
		 *     **Examples:**
		 *
		 *      ---
		 *
		 *      -> **lc** or **lc suffix** indicates that the lowercased version of the corpus is used. To avoid this, remove the suffix or use **word**.
		 *
		 *      q[lemma_lc="test"] -> Query for the lowercased lemma.
		 *
		 *      q[lc="test"] -> Query for the lowercased word.
		 *
		 *      q[lc="car" | lemma_lc="car"] -> Finds words that match the search word(s) or whose lemma matches the search word. Case insensitive.
		 *
		 *      q[lempos_lc="(pretty)-j"] -> adjective.
		 *
		 *      q[lempos_lc="(quickly)-a"] -> adverb.
		 *
		 *      q[lempos_lc="(but)-c"] -> conjunction.
		 *
		 *      q[lempos_lc="(test)-n"] -> noun.
		 *
		 *      q[lempos_lc="(two)-m"] -> numeral.
		 *
		 *      q[lempos_lc="(for)-i"] -> preposition.
		 *
		 *      q[lempos_lc="(they)-d"] -> pronoun.
		 *
		 *      q[lempos_lc="(run)-v"] -> verb.
		 *
		 *      q[lc="marathon"] [lc="runner"] -> Finds a multi-word phrase exactly as typed.
		 *
		 *      q[lc="running" & tag="J.*"] -> adjective.
		 *
		 *      q[lc="quickly" & tag="RB.?"] -> adverb.
		 *
		 *      q[lc="but" & tag="CC"] -> conjunction.
		 *
		 *      q[lc="a" & tag="DT"] -> determiner.
		 *
		 *      q[lc="car" & tag="N.*"] -> noun.
		 *
		 *      q[lc="car" & tag="NN"] -> noun singular.
		 *
		 *      q[lc="cars" & tag="NNS"] -> noun plural.
		 *
		 *      q[lc="two" & tag="CD"] -> numeral.
		 *
		 *      q[word=".*g.*"] -> Finds tokens containing a specific character or a string of characters.
		 * @example q[lc="test"|lemma_lc="test"]
		 */
		"041_q": string;
		"042_fcrit": string;
		/** @description Parameter to set the minimum frequency limit. Attributes with lower frequency that this limit do not appear in the resulting frequency list. */
		"043_flimit": number;
		/** @description The number of items in one response. */
		"044_fmaxitems": number;
		/** @description The identifier of the sorted column. Use `frq` (default) to sort by frequency. */
		"045_freq_sort": "frq" | "rel";
		/** @description The (positional attribute)[https://www.sketchengine.eu/my_keywords/positional-attribute/] (lemma, word form etc.) used in the computation. */
		"046_cattr": string;
		/** @description The left boundary of the window in which the collocations should be identified. Defined by the token position left or right of KWIC. */
		"047_cfromw": number;
		/** @description The right boundary of the window in which the collocations should be identified. Defined by the token position left or right of KWIC. */
		"048_ctow": number;
		/** @description The minimum frequency of the token in the corpus. */
		"049_cminfreq": number;
		/** @description The minimum frequency of the token in the window defined by `cfromw` and `ctow`. */
		"050_cminbgr": number;
		/** @description Sets the maximum number of items in the response. */
		"051_cmaxitems": number;
		/**
		 * @description Defines the types of statistics (association measures) to be computed.
		 *
		 *     `t` -> T-score
		 *
		 *     `m` -> MI
		 *
		 *     `3` -> MI3
		 *
		 *     `l` -> log likelihood
		 *
		 *     `s` -> min. sensitivity
		 *
		 *     `p` -> MI.log_f
		 *
		 *     `r` -> relative freq.
		 *
		 *     `f` -> absolute freq.
		 *
		 *     `d` -> logDice.
		 *
		 *      To send one value just type the value for example `t`. If you need to send more values write it as `["t","m","d","3","l","s","p"]`.
		 */
		"052_cbgrfns": string;
		/** @description Function according to which the result is sorted. */
		"053_csortfn": "t" | "m" | 3 | "l" | "s" | "p" | "r" | "f" | "d";
		/** @description Name of the subcorpus. */
		"054_subcname": string;
		/** @description Set to `1` if the corpus should be deleted. Only user subcorpora can be deleted. Nothing will be deleted if left empty (default value == 0). */
		"055_delete": 0 | 1;
		/** @description Corpus name of the reference corpus, it must have the same processing (the same attributes, the same term grammar). */
		"056_ref_corpname": string;
		/** @description The smoothing parameter for (simple maths) [https://www.sketchengine.eu/documentation/simple-maths/]. */
		"057_simple_n": 1 | 0;
		/** @description A list of comma-delimited attributes that are returned together with each token. Other examples are:`word, lc, lemma, tag` etc.. */
		"058_attrs": string;
		/** @description Limits the results to items containing only alphanumeric characters. */
		"060_alnum": 1 | 0;
		/** @description Limits the results to items containing at least one alphanumberic character. Words such as 16-year-old or 3D will be included. */
		"061_onealpha": 1 | 0;
		"062_max_keywords": number;
		/** @description The minimum n-gram length. Usually used with `ngrams_max_n` and `usengrams`. */
		"063_ngrams_n": 2 | 3 | 4 | 5;
		/** @description Switches the sorting of the collocates between `s` (by the score)and `f` (by the absolute frequency). */
		"065_sort_ws_columns": "s" | "f";
		/** @description Swithes between results grouped into grammatical relations (as in the default web interface view) or results presented as one long list of collocations not classified into grammatical relations. */
		"066_structured": 1 | 0;
		/** @description Used with `bilingual` word sketches. Selects the second corpus to which the corresponding grammatical relations should be aligned. `Example:` 'preloaded/detenten18_rft3'. */
		"067_bim_corpname": string;
		/** @description Used with `bilingual` word sketches. Sets the `lemma` in the second corpus. */
		"068_bim_lemma": string;
		/** @description Sets the lpos, the part of speeach of the lemma in the of second corpus. Used in bilingual word sketches. */
		"069_bim_lpos": "-n" | "-v" | "-j" | "-a" | "-d" | "-i";
		/** @description Sets the case sensitivity of the corpus, i.e. the data are extracted from a lowercased version of the corpus. It is used for case insensitive analysis. Parameter "1" means case sensitivity, "0" means case insensitivity. */
		"071_wlicase": 1 | 0;
		/** @description Sets the maximum frequency limit in the wordlist. */
		"072_wlmaxfreq": number;
		/** @description Includes, or excludes, nonwords in the in the result. Nonwords are tokens which do not start with letter of the alphabet (e.g. numbers, punctuation). The regex to match the nonwords is `[^[:alpha:]].*`. Certain specialized corpora may use their own specific definition of nonwords. */
		"073_include_nonwords": 1 | 0;
		/**
		 * @deprecated
		 * @description Parameter that represents if the wordlist is created from the first 10 milions lines of corpus. One if yes, no if he wordlist is created from the whole corpus.
		 */
		"074_random": 1 | 0;
		"076_min_unary_score": number;
		/** @description The attribute applied to tokens in the query which do not have an attribute specified explicitly as part of the query. */
		"077_default_attr": string;
		/** @description The text type (metadata) for which statistics should be calculated from the concordance. The default is `bncdoc.alltyp` (all available text types are included). Text types (attributes and there values) differ between corpora). You can find them in the response of `corpus_info` method in `freqttattrs` or `subcorpattrs` keys. Not all of them have attributes to show. */
		"078_refs": string;
		/** @description Determines which tokens will be returned with additional attributes defined in `attrs`. `kw` will add the attributes to the KWIC only. `all` will return them with all tokens. */
		"079_attr_allpos": "kw" | "all";
		/**
		 * @description Switches between sentence view, KWIC view and align.
		 *
		 *     `sen` returns complete sentences without trimming them.
		 *
		 *     `kwic` returns the KWIC view with the query in the centre and some context left and right.
		 *
		 *     `align` parallel concordance
		 */
		"080_viewmode": "sen" | "kwic" | "align";
		/**
		 * @description Only used with error-annotated corpora. It determines what should be highlighted. It is set to 'q' for corpora without error annotation. Meaning of individual options:
		 *
		 *      - `q` -> to higlight query result.
		 *
		 *      - `e` -> to higlight errors.
		 *
		 *      - `c` -> to highlight corrections.
		 *
		 *      - `b` -> to highlight both erros and corrections.
		 *
		 *      Example of such a corpus can be `preloaded/enwiki_error_sample_sentences`.
		 */
		"081_cup_hl": "q" | "e" | "c" | "b";
		/** @description A list of comma-delimited structures (=structure tags) that should be included in the result. */
		"082_structs": string;
		/** @description The number of the page that should be returned. */
		"083_fromp": number;
		/** @description The number of lines in the concordance. */
		"084_pagesize": number;
		/** @description The size of the left context in KWIC view. Number of tokens. */
		"085_kwicleftctx": string;
		/** @description The size of the right context in KWIC view. Number of tokens. */
		"086_kwicrightctx": string;
		/** @description The maximum n-gram length. The maximum is `6`. */
		"087_ngrams_max_n": 2 | 3 | 4 | 5 | 6;
		/** @description Represent if n-grams should be extracted or just simple keywords. */
		"088_usengrams": 0 | 1;
		/**
		 * @deprecated
		 * @description Parameter to set the format of ouput. Is it always set to `simple`, for the `struct_wordlist` is another enpoint called `struct_wordlist`.
		 */
		"089_wltype": "simple" | "struct_wordlist";
		/** @description Sets the attributes used for generating the wordlist. Up to 3 attributes are allowed (see wlstruct_attr2 and wlstruct_attr3). Some corpora may contain additional specific attributes. */
		"090_wlstruct_attr1": "word" | "lemma" | "tag" | "lempos";
		/** @description Includes the relative frequency of each item in the result. */
		"091_relfreq": 1 | 0;
		/** @description Calculate the document frequency for each item in the result. Must be used with `addfreqs` set to `docf`. */
		"092_reldocf": 1 | 0;
		/** @description To select page of the response. The number of items on the page is specified by parameter wlmaxitems. */
		"093_wlpage": number;
		/** @description The number of the response batch (page). The number of items in each batch is specified by `fmaxitems`. */
		"094_fpage": number;
		/** @description If there are more attributes (e.g. m1attr, m2attr), the results can be grouped by the first column/attribute. */
		"095_group": 1 | 0;
		/** @description Includes the percentage of the concordance in the result. */
		"096_showpoc": 1 | 0;
		/** @description Includes relative in text types value in the result. */
		"097_showreltt": 1 | 0;
		/** @description Includes the relative frequency in the result. */
		"098_showrel": 1 | 0;
		/** @description The number of attributes for which the frequencies should be counted. */
		"099_freqlevel": 1 | 2 | 3 | 4 | 5 | 6;
		/** @description Used to count the frequency of positional attributes [attributes](https://www.sketchengine.eu/my_keywords/positional-attribute/) or structure attributes (metadata/text types) of any token in the concordance.\n\n A maximum of 6 attributes is allowed (e.g. ml2attr, ml3attr). At least one attribute is required. */
		"100_ml1attr": string;
		/**
		 * @description Position of the selected attribute in the concordance. **Minus** means **left** context (-1<0). **Plus** means **right** context (6>0). **At least one attribute is required, the others are optional.** Every attribute (ml1attr, ml2attr. etc.) needs it's **own** context position (e.g. if 3 attributes are selected three context position **needs to** be set ml1ctx, ml2ctx, ml3ctx).
		 *
		 *
		 *     **Positions can be referenced as follows:**
		 *
		 *      `integer number` - where **0** is the first token in **KWIC**, **-1** the rightmost token in the left context etc.
		 *
		 *      `1:x` - where **x** is one of the corpus structures (e.g. “doc” or “s” if the corpus has the particular markup). Its meaning is the first token in the structure, except when it is the right boundary of a range - then it is the last token in the structure. Also, other numbers can be used, e.g. -2:x, 3:x, etc. (-1 is the same as 1 with meaning “structure containing KWIC”)
		 *
		 *      `a<0` - where **a** stands for a position reference as described in the first two points with meaning '**a** positions before/after the firs KWIC position' (so this is equivalent to **a**)
		 *
		 *      `a>0` - where **a** stands for the same position reference with meaning 'positions before/after the last KWIC position'
		 *
		 *      in the previous two points, if **0** is substituted with a natural number **k**, it means 'before/after **k**-th collocation' instead of 'before/after KWIC'. Collocations are special token groups in the context, that can be added using positive filters (see below).
		 *
		 *
		 *     `Ranges` can be referenced as a~b where **a**, **b** stand for token identifiers as above. Examples of positions and ranges:
		 *
		 *      `-1<0` - rightmost token in the left context
		 *
		 *      `3>0` - third token in right context
		 *
		 *      `0>0` - last token in KWIC
		 *
		 *      `0<0` - first token in KWIC
		 *
		 *      `0<0~0>0` - range of KWIC
		 *
		 *      `-1<0~1>0` - range of KWIC with one token from the left context and one from the right context
		 *
		 *      `1:s` - first token in the sentence containing KWIC (or its first token)
		 *
		 *      `1:s>0` - first token in the sentence containing KWIC (or its last token)
		 *
		 *      `0<1` - first token of the first-added collocation.
		 *
		 *
		 *     `Examples:`
		 *
		 *      sword/ **1>0~3>0**
		 *
		 *      sword/ **1>0~3>0**
		 *
		 *      slemma/ **0<0~0>0**
		 *
		 *      sword/i **-1**
		 *
		 *      sword/ **0** word/ir **-1<0** tag/r **-2<0**
		 */
		"101_ml1ctx": string;
		/** @description Additional optional attribute that should be included in the result. */
		"102_wlstruct_attr2": "word" | "lemma" | "tag" | "lempos";
		/** @description Additional optional attribute that should be included in the result. */
		"103_wlstruct_attr3": "word" | "lemma" | "tag" | "lempos";
		/** @description Switches to the `lc` attribute, i.e. the lower-cased version of the corpus to allow case insensitive searching. `1` means that case sensitivity is off, `0` means it is on. */
		"104_icase": 1 | 0;
		/** @description Parameter to set the minimum score for the thesaurus to be shown. */
		"105_minthesscore": number;
		/**
		 * @description The mode of wsdiff.
		 *
		 *     `Lemma` compares two lemmas via their collocates.
		 *
		 *     `Word form` compares two different word forms of the same lemma.
		 *
		 *     `subcorpus` compares the use of the same lemma in two different subcorpora of the same corpus.
		 */
		"106_diff_by": "lemma" | "word form" | "subcorpus";
		/** @description The first [wordform](https://www.sketchengine.eu/my_keywords/word-form/) of the lemma to compare. `diff_by` must be set to `word form`. */
		"107_wordform1": string;
		/** @description The second [wordform](https://www.sketchengine.eu/my_keywords/word-form/) of the lemma to compare. Parameter `diff_by` must be set to `word form`. */
		"108_wordform2": string;
		/** @description The first subcorpus to compare the lemma. `diff_by` must be set to `subcorpus`. */
		"109_subcorp1": string;
		/** @description The second subcorpus for searching collocates for wsdiff of the lemma you want. `diff_by` must be set to `subcorpus`. */
		"110_subcorp2": string;
		/** @description The position of the first token of KWIC in the corpus. */
		"111_pos": number;
		/** @description N-grams which are sub-ngrams of a longer n-gram will be grouped together with the longer n-gram. Nesting only works when a `ngrams_n` and `ngrams_max_n` are different values. */
		"112_nest_ngrams": 1 | 0;
		/** @description Maximum frequency of collocates. Collocates with a frequency above this limit will not be displayed. */
		"113_maxfreq": number;
		/**
		 * @description Switches between the computation of keywords, terms, key n-grams and key collocations. With keywords and n-grams, it also sets the attribute to be used for the computation.
		 *
		 *      For keywords, set to the required attribute, usually `lc`, `word` or `lemma`.
		 *
		 *      For n-grams, set to the required attribute and set `usengrams`, `ngrams_n` and `ngrams_max_n`.
		 *
		 *      For terms, set the attribute to `TERM`.
		 *
		 *      For collocations (word sketch triples, equivalent of using the Word Sketch with AS A LIST option in the web interace), set the attribute to `WSCOLLOC`. Consider using `wlpat`.
		 */
		"115_attr": "lemma" | "word" | "TERM" | "WSCOLLOC";
		"118_res": number;
		"119_normalize": number;
		"120_fc_lemword_window_type": string;
		"121_fc_lemword_wsize": number;
		"122_fc_lemword_type": string;
		"123_fc_pos_wsize": number;
		"124_fc_pos_type": string;
		"125_fc_pos_window_type": number;
		/** @description To set the word part of speech. Options: J.* (adjective), RB.? (adverb), CC (conjunction), DT (determiner), N.* (noun), CD (numeral), RP (particle), IN (preposition), PP.? (pronoun), V.* (verb). */
		"126_wpos": string;
		/** @description (Only for error-annotated corpora.) Determines what should be highlighted. Corr means **correction** and err means **error**. An **example** of such a corpus is `preloaded/enwiki_error_sample_sentences`. */
		"134_errcorr_switch": "corr" | "err";
		/** @description (Only for error-annotated corpora). Determines which error type to higlight. An example of such a corpus is `preloaded/enwiki_error_sample_sentences`. */
		"135_cup_err_code":
			| ".*"
			| "lexicosemantic"
			| "punct"
			| "spelling"
			| "style"
			| "typographical"
			| "unclassified";
		/** @description (Only for error-annotated corpora.) An error token to search. */
		"136_cup_err": string;
		/** @description (Only for error-annotated corpora.) A correction token to search. */
		"137_cup_corr": string;
		/** @description Only used by the web interface. Indicates the number of tokens that should be highlighted in red. */
		"138_hitlen": number;
		/** @description Size of the left context in tokens. */
		"139_detail_left_ctx": number;
		/** @description Size of the right context in tokens. */
		"140_detail_right_ctx": number;
		/** @description Used to count the frequency of positional attributes [attributes](https://www.sketchengine.eu/my_keywords/positional-attribute/). Just like `ml1attr` but optional. */
		"141_ml2attr": string;
		/** @description Position of the selected attribute in the concordance. Minus means left context (-1<0). Plus means right context (6>0). Just like ml1ctx but optional. */
		"142_ml2ctx": string;
		/** @description Used to count the frequency of positional attributes [attributes](https://www.sketchengine.eu/my_keywords/positional-attribute/). Just like `ml1attr` but optional. */
		"143_ml3attr": string;
		/** @description Position of the selected attribute in the concordance. Minus means left context (-1<0). Plus means right context (6>0). Just like ml1ctx but optional. */
		"144_ml3ctx": string;
		/** @description Used to count the frequency of positional attributes [attributes](https://www.sketchengine.eu/my_keywords/positional-attribute/). Just like `ml1attr` but optional. */
		"145_ml4attr": string;
		/** @description Position of the selected attribute in the concordance. Minus means left context (-1<0). Plus means right context (6>0). Just like ml1ctx but optional. */
		"146_ml4ctx": string;
		/** @description Used to count the frequency of positional attributes [attributes](https://www.sketchengine.eu/my_keywords/positional-attribute/). Just like `ml1attr` but optional. */
		"147_ml5attr": string;
		/** @description Position of the selected attribute in the concordance. Minus means left context (-1<0). Plus means right context (6>0). Just like ml1ctx but optional. */
		"148_ml5ctx": string;
		/** @description Used to count the frequency of positional attributes [attributes](https://www.sketchengine.eu/my_keywords/positional-attribute/). Just like `ml1attr` but optional. */
		"149_ml6attr": string;
		/** @description Position of the selected attribute in the concordance. Minus means left context (-1<0). Plus means right context (6>0). Just like ml1ctx but optional. */
		"150_ml6ctx": string;
		/**
		 * @description Sets the minimum frequency of the item.
		 * @example auto
		 */
		"151_minfreq_extract_keywords": string;
		/** @description Sets the maximum frequency of the item. */
		"152_maxfreq_extract_keywords": number;
		/** @description Represent what kind of frequnecy should be calculated. When used with `reldocf` it is set to `docf` to calculate document frequency. */
		"153_addfreqs": string;
		/**
		 * @description An optinal way of **wraping parameters**. It is possible to send all relevant parametres via this parameter only. It is classic JSON format.
		 *
		 *      The most frequent uses are:
		 *
		 *      `queryselector`: To select the query type. Supported options are: **cqlrow**, **iqueryrow**, **lemmarow**, **charrow**, **phraserow**, **wordrow**.
		 *
		 *      `iquery`: Use with `iqueryrow`.
		 *
		 *      `cql`: Use with `cqlrow`.
		 *
		 *      `lemma`: Use with `lemmarow`.
		 *
		 *      `lpos`: The part of speech of the lemma.
		 *
		 *      `qmcase`: Sets the attribute to its lowercased version, i.e. the data are extracted from a lowercased version of the corpus. It used for case insensitive analysis. 1 = case sensitive), 0 = lowercased corpus/case insensitive).
		 *
		 *      `char`: Use with charrow.
		 *
		 *      `phrase`: Use with phraserow.
		 *
		 *      `word`: Use with wordrow
		 *
		 *      `cbgrfns`: If you need to send more cbgrfnsDefines the types of statistics (association measures) to be computed. See example.
		 */
		"154_json_collx": Record<string, never>;
		/** @description Set to `1` if new subcorpus should be created. Subcorpus will not be created if left empty (default value == 0). */
		"155_create": 0 | 1;
		/**
		 * @description Query for creating subcorpora from concordance.
		 *
		 *     The search criteria are specified within brackets following a prefix like `alemma` or `aword`. This prefix often indicates the type of linguistic search (e.g., lemma-based, word-based). The criteria within the brackets can include checks for specific words, lemmas, parts of speech and more, using operators like | (OR), & (AND), and regular expressions.
		 *
		 *     The lists of available attributes, pos tags for specific corpus can be obtained via `/search/corp_info`.
		 *
		 *
		 *     `Examples:`
		 *
		 *
		 *      - Simple word or lemma search in the BNC corpus:
		 *
		 *         **q=alemma,[lc="test" | lemma_lc="test"]**
		 *
		 *
		 *      - Search for nouns with the lemma 'test' in a case-sensitive manner:
		 *
		 *         **q=alemma,[lempos_lc="(test)-n"]**
		 *
		 *
		 *      - Search for verbs with the lemma 'test', case-insensitive:
		 *
		 *         **q=alemma,[lempos="(test)-v"]**
		 *
		 *
		 *      - Searching for a specific phrase 'test' in a case-sensitive manner:
		 *
		 *         **q=aword,[word="test"]**
		 *
		 *
		 *      - Searching for the numeral '1955':
		 *
		 *         **q=alemma,[word="1955" & tag="CD"]**
		 *
		 *
		 *      - Regex-based search for words containing the character 'h':
		 *
		 *         **q=alemma,[word=".\*h.\*"]**
		 *
		 *
		 *      - Complex search involving the lemma 'book' followed by up to three words, then a verb:
		 *
		 *         **q=alemma,[lemma="book"][]{1,3}[tag="V.\*"]**
		 */
		"156_q_subcorp": Record<string, never>;
		/** @description Which corpus structure should be used in new subcorpus. Used withim concordance type of subcorpus. The lists of structures can be obtained via `/search/corp_info`. */
		"157_struct": string;
		/** @description The name of subcorpus you want to rename. */
		"158_subcorp_id": string;
		/** @description A new name for the subcorpus. */
		"159_new_subcorp_name": string;
		/**
		 * @description Used to specify text types for a subcorpus. Takes a JSON object as input, where the key-value pairs define the specific attributes. The attributes can vary depending on the corpus.
		 *
		 *      When using the json parameter in a query, you can define a JSON object with one or more attributes. Each attribute can have a single value or an array of values.
		 *
		 *
		 *     The list of available text types for specific corpus can be obtained via `/search/corp_info` just add `sca_` in front the name (see examples).
		 *
		 *
		 *     `Examples:`
		 *
		 *
		 *      - To create a subcorpus based on a specific spoken text type from the BNC corpus:
		 *
		 *         **{"sca_bncdoc.alltyp":["Spoken context-governed"]}**
		 *
		 *
		 *      - To filter texts from the BNC corpus that are both spoken context-governed and spoken demographic:
		 *
		 *         **{"sca_bncdoc.alltyp":["Spoken context-governed","Spoken demographic"]}**
		 *
		 *
		 *      - To select texts from the BNC corpus from a specific time period (1960-1974):
		 *
		 *         **{"sca_bncdoc.alltim":["1960-1974"]}**
		 *
		 *
		 *      - To create a subcorpus with texts from specific authors and time periods, along with regional specifications:
		 *
		 *         **{"sca_bncdoc.author": ["Author1","Author2",...],"sca_bncdoc.alltim": ["1985-1993","1975-1984"], "sca_bncdoc.wripp": ["UK (unspecific)","Ireland"]}**
		 *
		 *
		 *      - To filter texts from the Ententen corpus based on domain and topic:
		 *
		 *         **{"sca_doc.tld":["org","com"], "sca_doc.topic": ["arts","beauty & fashion","cars & bikes","culture & entertainment"]}**
		 *
		 *
		 *      - For a user-specific corpus, filtering based on document ID and filename:
		 *
		 *         **{"sca_doc.id":["file29173711"],"sca_doc.filename":["Filename.pdf"]}**
		 */
		"160_json_subcorp": Record<string, never>;
		/**
		 * @description **Minus** means **left** context (-1<0). **Plus** means **right** context (6>0).
		 *
		 *
		 *     **Positions can be referenced as follows:**
		 *
		 *      `integer number` - where **0** is the first token, **-1** the rightmost token in the left context etc.
		 *
		 *      `1:x` - where **x** is one of the corpus structures (e.g. “doc” or “s” if the corpus has the particular markup). Its meaning is the first token in the structure, except when it is the right boundary of a range - then it is the last token in the structure. Also, other numbers can be used, e.g. -2:x, 3:x, etc. (-1 is the same as 1 with meaning “structure containing searched word”)
		 *
		 *      `a<0` - where **a** stands for a position reference as described in the first two points with meaning '**a** positions before/after the first searched word position' (so this is equivalent to **a**)
		 *
		 *      `a>0` - where **a** stands for the same position reference with meaning 'positions before/after the last searched word position'
		 *
		 *      in the previous two points, if **0** is substituted with a natural number **k**, it means 'before/after **k**-th collocation' instead of 'before/after KWIC'. Collocations are special token groups in the context, that can be added using positive filters (see below).
		 *
		 *
		 *     `Ranges` can be referenced as a~b where **a**, **b** stand for token identifiers as above. Examples of positions and ranges:
		 *
		 *      `-1<0` - rightmost token in the left context
		 *
		 *      `3>0` - third token in right context
		 *
		 *      `0>0` - last token
		 *
		 *      `0<0` - first token
		 *
		 *      `0<0~0>0` - range
		 *
		 *      `-1<0~1>0` - range with one token from the left context and one from the right context
		 *
		 *      `1:s` - first token in the sentence containing searched word (or its first token)
		 *
		 *      `1:s>0` - first token in the sentence containing searched word (or its last token)
		 *
		 *      `0<1` - first token of the first-added collocation.
		 *
		 *
		 *     `Examples:`
		 *
		 *      sword/ **1>0~3>0**
		 *
		 *      sword/ **1>0~3>0**
		 *
		 *      slemma/ **0<0~0>0**
		 *
		 *      sword/i **-1**
		 *
		 *      sword/ **0**
		 *
		 *      word/ir **-1<0**
		 *
		 *      tag/r **-2<0**
		 */
		"161_ctx": string;
		/** @description A diachronic attribute to be selected. Available attributes **can differ** in corpora. Examples can be **doc.year** or **doc.month**. */
		"162_diaattr": string;
		/**
		 * @description `1`: display results during calculation
		 *     `0`: display results after all data has been calculated (can take quite a lot of time).
		 */
		"163_sse": 1 | 0;
		/** @description Determines which periods are included in the results. It signifies the percentage above the average size, acting as a **limit**. When relative frequency (rel_frq) surpasses this limit, it is discarded (moved to **removed_freqdist** object). */
		"164_threshold": string;
		/**
		 * @description An optinal way of **wraping parameters**. It is possible to send all relevant parametres via this parameter only. It is classic JSON format.
		 *
		 *      `wordlist`: words for which the relative frequency should be counted.
		 */
		"165_json_freqdist": Record<string, never>;
		/**
		 * @description Corpus name. To query your own corpus (e.g. username john, corpus mycorpus), `use` value `user/john/mycorpus`.
		 * @example preloaded/trends_et_3
		 */
		"166_corpname_freqdist": string;
		/** @description A wordlist of words for which the relative frequency should be counted. No exact example is here because it is already set in `JSON` parameter.\Example: ['the','a','lion']. */
		"167_wordlist": string;
		/**
		 * @description Corpus name. To query your own corpus (e.g. username john, corpus mycorpus), `use` value `user/john/mycorpus`.
		 *
		 *      **Example for parallel concordance:** "preloaded/OS_en"
		 * @example preloaded/bnc2_tt21
		 */
		"173_corpname_concordance": string;
		/** @description Numeric corpus ID. For corpora querying. */
		"01_corpus_id": number;
		/** @description Document ID. For document querying. */
		"02_document_id": number;
		/** @description Numerical template ID, but preloaded templates do not have ID but you can query them by their name. Example: `UNIVERSAL_3`. */
		"03_template_id": string;
		/** @description ID of language. ISO 639-1 codes. */
		"04_language_id": string;
		/**
		 * @description Name of log file. Name 'last.log' show the newest log for that corpus.
		 * @example last.log
		 */
		"05_logname": string;
		/** @description ID of file subdirectories. If sets to 0 it will return top-level folder of documents, so if you have a web corpora with folders web1 and web2 it will return web1. */
		"06_fileset_id": number;
		/** @description ID of file subdirectory. **0** stands for default document directory with name `upload`. */
		"07_fileset_id_query": number;
		/** @description Alphanumeric multilanguage file ID */
		"08_somefile_id": string;
		/**
		 * @description File format in which the corpus should be downloaded. Just three formats are supported.
		 * @example txt, vert, tmx
		 */
		"09_format": string;
		/** @description The contents of each file will be enclosed in a XML like structure of the specified name with the filename as its id attribute and the URL (if available) as the url attribute. If empty document boundaries will be lost. Example: `doc`. */
		"10_file_structure": string;
		/** @description Required when you want to download parallel corpora, **when format == tmx.** Specify aligned corpus name. */
		"11_aligned": string;
		/** @description Delay tagging by given number of `seconds`. */
		"18_wait_with_tagging": number;
		/** @description Start corpus compiling after web-crawler finishes downloading content from the internet. */
		"19_compile_when_finished": number;
	};
	requestBodies: never;
	headers: never;
	pathItems: never;
}
export type $defs = Record<string, never>;
export type operations = Record<string, never>;
